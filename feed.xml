<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ajg91.github.io//feed.xml" rel="self" type="application/atom+xml" /><link href="https://ajg91.github.io//" rel="alternate" type="text/html" /><updated>2026-04-19T23:10:05-07:00</updated><id>https://ajg91.github.io//feed.xml</id><title type="html">AJG’s Homepage</title><subtitle>A place for my projects.</subtitle><entry><title type="html">LoRA model: Adapting GPT-2</title><link href="https://ajg91.github.io//posts/large-language-models/lora-adapter/" rel="alternate" type="text/html" title="LoRA model: Adapting GPT-2" /><published>2026-04-19T00:00:00-07:00</published><updated>2026-04-19T00:00:00-07:00</updated><id>https://ajg91.github.io//posts/large-language-models/lora-adapter</id><content type="html" xml:base="https://ajg91.github.io//posts/large-language-models/lora-adapter/"><![CDATA[<p class="post-date">April 19, 2026</p>

<p>This week I will walk through the process of training a model using a low-rank adaptation (LoRA). For this, we will be using the GPT-2 model we’ve aleady used in previous posts. We cannot use the GPT-4.1 nano model since it does not come with adjustable weights.</p>

<p>LoRA is a parameter-efficient fine-tuning (PEFT) technique that introduces small trainable low-rank matrices into layers of a pretrained model while keeping the original model weights frozen (retains pre-trained knowledge) <a class="citation" href="#hu2021lora">[1]</a>. The purpose is to change the model behavior without retraining all the model parameters. It works by training the LoRA adapter (a.k.a. weights) on the information we want to include in the base model and then merging it into the model before inference. Essentially, we inject information into large language models (LLMs) by inserting neural layers between the transformer layers and modify the weights using LoRA, which are a very small fraction compared to the model weights. This is ideal when you want to take a model and make it domain specific instead of expending resources in retraining and adjusting the billions of parameters found in current models.</p>

<p>As always, the first step is to define our parameters in a <code class="language-plaintext highlighter-rouge">.yaml</code> file and use SimpleNamespace to store them. In this case, the parameters are the following:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
  <span class="sh">"</span><span class="s">model_name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">gpt2</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">seed</span><span class="sh">"</span><span class="p">:</span> <span class="mi">42</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">max_token_length</span><span class="sh">"</span><span class="p">:</span> <span class="mi">128</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">lr</span><span class="sh">"</span><span class="p">:</span> <span class="mf">0.0001</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">warmup_ratio</span><span class="sh">"</span><span class="p">:</span> <span class="mf">0.0</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">epochs</span><span class="sh">"</span><span class="p">:</span> <span class="mi">500</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">train_batch_size</span><span class="sh">"</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">strategy</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">steps</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">logging_steps</span><span class="sh">"</span><span class="p">:</span> <span class="mi">5</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">res_path</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">../results/</span><span class="sh">"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Most of these parameters should be familiar from previous posts so I will not go over them. Note that I am logging the training loss every $5$ steps.</p>

<p>Let’s first look as to how we are building the dataset that will be used to train the LoRA adapter. There are many different ways that a dataset can be built. In this case, I just wrapped a list of Q&amp;As in a function and converted it into a <code class="language-plaintext highlighter-rouge">Dataset</code> using the <code class="language-plaintext highlighter-rouge">datasets</code> <a href="https://huggingface.co/docs/datasets/index" title="hf-datasets">library</a> provided by Hugging Face. Here is the list:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">texts</span> <span class="o">=</span> <span class="p">[</span>
    <span class="sh">"</span><span class="s">Q: How do transformers work?</span><span class="se">\n</span><span class="s">A: Transformers process sequences using self-attention.</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">Q: What does physics explain?</span><span class="se">\n</span><span class="s">A: Physics explains natural phenomena using mathematical models.</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">Q: What do machine learning models do?</span><span class="se">\n</span><span class="s">A: Machine learning models learn patterns from data.</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">Q: How do transformers work?</span><span class="se">\n</span><span class="s">A: Transformers process sequences using self-attention mechanisms.</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">Q: What is self-attention?</span><span class="se">\n</span><span class="s">A: Self-attention allows tokens in a sequence to attend to each other.</span><span class="sh">"</span><span class="p">,</span>
<span class="p">]</span>
</code></pre></div></div>

<p>Notice the format of the data. I purposely wrote Q to label the question, included a line break <code class="language-plaintext highlighter-rouge">\n</code> and an A to label the answer. The reason for this is so that the model can know what is meant as a question and what is the answer. While this may not be necessary for more recent models, since I am using GPT-2 I wanted to be sure that the data was properly labeled before passing it into the model. With the data ready, let’s move to creating the LoRA model.</p>

<p>For this task we will use the <a href="https://huggingface.co/PEFT" title="hf-peft">PEFT</a> library from the Hugging Face ecosystem. While we will be focusing exclusively on LoRA, this library contains many different methods for adapting models. The function we will use is:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">create_lora_model</span><span class="p">(</span><span class="n">model</span><span class="p">):</span>
    <span class="n">config</span> <span class="o">=</span> <span class="nc">LoraConfig</span><span class="p">(</span>
        <span class="n">r</span><span class="o">=</span><span class="mi">8</span><span class="p">,</span>
        <span class="n">lora_alpha</span><span class="o">=</span><span class="mi">16</span><span class="p">,</span>
        <span class="n">target_modules</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">c_attn</span><span class="sh">"</span><span class="p">],</span>
        <span class="n">lora_dropout</span><span class="o">=</span><span class="mf">0.1</span><span class="p">,</span>
        <span class="n">bias</span><span class="o">=</span><span class="sh">"</span><span class="s">none</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">task_type</span><span class="o">=</span><span class="sh">"</span><span class="s">CAUSAL_LM</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">fan_in_fan_out</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="nf">get_peft_model</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">config</span><span class="p">)</span>
</code></pre></div></div>

<p>This function takes a model and applies a LoRA configuration. Let’s look at the parameters:</p>

<ol>
  <li><em>r</em>
    <ul>
      <li>The LoRA rank. A higher rank means more parameters to train, while a lower rank means less parameters.</li>
    </ul>
  </li>
  <li><em>lora_alpha</em>
    <ul>
      <li>The scaling factor. This parameter controls how much influence the LoRA adapter contributes relative to the original (frozen) weights.</li>
    </ul>
  </li>
  <li><em>target_modules</em>
    <ul>
      <li>Designate which modules with get augmented by the LoRA adapter. In this case, <code class="language-plaintext highlighter-rouge">c_attn</code> refers to the attention projection modules in GPT-2, whose job is to map the hidden states to vectors used for self-attention and vice versa.</li>
    </ul>
  </li>
  <li><em>lora_dropout</em>
    <ul>
      <li>Standard dropout used in machine learning.</li>
    </ul>
  </li>
  <li><em>bias</em>
    <ul>
      <li>Standard bias used in machine learning.</li>
    </ul>
  </li>
  <li><em>task_type</em>
    <ul>
      <li>Tells PEFT what kind of model you are passing in.</li>
    </ul>
  </li>
  <li><em>fan_in_fan_out</em>
    <ul>
      <li>Dictates how the model weights are stored. For GPT-2, this should be set to True.</li>
    </ul>
  </li>
</ol>

<p>From here, the LoRA model setup is complete. The class that contains all the GPT-2 functionality was covered in great detail in this <a href="https://ajg91.github.io/posts/large-language-models/decoder-gpt-2-example/" title="decoder-blog-post">post</a>. While I’ve made some small modifications since then, I will only show where I create the LoRA model (see the file <code class="language-plaintext highlighter-rouge">model_local.py</code> for more information). The LoRA model is built in the class constructor and looks like the following:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">train_model</span> <span class="o">=</span> <span class="n">AutoModelForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="n">model_name</span><span class="p">)</span>
<span class="n">self</span><span class="p">.</span><span class="n">lora_model</span> <span class="o">=</span> <span class="nf">create_lora_model</span><span class="p">(</span><span class="n">train_model</span><span class="p">)</span>
</code></pre></div></div>

<p>That’s all that’s needed for setting up a LoRA model. Now, all we have to do is evaluate the model, which is done in the Jupyter Notebook (and in this previous <a href="https://ajg91.github.io/posts/large-language-models/decoder-gpt-2-example/" title="decoder-blog-post">post</a>). Let’s look at the parameters of the base and LoRA model. The base model:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">model</span> <span class="o">=</span> <span class="nc">LocalLM</span><span class="p">(</span><span class="n">cfg</span><span class="p">.</span><span class="n">model_name</span><span class="p">)</span>

<span class="n">total_params</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="nf">numel</span><span class="p">()</span> <span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">model</span><span class="p">.</span><span class="n">model</span><span class="p">.</span><span class="nf">parameters</span><span class="p">())</span>
<span class="n">trainable_params</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="nf">numel</span><span class="p">()</span> <span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">model</span><span class="p">.</span><span class="n">model</span><span class="p">.</span><span class="nf">parameters</span><span class="p">()</span> <span class="k">if</span> <span class="n">p</span><span class="p">.</span><span class="n">requires_grad</span><span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Total parameters: </span><span class="si">{</span><span class="n">total_params</span><span class="si">:</span><span class="p">,</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Trainable parameters: </span><span class="si">{</span><span class="n">trainable_params</span><span class="si">:</span><span class="p">,</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>The output is:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Total</span> <span class="n">parameters</span><span class="p">:</span> <span class="mi">124</span><span class="p">,</span><span class="mi">439</span><span class="p">,</span><span class="mi">808</span>
<span class="n">Trainable</span> <span class="n">parameters</span><span class="p">:</span> <span class="mi">124</span><span class="p">,</span><span class="mi">439</span><span class="p">,</span><span class="mi">808</span>
</code></pre></div></div>

<p>So the base model has over one hundred million parameters and they are all trainable. The LoRA model:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">model</span><span class="p">.</span><span class="n">lora_model</span><span class="p">.</span><span class="nf">print_trainable_parameters</span><span class="p">()</span>
</code></pre></div></div>

<p>The output is:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">trainable</span> <span class="n">params</span><span class="p">:</span> <span class="mi">294</span><span class="p">,</span><span class="mi">912</span> <span class="o">||</span> <span class="nb">all</span> <span class="n">params</span><span class="p">:</span> <span class="mi">124</span><span class="p">,</span><span class="mi">734</span><span class="p">,</span><span class="mi">720</span> <span class="o">||</span> <span class="n">trainable</span><span class="o">%</span><span class="p">:</span> <span class="mf">0.2364</span>
</code></pre></div></div>

<p>Now, the total trainable parameters in the LoRA model are $0.24\%$ of the total trainable parameters in the base model. This is a huge difference computationally and much more lightweight overall.</p>

<p>The evaluation set that we will use to evaluate the model is the following:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>eval_prompts = [
    "Q: How do transformers work?\nA:",
    "Q: What does physics explain?\nA:",
    "Q: What do machine learning models do?\nA:",
    "Q: What is a language model?\nA:",
    "Q: What is overfitting?\nA:",
    "Q: What is a LoRA adapter?\nA:",
]
</code></pre></div></div>

<p>As you may note, some of these questions are exactly the same questions as in the training set. The purpose is to make sure that the LoRA model is learning correctly, since we expect the model to output the same answer we trained it with. We also include some questions that are not in the dataset so we can see how well the model adapts to information it was not directly trained on. We evaluate these questions with the base model and with the LoRA model to see the difference in output:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Base model output:
Q: How do transformers work?
A: An inverted S is a function which takes three parameters, and multiplies it with the corresponding magnitude.

LoRA model output:
Q: How do transformers work?
A: Transformers process sequences using self-attention mechanisms. Transformers allow tokens in a sequence to attend each other


Base model output:
Q: What does physics explain?
A: It doesn't necessarily have to be physical. You could say the same thing about quantum mechanics, and

LoRA model output:
Q: What does physics explain?
A: Physics explains natural phenomena using mathematical models. Model results show that objects in a sequence do not exist,


Base model output:
Q: What do machine learning models do?
A: In the past, I've discussed what we're talking about here. We can model a whole range

LoRA model output:
Q: What do machine learning models do?
A: Machine-learning model predictions learn patterns from data. Model results can be self-attributing or


Base model output:
Q: What is a language model?
A: Language models are the basic tools that allow you to create systems, algorithms, and procedures for using information

LoRA model output:
Q: What is a language model?
A: Machine learning models learn patterns from data. Model results are self-attentionable.
Rational


Base model output:
Q: What is overfitting?
A: It's not a new problem. But if the players don't understand how to use it, then

LoRA model output:
Q: What is overfitting?
A: Over fitting allows an object to fit together in a fashion. Objects can be fitted without self-att


Base model output:
Q: What is a LoRA adapter?
A: It's basically the same as an XC line. The difference being that there are two different interfaces

LoRA model output:
Q: What is a LoRA adapter?
A: A self-attention mechanism allows tokens in an array to attend to each other. Tokens can be
</code></pre></div></div>

<p>We see that the LoRA model outputs the answers expected for the questions we trained it on, but the output for the questions we did not include in the training dataset are not adequately answered. While this may be worrisome, it is not surprising. GPT-2’s base capabilities are not as good as recent models. Since the base model for the LoRA adapter was GPT-2, it will most likely only outperform GPT-2 with data it is directly trained on. With a more advanced model, there are much better chances of successful extrapolation to information the LoRA adapter was not directly trained on. Either way, the LoRA model was successfully trained since it could reproduce the answers contained in the training dataset.</p>

<p>We have now learned how LLMs can be adapted in practice using parameter-efficient fine-tuning. We took an existing model and wrapped it with a LoRA adapter. Using this model, we fine-tuned on a small dataset over $500$ epochs and tested the outputs using an evaluation dataset. From the results, we see that the LoRA model out-performed the base model on the data that it was trained on, but did not effectively extrapolate to other information. This was expected behavior since we used GPT-2 as the base model. A non-exhausted list of failure nodes are: training for a small number of epochs, not including enough weights in the LoRA adapter, not having a properly constructed dataset, and cross-contamination between the base and LoRA models (for testing outputs). Overall, we gained hands-on experience with a fine-tuning strategy widely used in the LLM work.</p>

<p>Some extensions involve using a larger base model that is trained on millions of domain-specific examples. With enough resources, one could set up a training scheme that involves hyperparameter tuning to ensure the best adapter is chosen. Additionally, multiple adapters can be trained for various tasks and used in conjunction. This enables flexibility and customization on the outputs without retraining the base model or having one large LoRA model.</p>

<p>The code presented in this post can be found on my Github under the <a href="https://github.com/AJG91/lora-fine-tune" title="lora-repo">lora-fine-tune</a> repo <a class="citation" href="#Garcia2026LoRAFineTune">[2]</a>. Feel free to reach out if you have any questions about what we covered this week. Next time we will combine RAG with a LoRA-adapter model into one framework. Stay tuned!</p>

<ol class="bibliography"><li><span id="hu2021lora">[1] Edward J. Hu, Yelong Shen, Phillip Wallis, et al., <i>LoRA: Low-Rank Adaptation of Large Language Models</i>, arXiv preprint arXiv:2106.09685, 2021. </span></li>
<li><span id="Garcia2026LoRAFineTune">[2] Alberto J. Garcia, <i>Parameter-Efficient Fine-Tuning with LoRA</i>, 2026. Available at: https://github.com/AJG91/lora-fine-tune.</span></li></ol>
<p><a href="/" class="btn">⬅ Back to Home</a></p>]]></content><author><name></name></author><category term="large-language-models" /><summary type="html"><![CDATA[April 19, 2026 This week I will walk through the process of training a model using a low-rank adaptation (LoRA). For this, we will be using the GPT-2 model we’ve aleady used in previous posts. We cannot use the GPT-4.1 nano model since it does not come with adjustable weights. LoRA is a parameter-efficient fine-tuning (PEFT) technique that introduces small trainable low-rank matrices into layers of a pretrained model while keeping the original model weights frozen (retains pre-trained knowledge) [2]. The purpose is to change the model behavior without retraining all the model parameters. It works by training the LoRA adapter (a.k.a. weights) on the information we want to include in the base model and then merging it into the model before inference. Essentially, we inject information into large language models (LLMs) by inserting neural layers between the transformer layers and modify the weights using LoRA, which are a very small fraction compared to the model weights. This is ideal when you want to take a model and make it domain specific instead of expending resources in retraining and adjusting the billions of parameters found in current models. As always, the first step is to define our parameters in a .yaml file and use SimpleNamespace to store them. In this case, the parameters are the following: { "model_name": "gpt2", "seed": 42, "max_token_length": 128, "lr": 0.0001, "warmup_ratio": 0.0, "epochs": 500, "train_batch_size": 1, "strategy": "steps", "logging_steps": 5, "res_path": "../results/" } Most of these parameters should be familiar from previous posts so I will not go over them. Note that I am logging the training loss every $5$ steps. Let’s first look as to how we are building the dataset that will be used to train the LoRA adapter. There are many different ways that a dataset can be built. In this case, I just wrapped a list of Q&amp;As in a function and converted it into a Dataset using the datasets library provided by Hugging Face. Here is the list: texts = [ "Q: How do transformers work?\nA: Transformers process sequences using self-attention.", "Q: What does physics explain?\nA: Physics explains natural phenomena using mathematical models.", "Q: What do machine learning models do?\nA: Machine learning models learn patterns from data.", "Q: How do transformers work?\nA: Transformers process sequences using self-attention mechanisms.", "Q: What is self-attention?\nA: Self-attention allows tokens in a sequence to attend to each other.", ] Notice the format of the data. I purposely wrote Q to label the question, included a line break \n and an A to label the answer. The reason for this is so that the model can know what is meant as a question and what is the answer. While this may not be necessary for more recent models, since I am using GPT-2 I wanted to be sure that the data was properly labeled before passing it into the model. With the data ready, let’s move to creating the LoRA model. For this task we will use the PEFT library from the Hugging Face ecosystem. While we will be focusing exclusively on LoRA, this library contains many different methods for adapting models. The function we will use is: def create_lora_model(model): config = LoraConfig( r=8, lora_alpha=16, target_modules=["c_attn"], lora_dropout=0.1, bias="none", task_type="CAUSAL_LM", fan_in_fan_out=True, ) return get_peft_model(model, config) This function takes a model and applies a LoRA configuration. Let’s look at the parameters: r The LoRA rank. A higher rank means more parameters to train, while a lower rank means less parameters. lora_alpha The scaling factor. This parameter controls how much influence the LoRA adapter contributes relative to the original (frozen) weights. target_modules Designate which modules with get augmented by the LoRA adapter. In this case, c_attn refers to the attention projection modules in GPT-2, whose job is to map the hidden states to vectors used for self-attention and vice versa. lora_dropout Standard dropout used in machine learning. bias Standard bias used in machine learning. task_type Tells PEFT what kind of model you are passing in. fan_in_fan_out Dictates how the model weights are stored. For GPT-2, this should be set to True. From here, the LoRA model setup is complete. The class that contains all the GPT-2 functionality was covered in great detail in this post. While I’ve made some small modifications since then, I will only show where I create the LoRA model (see the file model_local.py for more information). The LoRA model is built in the class constructor and looks like the following: train_model = AutoModelForCausalLM.from_pretrained(model_name) self.lora_model = create_lora_model(train_model) That’s all that’s needed for setting up a LoRA model. Now, all we have to do is evaluate the model, which is done in the Jupyter Notebook (and in this previous post). Let’s look at the parameters of the base and LoRA model. The base model: model = LocalLM(cfg.model_name) total_params = sum(p.numel() for p in model.model.parameters()) trainable_params = sum(p.numel() for p in model.model.parameters() if p.requires_grad) print(f"Total parameters: {total_params:,}") print(f"Trainable parameters: {trainable_params:,}") The output is: Total parameters: 124,439,808 Trainable parameters: 124,439,808 So the base model has over one hundred million parameters and they are all trainable. The LoRA model: model.lora_model.print_trainable_parameters() The output is: trainable params: 294,912 || all params: 124,734,720 || trainable%: 0.2364 Now, the total trainable parameters in the LoRA model are $0.24\%$ of the total trainable parameters in the base model. This is a huge difference computationally and much more lightweight overall. The evaluation set that we will use to evaluate the model is the following: eval_prompts = [ "Q: How do transformers work?\nA:", "Q: What does physics explain?\nA:", "Q: What do machine learning models do?\nA:", "Q: What is a language model?\nA:", "Q: What is overfitting?\nA:", "Q: What is a LoRA adapter?\nA:", ] As you may note, some of these questions are exactly the same questions as in the training set. The purpose is to make sure that the LoRA model is learning correctly, since we expect the model to output the same answer we trained it with. We also include some questions that are not in the dataset so we can see how well the model adapts to information it was not directly trained on. We evaluate these questions with the base model and with the LoRA model to see the difference in output: Base model output: Q: How do transformers work? A: An inverted S is a function which takes three parameters, and multiplies it with the corresponding magnitude. LoRA model output: Q: How do transformers work? A: Transformers process sequences using self-attention mechanisms. Transformers allow tokens in a sequence to attend each other Base model output: Q: What does physics explain? A: It doesn't necessarily have to be physical. You could say the same thing about quantum mechanics, and LoRA model output: Q: What does physics explain? A: Physics explains natural phenomena using mathematical models. Model results show that objects in a sequence do not exist, Base model output: Q: What do machine learning models do? A: In the past, I've discussed what we're talking about here. We can model a whole range LoRA model output: Q: What do machine learning models do? A: Machine-learning model predictions learn patterns from data. Model results can be self-attributing or Base model output: Q: What is a language model? A: Language models are the basic tools that allow you to create systems, algorithms, and procedures for using information LoRA model output: Q: What is a language model? A: Machine learning models learn patterns from data. Model results are self-attentionable. Rational Base model output: Q: What is overfitting? A: It's not a new problem. But if the players don't understand how to use it, then LoRA model output: Q: What is overfitting? A: Over fitting allows an object to fit together in a fashion. Objects can be fitted without self-att Base model output: Q: What is a LoRA adapter? A: It's basically the same as an XC line. The difference being that there are two different interfaces LoRA model output: Q: What is a LoRA adapter? A: A self-attention mechanism allows tokens in an array to attend to each other. Tokens can be We see that the LoRA model outputs the answers expected for the questions we trained it on, but the output for the questions we did not include in the training dataset are not adequately answered. While this may be worrisome, it is not surprising. GPT-2’s base capabilities are not as good as recent models. Since the base model for the LoRA adapter was GPT-2, it will most likely only outperform GPT-2 with data it is directly trained on. With a more advanced model, there are much better chances of successful extrapolation to information the LoRA adapter was not directly trained on. Either way, the LoRA model was successfully trained since it could reproduce the answers contained in the training dataset. We have now learned how LLMs can be adapted in practice using parameter-efficient fine-tuning. We took an existing model and wrapped it with a LoRA adapter. Using this model, we fine-tuned on a small dataset over $500$ epochs and tested the outputs using an evaluation dataset. From the results, we see that the LoRA model out-performed the base model on the data that it was trained on, but did not effectively extrapolate to other information. This was expected behavior since we used GPT-2 as the base model. A non-exhausted list of failure nodes are: training for a small number of epochs, not including enough weights in the LoRA adapter, not having a properly constructed dataset, and cross-contamination between the base and LoRA models (for testing outputs). Overall, we gained hands-on experience with a fine-tuning strategy widely used in the LLM work. Some extensions involve using a larger base model that is trained on millions of domain-specific examples. With enough resources, one could set up a training scheme that involves hyperparameter tuning to ensure the best adapter is chosen. Additionally, multiple adapters can be trained for various tasks and used in conjunction. This enables flexibility and customization on the outputs without retraining the base model or having one large LoRA model. The code presented in this post can be found on my Github under the lora-fine-tune repo [2]. Feel free to reach out if you have any questions about what we covered this week. Next time we will combine RAG with a LoRA-adapter model into one framework. Stay tuned! [1] Edward J. Hu, Yelong Shen, Phillip Wallis, et al., LoRA: Low-Rank Adaptation of Large Language Models, arXiv preprint arXiv:2106.09685, 2021. [2] Alberto J. Garcia, Parameter-Efficient Fine-Tuning with LoRA, 2026. Available at: https://github.com/AJG91/lora-fine-tune. ⬅ Back to Home]]></summary></entry><entry><title type="html">RAG chatbot: Querying documents effectively</title><link href="https://ajg91.github.io//posts/large-language-models/rag-chatbot/" rel="alternate" type="text/html" title="RAG chatbot: Querying documents effectively" /><published>2026-03-08T00:00:00-08:00</published><updated>2026-03-08T00:00:00-08:00</updated><id>https://ajg91.github.io//posts/large-language-models/rag-chatbot</id><content type="html" xml:base="https://ajg91.github.io//posts/large-language-models/rag-chatbot/"><![CDATA[<p class="post-date">March 08, 2026</p>

<p>This week I will demonstrate the basics of building a personal assistant with a focus on retrieval-augmented generation (RAG) to support long-term context without token limits. We will be using the chatbot we developed in the last <a href="https://ajg91.github.io/posts/large-language-models/basic-chatbot/" title="chatbot-post">post</a>, as well as the GPT-2 and GPT-4.1 nano models.</p>

<p>Retrieval-augmented generation (RAG) is an architecture used for language models that combines retrieving information with text generation. As such, RAG is meant to influence the text generated from a languange model. Essentially, one creates a bank filled with information. The information relevant to the query initiated by the user is retrieved from the bank and is then used to create a context prompt. The prompt is passed into the text generating model with clear instructions to base the output on the retrieved information. This architecture allows for outputs grounded in previously obtained knowledge as opposed to relying on what the model learned during its training phase.</p>

<p>Langue models are not easy nor cheap to consistantly train as more information is obtained (see <a href="https://ajg91.github.io/posts/large-language-models/decoder-gpt-2-example/" title="gpt-2-gen-post">post</a>). As such, they do not have access to recent data. When prompted, they can output outdated or even worse, completely wrong answers brought about by hallucinations. With RAG, you have a corpus of information you can periodically update and have the assistant chatbot retrieve the information to use as context for the text generation step without having to change the model’s weights. Additionally, RAG is a way for private companies to store information that is only meant to be accessible by employees. These internal documents can then be queried without including the information into the training set of a model. Overall, RAG provides a way to continuously update knowledge without having to retrain the model.</p>

<p>The first step is to define our parameters in a <code class="language-plaintext highlighter-rouge">.yaml</code> file and use SimpleNamespace to store them. In this case, the parameters are the following:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
  <span class="sh">"</span><span class="s">model_name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">gpt2</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">api_model_name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">gpt-4.1-nano</span><span class="sh">"</span><span class="p">,</span>

  <span class="sh">"</span><span class="s">seed</span><span class="sh">"</span><span class="p">:</span> <span class="mi">42</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">n_turns</span><span class="sh">"</span><span class="p">:</span> <span class="mi">6</span><span class="p">,</span>

  <span class="sh">"</span><span class="s">docs_dir</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">../data/raw/</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">log_dir</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">../outputs/logs/</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">state_dir</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">../outputs/state/</span><span class="sh">"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The only new parameter compared to last post is <code class="language-plaintext highlighter-rouge">docs_dir</code>. As one would imagine, this is where the documents we will use for our RAG assistant are stored.</p>

<p>The chatbot code and Gradio wrapper are basically the same as the previous post. The only changes are two lines that I have now included in the class constructor:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">DOCUMENTS</span> <span class="o">=</span> <span class="nf">iter_jsonl_directory</span><span class="p">(</span><span class="n">docs_dir</span><span class="p">)</span>
<span class="n">self</span><span class="p">.</span><span class="n">retriever</span> <span class="o">=</span> <span class="nc">DocsRetriever</span><span class="p">(</span><span class="n">DOCUMENTS</span><span class="p">)</span>
</code></pre></div></div>

<p>The first line is used to grab all the <code class="language-plaintext highlighter-rouge">.jsonl</code> files in the directory (I’m only including one for proof-of-concept), pass the text through a processing step, append to a list, and output it. The second line initializes the documents retriever and prepares for query. Let’s dive deeper (docstrings are suppressed for brevity. See the file <code class="language-plaintext highlighter-rouge">documents.py</code> for more information):</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">iter_jsonl_directory</span><span class="p">(</span><span class="n">directory</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">]:</span>
    <span class="n">directory</span> <span class="o">=</span> <span class="nc">Path</span><span class="p">(</span><span class="n">directory</span><span class="p">)</span>
    
    <span class="n">all_text</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">file_path</span> <span class="ow">in</span> <span class="n">directory</span><span class="p">.</span><span class="nf">glob</span><span class="p">(</span><span class="sh">"</span><span class="s">*.jsonl</span><span class="sh">"</span><span class="p">):</span>
        <span class="n">all_text</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">process_documents</span><span class="p">(</span><span class="n">file_path</span><span class="p">))</span>
    
    <span class="k">return</span> <span class="p">[</span><span class="n">text</span> <span class="k">for</span> <span class="n">subtext</span> <span class="ow">in</span> <span class="n">all_text</span> <span class="k">for</span> <span class="n">text</span> <span class="ow">in</span> <span class="n">subtext</span><span class="p">]</span>

<span class="k">def</span> <span class="nf">process_documents</span><span class="p">(</span><span class="n">path</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">]:</span>
    <span class="n">DOCUMENTS</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">doc</span> <span class="ow">in</span> <span class="nf">iter_jsonl</span><span class="p">(</span><span class="n">path</span><span class="p">):</span>
        <span class="n">DOCUMENTS</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="nf">chunk_text</span><span class="p">(</span><span class="n">doc</span><span class="p">[</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">]))</span>
    <span class="k">return</span> <span class="n">DOCUMENTS</span>

<span class="k">def</span> <span class="nf">chunk_text</span><span class="p">(</span><span class="n">text</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">chunk_size</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">80</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">]:</span>
    <span class="k">return</span> <span class="p">[</span><span class="n">text</span><span class="p">[</span><span class="n">i</span><span class="p">:</span><span class="n">i</span><span class="o">+</span><span class="n">chunk_size</span><span class="p">]</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">text</span><span class="p">),</span> <span class="n">chunk_size</span><span class="p">)]</span>
</code></pre></div></div>

<p>Since I’ve already described the primary purpose of the first function, let’s just into the next two functions, which is the processing step. For this example, the processing only entails breaking the text apart, known as chunking. For our purposes, we are breaking up the text at every $80$ characters.</p>

<p>What is the purpose of breaking the text up? Context windows have finite length. If you pass text longer than your context window, the text will get truncated. We want to avoid that since that would mean losing information crucial for the model output. Another issue could be giving too much context that the model ends up loosing the point of the query. You can image having a manager tasking you with priorities, but if he ends up dragging on for 30 minutes, it’ll be difficult to figure out exactly what he wants since you can become over-saturated with information. The same thing can happen with these models. To avoid any issues, we break the text up into managable pieces that we can insert into the context window.</p>

<p>When chunking the text, we also have to be conscious of what length to break the text up. I’ve already talked about what happens if the text is too long, but what if it’s not long enough? Going back to the manager example, if you manager gives you a one sentence priority list, this may not be a good thing because you may not have the context of the problem. Without context, you may solve the problem a way that is not valuable to the customer. The same thing occurs with these models. If your text is too short, the model will not have enough context to properly produce an output.</p>

<p>The next step is passing the text into the documents retriever. The code for this is (docstrings are suppressed for brevity. See the file <code class="language-plaintext highlighter-rouge">retriever.py</code> for more information):</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">DocsRetriever</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">documents</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">],</span> 
        <span class="n">model</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">all-MiniLM-L6-v2</span><span class="sh">"</span>
    <span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">documents</span> <span class="o">=</span> <span class="n">documents</span>
        <span class="n">self</span><span class="p">.</span><span class="n">model</span> <span class="o">=</span> <span class="nc">SentenceTransformer</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>

        <span class="n">embeddings</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">model</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">documents</span><span class="p">,</span> <span class="n">convert_to_numpy</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">index</span> <span class="o">=</span> <span class="n">faiss</span><span class="p">.</span><span class="nc">IndexFlatL2</span><span class="p">(</span><span class="n">embeddings</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span>
        <span class="n">self</span><span class="p">.</span><span class="n">index</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="n">embeddings</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">retrieve</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> 
        <span class="n">k</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">1</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">]:</span>
        <span class="n">query_emb</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">model</span><span class="p">.</span><span class="nf">encode</span><span class="p">([</span><span class="n">query</span><span class="p">],</span> <span class="n">convert_to_numpy</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">_</span><span class="p">,</span> <span class="n">idcs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">index</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span><span class="n">query_emb</span><span class="p">,</span> <span class="n">k</span><span class="p">)</span>
        <span class="k">return</span> <span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">documents</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="n">idcs</span><span class="p">[</span><span class="mi">0</span><span class="p">]]</span>
</code></pre></div></div>

<p>This class is designed with the purpose of mapping text to vectors and to retrieve the documents that lie close to the query. In the constructor, we load a sentence-transformer model provided by Hugging Face called <em>all-MiniLM-L6-v2</em>. This model is nice to use because it’s embedding size is on the smaller end making it fast, but more complicated systems may need a bigger model. You can read more about it <a href="https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2" title="sentence-transformer">here</a>. Now, we use this model calculate the embeddings, which is the portion that converts text to a dense vector, and returns them as a numpy array. Next, we create an index that we can use to organize the documents. For our example, we are using the FAISS (Facebook AI Similarity Search) library <a class="citation" href="#douze2024faiss">[1]</a>. This library finds the nearest neighbors to a query vector efficiently. Since this is for proof-of-concept, we use a Euclidean distance, brute-force search. More efficient searches can be implemented, but <code class="language-plaintext highlighter-rouge">IndexFlatL2</code> is enough for our example. More information can be found <a href="https://github.com/facebookresearch/faiss/wiki/Faiss-indexes" title="faiss-index">here</a>. Finally, the embeddings are assigned an index.</p>

<p>When we go to retrieve, we pass in the query, created embeddings for the query using <code class="language-plaintext highlighter-rouge">encode</code>, and perform a search over all documents to find the one that lies closest to the query. As a note, we have set $k = 1$, which means that the search will return the highest matching document. Other options can be setting $k &gt; 1$ to return other documents that may also lie close, and using other algorithms to determine the best output. That is all it takes to retrieve documents for a query.</p>

<p>The last step is to inject the documents queried into the prompt. For this, we write functions that put together the prompt similar to the chatbot. The only difference is that now we purposely indicate to the model which information it should use to generate output text. I won’t copy over all the functions since they are mostly identical to what I showed in the previous post. I will only show the difference:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">return </span><span class="p">(</span>
    <span class="sa">f</span><span class="sh">"</span><span class="s">System prompt: </span><span class="si">{</span><span class="n">SYSTEM_PROMPT</span><span class="si">}</span><span class="se">\n\n</span><span class="sh">"</span>
    <span class="sa">f</span><span class="sh">"</span><span class="s">Use ONLY the information below to answer.</span><span class="se">\n</span><span class="sh">"</span>
    <span class="sa">f</span><span class="sh">"</span><span class="s">Context:</span><span class="se">\n</span><span class="si">{</span><span class="n">context</span><span class="si">}</span><span class="se">\n\n</span><span class="sh">"</span>
    <span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">history_text</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span>
    <span class="sa">f</span><span class="sh">"</span><span class="s">User: </span><span class="si">{</span><span class="n">user_message</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span>
    <span class="sa">f</span><span class="sh">"</span><span class="s">Assistant:</span><span class="sh">"</span>
<span class="p">)</span>
</code></pre></div></div>

<p>We can see that when we inject the retrieved information into the prompt, we literally just add it into the prompt! We also specify to the model to answer the question using only the context obtained form the documents query. This step serves to constrain the text generation to only the context provided. Now, the model will know that it needs to use the specific information gathered from the documents to answer the user’s query. Note that we also keep the history (memory state) as before, since a second query may depend on the first.</p>

<p>All the code I’ve shown is incorporated into the chatbot from last post in the constructor and in the <code class="language-plaintext highlighter-rouge">chat</code> method, where the function call to the retriever happens and is shown below:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">chat</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">question</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="n">docs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">retriever</span><span class="p">.</span><span class="nf">retrieve</span><span class="p">(</span><span class="n">question</span><span class="p">)</span>
    
    <span class="n">prompt</span> <span class="o">=</span> <span class="nf">build_prompt_with_history</span><span class="p">(</span>
        <span class="n">self</span><span class="p">.</span><span class="n">memory</span><span class="p">.</span><span class="nf">last_n_turns</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">n_turns</span><span class="p">),</span> 
        <span class="n">docs</span><span class="p">,</span>
        <span class="n">question</span>
    <span class="p">)</span>

    <span class="n">text</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">lm</span><span class="p">.</span><span class="nf">generate_text</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>
    <span class="n">reply</span> <span class="o">=</span> <span class="nf">extract_assistant_reply</span><span class="p">(</span><span class="n">text</span><span class="p">)</span>

    <span class="n">self</span><span class="p">.</span><span class="n">memory</span><span class="p">.</span><span class="nf">add_user</span><span class="p">(</span><span class="n">question</span><span class="p">)</span>
    <span class="n">self</span><span class="p">.</span><span class="n">memory</span><span class="p">.</span><span class="nf">add_assistant</span><span class="p">(</span><span class="n">reply</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">reply</span>
</code></pre></div></div>

<p>That is essentially all that changes (besides some parameter passing). Like before, the chat logs and conversation states are also saved, but I won’t include them since we covered that in the last post. Before we move on, we should see what the document we will be using looks like:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
    <span class="sh">"</span><span class="s">id</span><span class="sh">"</span><span class="p">:</span><span class="sh">"</span><span class="s">doc_001</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span><span class="sh">"</span><span class="s">Quantum entanglement describes correlations between particles that cannot be explained classically.</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">:</span><span class="sh">"</span><span class="s">physics</span><span class="sh">"</span>
<span class="p">}</span>
<span class="p">{</span>
    <span class="sh">"</span><span class="s">id</span><span class="sh">"</span><span class="p">:</span><span class="sh">"</span><span class="s">doc_002</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span><span class="sh">"</span><span class="s">The Higgs boson explains how particles acquire mass through interaction with the Higgs field.</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">:</span><span class="sh">"</span><span class="s">physics</span><span class="sh">"</span>
<span class="p">}</span>
<span class="p">{</span>
    <span class="sh">"</span><span class="s">id</span><span class="sh">"</span><span class="p">:</span><span class="sh">"</span><span class="s">doc_003</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span><span class="sh">"</span><span class="s">Transformers rely on self-attention mechanisms to model token relationships.</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">:</span><span class="sh">"</span><span class="s">ml</span><span class="sh">"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Each piece of information contains an ID and a topic. The ID is used so that you know which piece of information will be used in that specific query can be identified and logged. The topic can be used for more in-depth and topic-specific queries where one grabs all the information related to a topic. The text is the information that will be queried.</p>

<p>Now that we’ve gone over all the important pieces, let’s run the code and see how well it works. Although initial testing was done with the GPT-2 model, I will only show the GPT-4.1 nano model output since we know it outperforms the GPT-2 model. As before, we will build the user interface (UI) with Gradio <a class="citation" href="#abid2019gradio">[2]</a>.</p>

<p><img src="/assets/images/rag_chatbot/rag_output.png" alt="rag_chatbot" /></p>

<p>From the figure we can see that the questions I inputted are in fact being answered using the information from the document. Of course, the model then adds a bit more information since I have the max output tokens set to $100$, but we can see that the first sentence is just a slight rewording of the information we provided.</p>

<p>We have now created a RAG-based chatbot that uses internal documents to answer user queries. We went over how the query is performed and demonstrated a simple way of processing the information. We also discussed where and how the information gets included to ensure that the model uses it when generating an output. Finally, we showed a basic example as to how the documents can organized. In demonstrating the model output, we saw that our RAG model did in fact used the information obtained from the document to answer our questions.</p>

<p>The RAG process can be scaled by using a different search function since FAISS is CPU-based and <code class="language-plaintext highlighter-rouge">IndexFlatL2</code> is a brute-force nearest-neighbor search. Additionally, the query and embeddings creation is synchronous, which means that it is performed at every user query. An efficient RAG model would pre-compute the embeddings and store them for retrieval. We can also have more complicated documents such as PDFs that we can scrape and store. Ideally, a database can be pre-built to avoid unnecessary computations that account for latency in the output. Another step can be added to the query that involves metadata filtering, such as keyword search, which can greatly improve the fidelity of the output, especially as more information is added to the library the model pulls from.</p>

<p>Note that while RAG is definitely a powerful tool, it is not fool-proof. The text generation from the model is probabilistic and the context obtained from the documents may not be enough to answer a question successfully. The information needed may not be contained in the documents retrieved or perhaps it is ambiguous and the model cannot successfully reason through it. Care must be taken when analyzing outputs to ensure that the model did not hallucinate and produce a wrong solution.</p>

<p>The code presented in this post can be found on my Github under the <a href="https://github.com/AJG91/rag-chatbot" title="rag-chatbot-repo">rag-chatbot</a> repo <a class="citation" href="#Garcia2026RAGChatbot">[3]</a>. Feel free to reach out if you have any questions about what we covered this week. Next time we’ll discuss parameter-efficient fine-tuning using low-rank adaptation (LoRA). Stay tuned!</p>

<ol class="bibliography"><li><span id="douze2024faiss">[1] Matthijs Douze al., <i>The Faiss library</i>, 2024. </span></li>
<li><span id="abid2019gradio">[2] Abubakar Abid, Ali Abdalla, Ali Abid, et al., <i>Gradio: Hassle-Free Sharing and Testing of ML Models in the Wild</i>, arXiv preprint arXiv:1906.02569, 2019. </span></li>
<li><span id="Garcia2026RAGChatbot">[3] Alberto J. Garcia, <i>Conversational Chatbot with Retrieval-Augmented Generation (RAG)</i>, 2026. Available at: https://github.com/AJG91/rag-chatbot.</span></li></ol>
<p><a href="/" class="btn">⬅ Back to Home</a></p>]]></content><author><name></name></author><category term="large-language-models" /><summary type="html"><![CDATA[March 08, 2026 This week I will demonstrate the basics of building a personal assistant with a focus on retrieval-augmented generation (RAG) to support long-term context without token limits. We will be using the chatbot we developed in the last post, as well as the GPT-2 and GPT-4.1 nano models. Retrieval-augmented generation (RAG) is an architecture used for language models that combines retrieving information with text generation. As such, RAG is meant to influence the text generated from a languange model. Essentially, one creates a bank filled with information. The information relevant to the query initiated by the user is retrieved from the bank and is then used to create a context prompt. The prompt is passed into the text generating model with clear instructions to base the output on the retrieved information. This architecture allows for outputs grounded in previously obtained knowledge as opposed to relying on what the model learned during its training phase. Langue models are not easy nor cheap to consistantly train as more information is obtained (see post). As such, they do not have access to recent data. When prompted, they can output outdated or even worse, completely wrong answers brought about by hallucinations. With RAG, you have a corpus of information you can periodically update and have the assistant chatbot retrieve the information to use as context for the text generation step without having to change the model’s weights. Additionally, RAG is a way for private companies to store information that is only meant to be accessible by employees. These internal documents can then be queried without including the information into the training set of a model. Overall, RAG provides a way to continuously update knowledge without having to retrain the model. The first step is to define our parameters in a .yaml file and use SimpleNamespace to store them. In this case, the parameters are the following: { "model_name": "gpt2", "api_model_name": "gpt-4.1-nano", "seed": 42, "n_turns": 6, "docs_dir": "../data/raw/", "log_dir": "../outputs/logs/", "state_dir": "../outputs/state/" } The only new parameter compared to last post is docs_dir. As one would imagine, this is where the documents we will use for our RAG assistant are stored. The chatbot code and Gradio wrapper are basically the same as the previous post. The only changes are two lines that I have now included in the class constructor: DOCUMENTS = iter_jsonl_directory(docs_dir) self.retriever = DocsRetriever(DOCUMENTS) The first line is used to grab all the .jsonl files in the directory (I’m only including one for proof-of-concept), pass the text through a processing step, append to a list, and output it. The second line initializes the documents retriever and prepares for query. Let’s dive deeper (docstrings are suppressed for brevity. See the file documents.py for more information): def iter_jsonl_directory(directory: str) -&gt; list[str]: directory = Path(directory) all_text = [] for file_path in directory.glob("*.jsonl"): all_text.append(process_documents(file_path)) return [text for subtext in all_text for text in subtext] def process_documents(path: str) -&gt; list[str]: DOCUMENTS = [] for doc in iter_jsonl(path): DOCUMENTS.extend(chunk_text(doc["text"])) return DOCUMENTS def chunk_text(text: str, chunk_size: int = 80) -&gt; list[str]: return [text[i:i+chunk_size] for i in range(0, len(text), chunk_size)] Since I’ve already described the primary purpose of the first function, let’s just into the next two functions, which is the processing step. For this example, the processing only entails breaking the text apart, known as chunking. For our purposes, we are breaking up the text at every $80$ characters. What is the purpose of breaking the text up? Context windows have finite length. If you pass text longer than your context window, the text will get truncated. We want to avoid that since that would mean losing information crucial for the model output. Another issue could be giving too much context that the model ends up loosing the point of the query. You can image having a manager tasking you with priorities, but if he ends up dragging on for 30 minutes, it’ll be difficult to figure out exactly what he wants since you can become over-saturated with information. The same thing can happen with these models. To avoid any issues, we break the text up into managable pieces that we can insert into the context window. When chunking the text, we also have to be conscious of what length to break the text up. I’ve already talked about what happens if the text is too long, but what if it’s not long enough? Going back to the manager example, if you manager gives you a one sentence priority list, this may not be a good thing because you may not have the context of the problem. Without context, you may solve the problem a way that is not valuable to the customer. The same thing occurs with these models. If your text is too short, the model will not have enough context to properly produce an output. The next step is passing the text into the documents retriever. The code for this is (docstrings are suppressed for brevity. See the file retriever.py for more information): class DocsRetriever: def __init__( self, documents: list[str], model: str = "all-MiniLM-L6-v2" ): self.documents = documents self.model = SentenceTransformer(model) embeddings = self.model.encode(documents, convert_to_numpy=True) self.index = faiss.IndexFlatL2(embeddings.shape[1]) self.index.add(embeddings) def retrieve( self, query: str, k: int = 1 ) -&gt; list[str]: query_emb = self.model.encode([query], convert_to_numpy=True) _, idcs = self.index.search(query_emb, k) return [self.documents[i] for i in idcs[0]] This class is designed with the purpose of mapping text to vectors and to retrieve the documents that lie close to the query. In the constructor, we load a sentence-transformer model provided by Hugging Face called all-MiniLM-L6-v2. This model is nice to use because it’s embedding size is on the smaller end making it fast, but more complicated systems may need a bigger model. You can read more about it here. Now, we use this model calculate the embeddings, which is the portion that converts text to a dense vector, and returns them as a numpy array. Next, we create an index that we can use to organize the documents. For our example, we are using the FAISS (Facebook AI Similarity Search) library [3]. This library finds the nearest neighbors to a query vector efficiently. Since this is for proof-of-concept, we use a Euclidean distance, brute-force search. More efficient searches can be implemented, but IndexFlatL2 is enough for our example. More information can be found here. Finally, the embeddings are assigned an index. When we go to retrieve, we pass in the query, created embeddings for the query using encode, and perform a search over all documents to find the one that lies closest to the query. As a note, we have set $k = 1$, which means that the search will return the highest matching document. Other options can be setting $k &gt; 1$ to return other documents that may also lie close, and using other algorithms to determine the best output. That is all it takes to retrieve documents for a query. The last step is to inject the documents queried into the prompt. For this, we write functions that put together the prompt similar to the chatbot. The only difference is that now we purposely indicate to the model which information it should use to generate output text. I won’t copy over all the functions since they are mostly identical to what I showed in the previous post. I will only show the difference: return ( f"System prompt: {SYSTEM_PROMPT}\n\n" f"Use ONLY the information below to answer.\n" f"Context:\n{context}\n\n" f"{history_text}\n" f"User: {user_message}\n" f"Assistant:" ) We can see that when we inject the retrieved information into the prompt, we literally just add it into the prompt! We also specify to the model to answer the question using only the context obtained form the documents query. This step serves to constrain the text generation to only the context provided. Now, the model will know that it needs to use the specific information gathered from the documents to answer the user’s query. Note that we also keep the history (memory state) as before, since a second query may depend on the first. All the code I’ve shown is incorporated into the chatbot from last post in the constructor and in the chat method, where the function call to the retriever happens and is shown below: def chat(self, question: str) -&gt; str: docs = self.retriever.retrieve(question) prompt = build_prompt_with_history( self.memory.last_n_turns(self.n_turns), docs, question ) text = self.lm.generate_text(prompt) reply = extract_assistant_reply(text) self.memory.add_user(question) self.memory.add_assistant(reply) return reply That is essentially all that changes (besides some parameter passing). Like before, the chat logs and conversation states are also saved, but I won’t include them since we covered that in the last post. Before we move on, we should see what the document we will be using looks like: { "id":"doc_001", "text":"Quantum entanglement describes correlations between particles that cannot be explained classically.", "topic":"physics" } { "id":"doc_002", "text":"The Higgs boson explains how particles acquire mass through interaction with the Higgs field.", "topic":"physics" } { "id":"doc_003", "text":"Transformers rely on self-attention mechanisms to model token relationships.", "topic":"ml" } Each piece of information contains an ID and a topic. The ID is used so that you know which piece of information will be used in that specific query can be identified and logged. The topic can be used for more in-depth and topic-specific queries where one grabs all the information related to a topic. The text is the information that will be queried. Now that we’ve gone over all the important pieces, let’s run the code and see how well it works. Although initial testing was done with the GPT-2 model, I will only show the GPT-4.1 nano model output since we know it outperforms the GPT-2 model. As before, we will build the user interface (UI) with Gradio [3]. From the figure we can see that the questions I inputted are in fact being answered using the information from the document. Of course, the model then adds a bit more information since I have the max output tokens set to $100$, but we can see that the first sentence is just a slight rewording of the information we provided. We have now created a RAG-based chatbot that uses internal documents to answer user queries. We went over how the query is performed and demonstrated a simple way of processing the information. We also discussed where and how the information gets included to ensure that the model uses it when generating an output. Finally, we showed a basic example as to how the documents can organized. In demonstrating the model output, we saw that our RAG model did in fact used the information obtained from the document to answer our questions. The RAG process can be scaled by using a different search function since FAISS is CPU-based and IndexFlatL2 is a brute-force nearest-neighbor search. Additionally, the query and embeddings creation is synchronous, which means that it is performed at every user query. An efficient RAG model would pre-compute the embeddings and store them for retrieval. We can also have more complicated documents such as PDFs that we can scrape and store. Ideally, a database can be pre-built to avoid unnecessary computations that account for latency in the output. Another step can be added to the query that involves metadata filtering, such as keyword search, which can greatly improve the fidelity of the output, especially as more information is added to the library the model pulls from. Note that while RAG is definitely a powerful tool, it is not fool-proof. The text generation from the model is probabilistic and the context obtained from the documents may not be enough to answer a question successfully. The information needed may not be contained in the documents retrieved or perhaps it is ambiguous and the model cannot successfully reason through it. Care must be taken when analyzing outputs to ensure that the model did not hallucinate and produce a wrong solution. The code presented in this post can be found on my Github under the rag-chatbot repo [3]. Feel free to reach out if you have any questions about what we covered this week. Next time we’ll discuss parameter-efficient fine-tuning using low-rank adaptation (LoRA). Stay tuned! [1] Matthijs Douze al., The Faiss library, 2024. [2] Abubakar Abid, Ali Abdalla, Ali Abid, et al., Gradio: Hassle-Free Sharing and Testing of ML Models in the Wild, arXiv preprint arXiv:1906.02569, 2019. [3] Alberto J. Garcia, Conversational Chatbot with Retrieval-Augmented Generation (RAG), 2026. Available at: https://github.com/AJG91/rag-chatbot. ⬅ Back to Home]]></summary></entry><entry><title type="html">Basic chatbot: Keeping context awareness</title><link href="https://ajg91.github.io//posts/large-language-models/basic-chatbot/" rel="alternate" type="text/html" title="Basic chatbot: Keeping context awareness" /><published>2026-02-08T00:00:00-08:00</published><updated>2026-02-08T00:00:00-08:00</updated><id>https://ajg91.github.io//posts/large-language-models/basic-chatbot</id><content type="html" xml:base="https://ajg91.github.io//posts/large-language-models/basic-chatbot/"><![CDATA[<p class="post-date">February 08, 2026</p>

<p>Welcome back! I hope you had a good holiday season and a great start to the new year. I ended up taking a little more time than I thought. Between holidays, traveling, and getting sick I managed to work on some coding, but could not get around to writing a blog post. Anyways, I’m back with the next post.</p>

<p>This week I will demonstrate the basics of building a personal chatbot with a focus on prompt building and ensuring the chatbot remembers context. The models we will be using are GPT-2, which is the same as the model used in the last <a href="https://ajg91.github.io/posts/large-language-models/decoder-gpt-2-example/" title="decoder-blog-post">post</a>, and GPT-4.1 nano. I will not be fine-tuning the GPT2 model since the point of this post is to show how to build a chatbot that maintains context, but if you are building one with a specific task in mind, you should align it to the task by fine-tuning the model as discussed in the previous post. GPT-4.1 nano does not need fine-tuning.</p>

<p>A chatbot differs from what we’ve previously covered in a specific aspect, which is <strong>context</strong>. A decoder, as in the previous post, is good for single-turn text generation but it will not be able to ‘‘hold’’ a conversation since they are inherently stateless. On the other hand, chatbots need to be able to remember what you previously said in the conversation and keep it for when you ask something related to it. For example, if you ask ‘‘how long does it take to get from San Francisco to Los Angeles?’’ The chatbot will answer your question. But if you then ask ‘‘once there, where can I get some good Mexican food?’’ The chatbot will no longer know that you are traveling from San Francisco to Los Angeles, so it will not know that you are trying to find Mexican food in Los Angeles.</p>

<p>This difference makes chatbots inherently more challenging to design since they must now maintain a dialogue state and generate fluent responses. We must constantly reconstruct the context and feed it back into the model. A few ways of doing this are:</p>

<ol>
  <li>Sliding window memory
    <ul>
      <li>Keeps the last $N$ messages and feeds them back into the model.</li>
      <li>This approach keeps the context prompt relatively small and reduces the computation needed to generate a response.</li>
    </ul>
  </li>
  <li>Summarized memory
    <ul>
      <li>Summarizes past dialogue and keeps last $N$ messages.</li>
      <li>This approach helps preserve important details that may have been discussed early in the conversation.</li>
    </ul>
  </li>
  <li>Structured memory
    <ul>
      <li>Stores information that is considered to be relevant, such as user preferences and tasks.</li>
      <li>This approach can help minimize the amount of information that is contained in the context prompt since it avoids unnecessary and redundant details. It also has the advantage of keeping the memory state separate from the conversation state since it is more so like storing information in a database that the model can reference.</li>
    </ul>
  </li>
</ol>

<p>For our application, we will use the sliding window memory since it is the easiest to implement. In general, care must be taken with this method since long histories may exceed the model’s token window, causing truncation or memory loss of earlier messages. This won’t be an issue in our case since we are just testing the chatbot. Let’s jump into it.</p>

<p>As always, the first step is to define our parameters in a <code class="language-plaintext highlighter-rouge">.yaml</code> file and use SimpleNamespace to store them. In this case, the parameters are the following:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
  <span class="sh">"</span><span class="s">model_name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">gpt2</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">api_model_name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">gpt-4.1-nano</span><span class="sh">"</span><span class="p">,</span>

  <span class="sh">"</span><span class="s">seed</span><span class="sh">"</span><span class="p">:</span> <span class="mi">42</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">n_turns</span><span class="sh">"</span><span class="p">:</span> <span class="mi">6</span><span class="p">,</span>

  <span class="sh">"</span><span class="s">log_dir</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">../outputs/logs/</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">state_dir</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">../outputs/state/</span><span class="sh">"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>There are a few new parameters here. Let’s describe them:</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">n_turns</code>
    <ul>
      <li>This parameter defines how many turns in a conversation the chatbot will remember. Here, the chatbot will remember the last <code class="language-plaintext highlighter-rouge">6</code> turns.</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">log_dir</code>
    <ul>
      <li>This parameter indicates the location where the log file will be stored. This file keeps track of everything that occurs in the chat, including time stamps.</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">state_dir</code>
    <ul>
      <li>This parameter indicates the location where the conversation state file will be stored. This keeps track of the chat input and output.</li>
    </ul>
  </li>
</ol>

<p>Before we jump into the class that constructs the chatbot, let’s look at how memory is stored. Below is the memory class (longer docstrings are suppressed for brevity. See the file <code class="language-plaintext highlighter-rouge">memory.py</code> for more information):</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ChatMemory</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">turns</span> <span class="o">=</span> <span class="p">[]</span>

    <span class="k">def</span> <span class="nf">add_user</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">text</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
        <span class="sh">"</span><span class="s">Adds the user</span><span class="sh">'</span><span class="s">s text to turns list.</span><span class="sh">"</span>
        <span class="n">self</span><span class="p">.</span><span class="n">turns</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">User: </span><span class="si">{</span><span class="n">text</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">add_assistant</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">text</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
        <span class="sh">"</span><span class="s">Adds the Assistant</span><span class="sh">'</span><span class="s">s text to turns list.</span><span class="sh">"</span>
        <span class="n">self</span><span class="p">.</span><span class="n">turns</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Assistant: </span><span class="si">{</span><span class="n">text</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">last_n_turns</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">n</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">6</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">]:</span>
        <span class="sh">"</span><span class="s">Grabs the last n turns in the list.</span><span class="sh">"</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">turns</span><span class="p">[</span><span class="o">-</span><span class="n">n</span><span class="p">:]</span>
    
    <span class="k">def</span> <span class="nf">clear_memory</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">path</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">conversation_state.json</span><span class="sh">"</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">Clears the stored conversation (memory) in turns.</span><span class="sh">"""</span>
        <span class="n">self</span><span class="p">.</span><span class="n">turns</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">save</span><span class="p">(</span><span class="n">path</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">save</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">path</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">conversation_state.json</span><span class="sh">"</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">Saves the conversation state to a JSON file.</span><span class="sh">"""</span>
        <span class="n">path</span> <span class="o">=</span> <span class="nc">Path</span><span class="p">(</span><span class="n">path</span><span class="p">)</span>
        <span class="n">path</span><span class="p">.</span><span class="n">parent</span><span class="p">.</span><span class="nf">mkdir</span><span class="p">(</span><span class="n">parents</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">exist_ok</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

        <span class="k">with</span> <span class="n">path</span><span class="p">.</span><span class="nf">open</span><span class="p">(</span><span class="sh">"</span><span class="s">w</span><span class="sh">"</span><span class="p">,</span> <span class="n">encoding</span><span class="o">=</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
            <span class="n">json</span><span class="p">.</span><span class="nf">dump</span><span class="p">({</span><span class="sh">"</span><span class="s">turns</span><span class="sh">"</span><span class="p">:</span> <span class="n">self</span><span class="p">.</span><span class="n">turns</span><span class="p">},</span> <span class="n">f</span><span class="p">,</span> <span class="n">ensure_ascii</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span> <span class="n">indent</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">load</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">path</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">conversation_state.json</span><span class="sh">"</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">Loads the conversation state from a JSON file and adds it to turns.</span><span class="sh">"""</span>
        <span class="n">path</span> <span class="o">=</span> <span class="nc">Path</span><span class="p">(</span><span class="n">path</span><span class="p">)</span>

        <span class="k">if</span> <span class="ow">not</span> <span class="n">path</span><span class="p">.</span><span class="nf">exists</span><span class="p">():</span>
            <span class="n">self</span><span class="p">.</span><span class="n">turns</span> <span class="o">=</span> <span class="p">[]</span>
            <span class="k">return</span>

        <span class="k">with</span> <span class="n">path</span><span class="p">.</span><span class="nf">open</span><span class="p">(</span><span class="sh">"</span><span class="s">r</span><span class="sh">"</span><span class="p">,</span> <span class="n">encoding</span><span class="o">=</span><span class="sh">"</span><span class="s">utf-8</span><span class="sh">"</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
            <span class="n">data</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">f</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">turns</span> <span class="o">=</span> <span class="n">data</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">turns</span><span class="sh">"</span><span class="p">,</span> <span class="p">[])</span>
</code></pre></div></div>

<p>We see that a list labeled as <code class="language-plaintext highlighter-rouge">turns</code> is defined in the constructor. This is were the sliding window memory will be stored. The next two methods appends the user text and the assistant (chatbot) text, respectively. The method that follows indexes the last <code class="language-plaintext highlighter-rouge">n</code> turns in the memory list. In our parameters, we defined <code class="language-plaintext highlighter-rouge">n_turns</code>. This method is where that parameter will be used. The next method clears the <code class="language-plaintext highlighter-rouge">turns</code> list and logs the action. Finally, the last two methods save and loads this list from and to a JSON file. The load method also adds the conversation state to the <code class="language-plaintext highlighter-rouge">turns</code> list. There’s nothing really complicated about this class. It’s pretty straightforward how the memory bookkeeping is done.</p>

<p>Let’s move on to the prompt-building. Below are the functions used to put together the prompt (see the file <code class="language-plaintext highlighter-rouge">prompt_utils.py</code> for more information):</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">SYSTEM_PROMPT</span> <span class="o">=</span> <span class="sh">"</span><span class="s">You are a helpful assistant.</span><span class="sh">"</span>

<span class="k">def</span> <span class="nf">build_prompt</span><span class="p">(</span><span class="n">user_message</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Builds the output prompt template for the chatbot.</span><span class="sh">"""</span>
    <span class="nf">return </span><span class="p">(</span>
        <span class="sa">f</span><span class="sh">"</span><span class="s">System prompt: </span><span class="si">{</span><span class="n">SYSTEM_PROMPT</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span>
        <span class="sa">f</span><span class="sh">"</span><span class="s">User: </span><span class="si">{</span><span class="n">user_message</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span>
        <span class="sa">f</span><span class="sh">"</span><span class="s">Assistant:</span><span class="sh">"</span>
    <span class="p">)</span>

<span class="k">def</span> <span class="nf">build_prompt_with_history</span><span class="p">(</span>
    <span class="n">history_turns</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">],</span> 
    <span class="n">user_message</span><span class="p">:</span> <span class="nb">str</span>
<span class="p">)</span><span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Builds the output prompt template for the chatbot with previous history.</span><span class="sh">"""</span>
    <span class="n">history_text</span> <span class="o">=</span> <span class="sh">"</span><span class="se">\n</span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">history_turns</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">history_text</span><span class="p">.</span><span class="nf">strip</span><span class="p">():</span>
        <span class="nf">return </span><span class="p">(</span>
            <span class="sa">f</span><span class="sh">"</span><span class="s">System prompt: </span><span class="si">{</span><span class="n">SYSTEM_PROMPT</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span>
            <span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">history_text</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span>
            <span class="sa">f</span><span class="sh">"</span><span class="s">User: </span><span class="si">{</span><span class="n">user_message</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span>
            <span class="sa">f</span><span class="sh">"</span><span class="s">Assistant:</span><span class="sh">"</span>
        <span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">return</span> <span class="nf">build_prompt</span><span class="p">(</span><span class="n">user_message</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">extract_assistant_reply</span><span class="p">(</span><span class="n">text</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Extracts the chatbot</span><span class="sh">'</span><span class="s">s reply.</span><span class="sh">"""</span>
    <span class="k">if</span> <span class="sh">"</span><span class="s">Assistant</span><span class="sh">"</span> <span class="ow">in</span> <span class="n">text</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">text</span><span class="p">.</span><span class="nf">rsplit</span><span class="p">(</span><span class="sh">"</span><span class="s">Assistant:</span><span class="sh">"</span><span class="p">,</span> <span class="mi">1</span><span class="p">)[</span><span class="o">-</span><span class="mi">1</span><span class="p">].</span><span class="nf">strip</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">text</span><span class="p">.</span><span class="nf">strip</span><span class="p">()</span>
</code></pre></div></div>

<p>These are just as straightforward as the memory class. Let’s break each part down. Up top, we have a global variable that gives the chatbot instructions. I kept it very simple for this example, but this part can be as long and focused as you need. Think of this variable as your way of telling the chatbot how to act. You can tell it to get straight to the point, be as verbose as possible, or even to be strict and rude. It all depends on what you are looking for in an assistant. The next part put together the template for the prompt. We can see that it has the form</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>System prompt: "
User: "
Assistant:"
</code></pre></div></div>

<p>with the different fields being filled in accordingly. The next function is where memory is taken into account. This function grabs the memory list and appends it to the prompt template in such a way so that the model can first see how you want it to behave, then see the chat history between you and itself, and finally gets to the user input text. If there is no memory, the function defaults to the original template. The final function extracts the chatbot’s reply. This is done so that it can be saved to memory later without saving redundant information such as the system prompt, history, and user message. The details of this step are found in the class that builds the chatbot.</p>

<p>That’s pretty much it for the memory and prompt building! Pretty simple right? Let’s look at how all this is put together by going over the chatbot class (docstrings are suppressed for brevity. See the file <code class="language-plaintext highlighter-rouge">chatbot.py</code> for more information):</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MyChat</span><span class="p">():</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">model_name</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> 
        <span class="n">n_turns</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span>
        <span class="n">state_dir</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span>
        <span class="n">log_dir</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span>
        <span class="n">state_fname</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">conversation_state.json</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">log_fname</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">chat_logs.jsonl</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">api_key</span><span class="p">:</span> <span class="nb">str</span> <span class="o">|</span> <span class="bp">None</span> <span class="o">=</span> <span class="bp">None</span>
    <span class="p">):</span>
        <span class="k">if</span> <span class="n">api_key</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">lm</span> <span class="o">=</span> <span class="nc">OpenAIChat</span><span class="p">(</span><span class="n">model_name</span><span class="p">,</span> <span class="n">api_key</span><span class="o">=</span><span class="n">api_key</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">lm</span> <span class="o">=</span> <span class="nc">LocalLM</span><span class="p">(</span><span class="n">model_name</span><span class="p">)</span>

        <span class="n">state_path</span> <span class="o">=</span> <span class="n">state_dir</span> <span class="o">+</span> <span class="n">state_fname</span>
        <span class="n">log_path</span> <span class="o">=</span> <span class="n">log_dir</span> <span class="o">+</span> <span class="n">log_fname</span>

        <span class="n">self</span><span class="p">.</span><span class="n">memory</span> <span class="o">=</span> <span class="nc">ChatMemory</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">memory</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">state_path</span><span class="p">)</span>

        <span class="n">self</span><span class="p">.</span><span class="n">n_turns</span> <span class="o">=</span> <span class="n">n_turns</span>
        <span class="n">self</span><span class="p">.</span><span class="n">state_path</span> <span class="o">=</span> <span class="n">state_path</span>
        <span class="n">self</span><span class="p">.</span><span class="n">log_path</span> <span class="o">=</span> <span class="n">log_path</span>

    <span class="k">def</span> <span class="nf">chat</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">user_message</span><span class="p">:</span> <span class="nb">str</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
        <span class="n">prompt</span> <span class="o">=</span> <span class="nf">build_prompt_with_history</span><span class="p">(</span>
            <span class="n">self</span><span class="p">.</span><span class="n">memory</span><span class="p">.</span><span class="nf">last_n_turns</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">n_turns</span><span class="p">),</span> 
            <span class="n">user_message</span>
        <span class="p">)</span>
        <span class="n">text</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">lm</span><span class="p">.</span><span class="nf">generate_text</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>
        <span class="n">reply</span> <span class="o">=</span> <span class="nf">extract_assistant_reply</span><span class="p">(</span><span class="n">text</span><span class="p">)</span>
        
        <span class="n">self</span><span class="p">.</span><span class="n">memory</span><span class="p">.</span><span class="nf">add_user</span><span class="p">(</span><span class="n">user_message</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">memory</span><span class="p">.</span><span class="nf">add_assistant</span><span class="p">(</span><span class="n">reply</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">reply</span>
    
    <span class="k">def</span> <span class="nf">respond</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span>
        <span class="n">user_message</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> 
        <span class="n">chat_history</span><span class="p">:</span> <span class="nb">list</span> <span class="o">|</span> <span class="bp">None</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">tuple</span><span class="p">[</span><span class="nb">list</span><span class="p">,</span> <span class="nb">str</span><span class="p">]:</span>
        <span class="k">if</span> <span class="n">chat_history</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">chat_history</span> <span class="o">=</span> <span class="p">[]</span>

        <span class="n">chat_history</span><span class="p">.</span><span class="nf">append</span><span class="p">({</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">user_message</span><span class="p">})</span>

        <span class="k">try</span><span class="p">:</span>
            <span class="n">reply</span> <span class="o">=</span> <span class="nf">str</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">chat</span><span class="p">(</span><span class="n">user_message</span><span class="p">))</span>
        <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
            <span class="n">reply</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">[ERROR] </span><span class="si">{</span><span class="nf">type</span><span class="p">(</span><span class="n">e</span><span class="p">).</span><span class="n">__name__</span><span class="si">}</span><span class="s">: </span><span class="si">{</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span>

        <span class="nf">log_output</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">log_path</span><span class="p">,</span> <span class="n">user_message</span><span class="p">,</span> <span class="n">reply</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">memory</span><span class="p">.</span><span class="nf">save</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">state_path</span><span class="p">)</span>
        <span class="n">chat_history</span><span class="p">.</span><span class="nf">append</span><span class="p">({</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">assistant</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">reply</span><span class="p">})</span>
        <span class="k">return</span> <span class="n">chat_history</span>
    
    <span class="k">def</span> <span class="nf">clear_chat</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">:</span>
        <span class="n">self</span><span class="p">.</span><span class="n">memory</span><span class="p">.</span><span class="nf">clear_memory</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">state_path</span><span class="p">)</span>
        <span class="nf">log_output</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">log_path</span><span class="p">,</span> <span class="sh">"</span><span class="s">Memory cleared</span><span class="sh">"</span><span class="p">,</span> <span class="sh">""</span><span class="p">)</span>
        <span class="k">return</span> <span class="p">[]</span>
</code></pre></div></div>

<p>Looking at the constructor, we see that two different models are given as options for this chatbot: a local model (GPT-2) or a newer OpenAI API model. This class is not limited to these models and can be used for any model as long as the appropriate code is built to utilize it. After the proper paths for the logs are created, the memory class is initialized and the load method is called to load in previous states if they are available.</p>

<p>The next method, called chat, is what builds the prompt. It calls the function <code class="language-plaintext highlighter-rouge">build_prompt_with_history</code> found in the <code class="language-plaintext highlighter-rouge">prompts_utils.py</code> file that we discussed earlier. From here, it passes the prompt to the language model and that generates a response. From that response, the function extracts the reply and adds the user question and the chatbot’s reply to memory. The reply is then outputted. The method that follows puts together all the functionality needed to utilize the chatbot. It takes a list of the chat history and appends the user and the user message to it. It then calls the <code class="language-plaintext highlighter-rouge">chat</code> method to get a response from the language model. This response and the user message are saved to the log file and the conversation state file. The final step is then to append the chatbot’s reply to the list that contains the chat history. Finally, the last method in this class is used to clear the chat history, which is to essentially start a fresh chat. This action is logged in the log file, as well.</p>

<p>We have now gone over how memory is stored, how the prompt is built, and how it is all put together into one coherent class that encompasses the chatbot. We can stop here and we would have created a chatbot that takes in a message and outputs a reply. Instead, we can go one step further and work all this into a user interface (UI) like all the modern chatbots have. For this task we will be using Gradio. The code can be found in the <code class="language-plaintext highlighter-rouge">gradio_ui.py</code> file. I won’t go into the details regarding the code used for implementing the UI since most of it can be found in the <a href="https://www.gradio.app/guides/quickstart" title="gradio-docs">documentation</a> <a class="citation" href="#abid2019gradio">[1]</a>.</p>

<p>For the final step, we write a helper function that instances the <code class="language-plaintext highlighter-rouge">MyChat</code> class and the Gradio interface, and finally launches the UI.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">MyAssistant</span><span class="p">(</span>
    <span class="n">model</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> 
    <span class="n">n_turns</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> 
    <span class="n">state_dir</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> 
    <span class="n">log_dir</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> 
    <span class="n">api_key</span><span class="p">:</span> <span class="nb">str</span> <span class="o">|</span> <span class="bp">None</span> <span class="o">=</span> <span class="bp">None</span><span class="p">,</span>
    <span class="n">share</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span> 
    <span class="n">inline</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">False</span>
<span class="p">):</span>
    <span class="n">bot</span> <span class="o">=</span> <span class="nc">MyChat</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">n_turns</span><span class="p">,</span> <span class="n">state_dir</span><span class="p">,</span> <span class="n">log_dir</span><span class="p">,</span> <span class="n">api_key</span><span class="o">=</span><span class="n">api_key</span><span class="p">)</span>
    <span class="n">demo</span> <span class="o">=</span> <span class="nf">chat_interface</span><span class="p">(</span><span class="n">bot</span><span class="p">)</span>
    <span class="n">demo</span><span class="p">.</span><span class="nf">launch</span><span class="p">(</span><span class="n">share</span><span class="o">=</span><span class="n">share</span><span class="p">,</span> <span class="n">inline</span><span class="o">=</span><span class="n">inline</span><span class="p">)</span>
</code></pre></div></div>

<p>Once you run this function, click the local URL that appears and a window opens that contains the chatbot UI. Here is a screenshot of what that looks like:</p>

<p><img src="/assets/images/basic_chatbot/gradio_chatbot.png" alt="gradio_chatbot" /></p>

<p>We have three boxes with three labels:</p>

<ol>
  <li>Chatbox
    <ul>
      <li>Where the chat takes place</li>
    </ul>
  </li>
  <li>Message
    <ul>
      <li>Where the user types their input</li>
    </ul>
  </li>
  <li>Clear
    <ul>
      <li>Used to clear the chat and wipe the stored memory.</li>
    </ul>
  </li>
</ol>

<p>Let’s test our San Francisco/Los Angeles example and see how well the chatbot responds using the local model:</p>

<p><img src="/assets/images/basic_chatbot/gradio_local_model.png" alt="gradio_chatbot" /></p>

<p>While the local model does produce something coherent, it does not answer our initial question and it definitely did not understand what we were asking. The answer to our follow up question also does not make sense since there is no <em>El Camino Real &amp; Cafe</em> in Los Angeles. The issue with the local model may be that is it not fine-tuned to the problem at hand. It is also an older model so its reasoning is not as good as the current models. Considering both of those options, it is reasonable to believe that the model would not give us what we expected.</p>

<p>Instead, let’s try using a more current model. The model of choice will be GPT-4.1 nano by OpenAI. More information on this model can be found <a href="https://platform.openai.com/docs/models/gpt-4.1-nano" title="gpt-4.1-nano-docs">here</a>. Note that to use this model (and similar models), you need to purchase an API key from OpenAI. <strong>Be careful in integrating your API key with your code since others can also use your API key. This can result in your credit card being overcharged from the unexpected token usage. Read online documentation on how to store and use your API key before you purchase one</strong>. As mentioned before, these models are stateless and do not inherently retain memory between API calls. They only know what you asked it in that moment, not what you asked before or what it previously told you. In other words, even though we are using GPT-4.1 nano from OpenAI, we would still need to build the eco-system (memory, prompt, etc.) around it to be able to use it as a chatbot.</p>

<p>The code to call this model is similar to the one used for the local model (with slight variations) so I will not go over it. See the file <code class="language-plaintext highlighter-rouge">model_openai.py</code> for more information. Let’s ask the same questions as before and see how well the model responds:</p>

<p><img src="/assets/images/basic_chatbot/gradio_openai_model_fig1.png" alt="gradio_chatbot" />
<br /><br />
<img src="/assets/images/basic_chatbot/gradio_openai_model_fig2.png" alt="gradio_chatbot" /></p>

<p>We now see that the chatbot has context! It remembered that we were talking about going to Los Angeles and it gave us food options there. The driving distance and time it takes to get from San Francisco to Los Angeles are correct. The Mexican places that were recommended exist and are found in Los Angeles. Note that option 5 does not finish displaying since we have a cap on the total amount of tokens that will be used for text generation (see the method <code class="language-plaintext highlighter-rouge">generate_text</code> in the <code class="language-plaintext highlighter-rouge">OpenAIChat</code> class).</p>

<p><strong>We’ve successfully built a working chatbot!</strong></p>

<p>Last but not least, let’s review the log files to ensure that they are outputting the information we want. The <code class="language-plaintext highlighter-rouge">chat_log.jsonl</code> file contains the following:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="sh">"</span><span class="s">time</span><span class="sh">"</span><span class="p">:</span> <span class="mf">1770322996.336443</span><span class="p">,</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">How long does it take to get from San Francisco to Los Angeles?</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">assistant</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">I have about 4 hours! It</span><span class="sh">'</span><span class="s">s not that easy, but you can do more than just wait for the bus and then return when we</span><span class="sh">'</span><span class="s">re done with your commute or whatever else matters. Just ask me if there is anything wrong during my time in LA (or even while driving on foot). If so please let me know via email at [email protected]</span><span class="sh">"</span><span class="p">}</span>
<span class="p">{</span><span class="sh">"</span><span class="s">time</span><span class="sh">"</span><span class="p">:</span> <span class="mf">1770323030.933811</span><span class="p">,</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Once there, where can I get some good Mexican food?</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">assistant</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">In El Camino Real &amp; Cafe downtown…if they offer any type of tacos as well..they will gladly help!! There seems only one place anywhere which accepts them : http://www-foods.com/taco...a+cafeteria%20with_deluxe#post17756879</span><span class="sh">"</span><span class="p">}</span>
<span class="p">{</span><span class="sh">"</span><span class="s">time</span><span class="sh">"</span><span class="p">:</span> <span class="mf">1770323316.100607</span><span class="p">,</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Memory cleared</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">assistant</span><span class="sh">"</span><span class="p">:</span> <span class="sh">""</span><span class="p">}</span>
<span class="p">{</span><span class="sh">"</span><span class="s">time</span><span class="sh">"</span><span class="p">:</span> <span class="mf">1770323676.5573509</span><span class="p">,</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">How long does it take to get from San Francisco to Los Angeles?</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">assistant</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">The driving distance between San Francisco and Los Angeles is approximately 380 to 400 miles (610 to 640 kilometers), and it typically takes about 6 to 7 hours by car, depending on traffic and the specific starting and ending locations. If you</span><span class="sh">'</span><span class="s">re considering a flight, the flight duration is around 1 hour, but you should also factor in time for airport security and travel to and from the airports.</span><span class="sh">"</span><span class="p">}</span>
<span class="p">{</span><span class="sh">"</span><span class="s">time</span><span class="sh">"</span><span class="p">:</span> <span class="mf">1770323690.1769412</span><span class="p">,</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Once there, where can I get some good Mexican food?</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">assistant</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">San Francisco and Los Angeles both have fantastic options for Mexican cuisine. In Los Angeles, some highly recommended spots include:</span><span class="se">\n\n</span><span class="s">1. Guelaguetza – Known for its authentic Oaxacan dishes and delicious moles.</span><span class="se">\n</span><span class="s">2. La Casita Mexicana – Famous for its traditional Mexican flavors and creative dishes.</span><span class="se">\n</span><span class="s">3. El Chato Taco Truck – A popular spot for authentic street-style tacos.</span><span class="se">\n</span><span class="s">4. El Compadre – A lively restaurant offering classic Mexican favorites and great cocktails.</span><span class="se">\n</span><span class="s">5. Tacos</span><span class="sh">"</span><span class="p">}</span>
</code></pre></div></div>

<p>We can see that the messages have time stamps and are split up by user and assistant. The first two messages are from the local GPT2 model and the last two are the GPT-4.1 nano model. Right before we moved on to the GPT-4.1 nano we see that the memory was cleared. So it seems like everything is logging correctly. Let’s now take a look at the <code class="language-plaintext highlighter-rouge">conversation_state.json</code> file:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
  <span class="sh">"</span><span class="s">turns</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span>
    <span class="sh">"</span><span class="s">User: How long does it take to get from San Francisco to Los Angeles?</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">Assistant: The driving distance between San Francisco and Los Angeles is approximately 380 to 400 miles (610 to 640 kilometers), and it typically takes about 6 to 7 hours by car, depending on traffic and the specific starting and ending locations. If you</span><span class="sh">'</span><span class="s">re considering a flight, the flight duration is around 1 hour, but you should also factor in time for airport security and travel to and from the airports.</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">User: Once there, where can I get some good Mexican food?</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">Assistant: San Francisco and Los Angeles both have fantastic options for Mexican cuisine. In Los Angeles, some highly recommended spots include:</span><span class="se">\n\n</span><span class="s">1. Guelaguetza – Known for its authentic Oaxacan dishes and delicious moles.</span><span class="se">\n</span><span class="s">2. La Casita Mexicana – Famous for its traditional Mexican flavors and creative dishes.</span><span class="se">\n</span><span class="s">3. El Chato Taco Truck – A popular spot for authentic street-style tacos.</span><span class="se">\n</span><span class="s">4. El Compadre – A lively restaurant offering classic Mexican favorites and great cocktails.</span><span class="se">\n</span><span class="s">5. Tacos</span><span class="sh">"</span>
  <span class="p">]</span>
<span class="p">}</span>
</code></pre></div></div>

<p>This output is what should be expected since the only memory that the chatbot should have is from after it was cleared, i.e., when we moved to the GPT-4.1 nano model. Note that <code class="language-plaintext highlighter-rouge">\n</code> signifies a line break.</p>

<p>One may ask what the point of creating these log files are. Logging is important for AI systems since it enables monitoring model behavior, debugging issues, and auditing user interactions for safety and compliance. Additionally, the <code class="language-plaintext highlighter-rouge">conversation_state.json</code> file can be used as a save state so that you can reload your previous state into a new chatbot. Even for a small project like this it is good to start thinking about building logs since mostly all commercially available software stores information in some way. Once you start dealing with actual users, more care about what you log and how you log things needs to be considered since logs may now contain sensitive personal data if not properly filtered. This is where anonymizing data starts becoming important, but this is outside the scope of this project.</p>

<p>We have now created a chatbot that preserves memory so it functions like an actual assistant. In addition, we also went through how to build the user prompt and log the actions taken when using the chatbot. On top of that, we went over implementing classes to use API models made available through purchasing API keys. Here, we focused on OpenAI, but these concepts can be extended to any available company/model. To wrap up the chatbot, we implemented a user interface that interacts with our code. This chatbot can be scaled by adding a more complex memory setup for a longer and more efficient memort state, as well as incorporating RAG (retrieval-augmented generation) to support long-term context without token limits.</p>

<p>The code presented in this post can be found on my Github under the <a href="https://github.com/AJG91/basic-chatbot" title="basic-chatbot-repo">basic-chatbot</a> repo <a class="citation" href="#Garcia2025BasicChatbot">[2]</a>. Feel free to reach out if you have any questions about what we covered this week. Next time we’ll discuss Retrieval-Augmented Generation (RAG) and how it can be integrated into the chatbot. Stay tuned!</p>

<ol class="bibliography"><li><span id="abid2019gradio">[1] Abubakar Abid, Ali Abdalla, Ali Abid, et al., <i>Gradio: Hassle-Free Sharing and Testing of ML Models in the Wild</i>, arXiv preprint arXiv:1906.02569, 2019. </span></li>
<li><span id="Garcia2025BasicChatbot">[2] Alberto J. Garcia, <i>Basic Conversational Chatbot</i>, 2025. Available at: https://github.com/AJG91/basic-chatbot.</span></li></ol>
<p><a href="/" class="btn">⬅ Back to Home</a></p>]]></content><author><name></name></author><category term="large-language-models" /><summary type="html"><![CDATA[February 08, 2026 Welcome back! I hope you had a good holiday season and a great start to the new year. I ended up taking a little more time than I thought. Between holidays, traveling, and getting sick I managed to work on some coding, but could not get around to writing a blog post. Anyways, I’m back with the next post. This week I will demonstrate the basics of building a personal chatbot with a focus on prompt building and ensuring the chatbot remembers context. The models we will be using are GPT-2, which is the same as the model used in the last post, and GPT-4.1 nano. I will not be fine-tuning the GPT2 model since the point of this post is to show how to build a chatbot that maintains context, but if you are building one with a specific task in mind, you should align it to the task by fine-tuning the model as discussed in the previous post. GPT-4.1 nano does not need fine-tuning. A chatbot differs from what we’ve previously covered in a specific aspect, which is context. A decoder, as in the previous post, is good for single-turn text generation but it will not be able to ‘‘hold’’ a conversation since they are inherently stateless. On the other hand, chatbots need to be able to remember what you previously said in the conversation and keep it for when you ask something related to it. For example, if you ask ‘‘how long does it take to get from San Francisco to Los Angeles?’’ The chatbot will answer your question. But if you then ask ‘‘once there, where can I get some good Mexican food?’’ The chatbot will no longer know that you are traveling from San Francisco to Los Angeles, so it will not know that you are trying to find Mexican food in Los Angeles. This difference makes chatbots inherently more challenging to design since they must now maintain a dialogue state and generate fluent responses. We must constantly reconstruct the context and feed it back into the model. A few ways of doing this are: Sliding window memory Keeps the last $N$ messages and feeds them back into the model. This approach keeps the context prompt relatively small and reduces the computation needed to generate a response. Summarized memory Summarizes past dialogue and keeps last $N$ messages. This approach helps preserve important details that may have been discussed early in the conversation. Structured memory Stores information that is considered to be relevant, such as user preferences and tasks. This approach can help minimize the amount of information that is contained in the context prompt since it avoids unnecessary and redundant details. It also has the advantage of keeping the memory state separate from the conversation state since it is more so like storing information in a database that the model can reference. For our application, we will use the sliding window memory since it is the easiest to implement. In general, care must be taken with this method since long histories may exceed the model’s token window, causing truncation or memory loss of earlier messages. This won’t be an issue in our case since we are just testing the chatbot. Let’s jump into it. As always, the first step is to define our parameters in a .yaml file and use SimpleNamespace to store them. In this case, the parameters are the following: { "model_name": "gpt2", "api_model_name": "gpt-4.1-nano", "seed": 42, "n_turns": 6, "log_dir": "../outputs/logs/", "state_dir": "../outputs/state/" } There are a few new parameters here. Let’s describe them: n_turns This parameter defines how many turns in a conversation the chatbot will remember. Here, the chatbot will remember the last 6 turns. log_dir This parameter indicates the location where the log file will be stored. This file keeps track of everything that occurs in the chat, including time stamps. state_dir This parameter indicates the location where the conversation state file will be stored. This keeps track of the chat input and output. Before we jump into the class that constructs the chatbot, let’s look at how memory is stored. Below is the memory class (longer docstrings are suppressed for brevity. See the file memory.py for more information): class ChatMemory: def __init__(self): self.turns = [] def add_user(self, text: str): "Adds the user's text to turns list." self.turns.append(f"User: {text}") def add_assistant(self, text: str): "Adds the Assistant's text to turns list." self.turns.append(f"Assistant: {text}") def last_n_turns(self, n: int = 6) -&gt; list[str]: "Grabs the last n turns in the list." return self.turns[-n:] def clear_memory(self, path: str = "conversation_state.json"): """Clears the stored conversation (memory) in turns.""" self.turns = [] self.save(path) def save(self, path: str = "conversation_state.json"): """Saves the conversation state to a JSON file.""" path = Path(path) path.parent.mkdir(parents=True, exist_ok=True) with path.open("w", encoding="utf-8") as f: json.dump({"turns": self.turns}, f, ensure_ascii=False, indent=2) def load(self, path: str = "conversation_state.json"): """Loads the conversation state from a JSON file and adds it to turns.""" path = Path(path) if not path.exists(): self.turns = [] return with path.open("r", encoding="utf-8") as f: data = json.load(f) self.turns = data.get("turns", []) We see that a list labeled as turns is defined in the constructor. This is were the sliding window memory will be stored. The next two methods appends the user text and the assistant (chatbot) text, respectively. The method that follows indexes the last n turns in the memory list. In our parameters, we defined n_turns. This method is where that parameter will be used. The next method clears the turns list and logs the action. Finally, the last two methods save and loads this list from and to a JSON file. The load method also adds the conversation state to the turns list. There’s nothing really complicated about this class. It’s pretty straightforward how the memory bookkeeping is done. Let’s move on to the prompt-building. Below are the functions used to put together the prompt (see the file prompt_utils.py for more information): SYSTEM_PROMPT = "You are a helpful assistant." def build_prompt(user_message: str) -&gt; str: """Builds the output prompt template for the chatbot.""" return ( f"System prompt: {SYSTEM_PROMPT}\n" f"User: {user_message}\n" f"Assistant:" ) def build_prompt_with_history( history_turns: list[str], user_message: str )-&gt; str: """Builds the output prompt template for the chatbot with previous history.""" history_text = "\n".join(history_turns) if history_text.strip(): return ( f"System prompt: {SYSTEM_PROMPT}\n" f"{history_text}\n" f"User: {user_message}\n" f"Assistant:" ) else: return build_prompt(user_message) def extract_assistant_reply(text: str) -&gt; str: """Extracts the chatbot's reply.""" if "Assistant" in text: return text.rsplit("Assistant:", 1)[-1].strip() return text.strip() These are just as straightforward as the memory class. Let’s break each part down. Up top, we have a global variable that gives the chatbot instructions. I kept it very simple for this example, but this part can be as long and focused as you need. Think of this variable as your way of telling the chatbot how to act. You can tell it to get straight to the point, be as verbose as possible, or even to be strict and rude. It all depends on what you are looking for in an assistant. The next part put together the template for the prompt. We can see that it has the form System prompt: " User: " Assistant:" with the different fields being filled in accordingly. The next function is where memory is taken into account. This function grabs the memory list and appends it to the prompt template in such a way so that the model can first see how you want it to behave, then see the chat history between you and itself, and finally gets to the user input text. If there is no memory, the function defaults to the original template. The final function extracts the chatbot’s reply. This is done so that it can be saved to memory later without saving redundant information such as the system prompt, history, and user message. The details of this step are found in the class that builds the chatbot. That’s pretty much it for the memory and prompt building! Pretty simple right? Let’s look at how all this is put together by going over the chatbot class (docstrings are suppressed for brevity. See the file chatbot.py for more information): class MyChat(): def __init__( self, model_name: str, n_turns: int, state_dir: str, log_dir: str, state_fname: str = "conversation_state.json", log_fname: str = "chat_logs.jsonl", api_key: str | None = None ): if api_key: self.lm = OpenAIChat(model_name, api_key=api_key) else: self.lm = LocalLM(model_name) state_path = state_dir + state_fname log_path = log_dir + log_fname self.memory = ChatMemory() self.memory.load(state_path) self.n_turns = n_turns self.state_path = state_path self.log_path = log_path def chat( self, user_message: str ) -&gt; str: prompt = build_prompt_with_history( self.memory.last_n_turns(self.n_turns), user_message ) text = self.lm.generate_text(prompt) reply = extract_assistant_reply(text) self.memory.add_user(user_message) self.memory.add_assistant(reply) return reply def respond( self, user_message: str, chat_history: list | None ) -&gt; tuple[list, str]: if chat_history is None: chat_history = [] chat_history.append({"role": "user", "content": user_message}) try: reply = str(self.chat(user_message)) except Exception as e: reply = f"[ERROR] {type(e).__name__}: {e}" log_output(self.log_path, user_message, reply) self.memory.save(self.state_path) chat_history.append({"role": "assistant", "content": reply}) return chat_history def clear_chat(self) -&gt; list: self.memory.clear_memory(self.state_path) log_output(self.log_path, "Memory cleared", "") return [] Looking at the constructor, we see that two different models are given as options for this chatbot: a local model (GPT-2) or a newer OpenAI API model. This class is not limited to these models and can be used for any model as long as the appropriate code is built to utilize it. After the proper paths for the logs are created, the memory class is initialized and the load method is called to load in previous states if they are available. The next method, called chat, is what builds the prompt. It calls the function build_prompt_with_history found in the prompts_utils.py file that we discussed earlier. From here, it passes the prompt to the language model and that generates a response. From that response, the function extracts the reply and adds the user question and the chatbot’s reply to memory. The reply is then outputted. The method that follows puts together all the functionality needed to utilize the chatbot. It takes a list of the chat history and appends the user and the user message to it. It then calls the chat method to get a response from the language model. This response and the user message are saved to the log file and the conversation state file. The final step is then to append the chatbot’s reply to the list that contains the chat history. Finally, the last method in this class is used to clear the chat history, which is to essentially start a fresh chat. This action is logged in the log file, as well. We have now gone over how memory is stored, how the prompt is built, and how it is all put together into one coherent class that encompasses the chatbot. We can stop here and we would have created a chatbot that takes in a message and outputs a reply. Instead, we can go one step further and work all this into a user interface (UI) like all the modern chatbots have. For this task we will be using Gradio. The code can be found in the gradio_ui.py file. I won’t go into the details regarding the code used for implementing the UI since most of it can be found in the documentation [2]. For the final step, we write a helper function that instances the MyChat class and the Gradio interface, and finally launches the UI. def MyAssistant( model: str, n_turns: int, state_dir: str, log_dir: str, api_key: str | None = None, share: bool = False, inline: bool = False ): bot = MyChat(model, n_turns, state_dir, log_dir, api_key=api_key) demo = chat_interface(bot) demo.launch(share=share, inline=inline) Once you run this function, click the local URL that appears and a window opens that contains the chatbot UI. Here is a screenshot of what that looks like: We have three boxes with three labels: Chatbox Where the chat takes place Message Where the user types their input Clear Used to clear the chat and wipe the stored memory. Let’s test our San Francisco/Los Angeles example and see how well the chatbot responds using the local model: While the local model does produce something coherent, it does not answer our initial question and it definitely did not understand what we were asking. The answer to our follow up question also does not make sense since there is no El Camino Real &amp; Cafe in Los Angeles. The issue with the local model may be that is it not fine-tuned to the problem at hand. It is also an older model so its reasoning is not as good as the current models. Considering both of those options, it is reasonable to believe that the model would not give us what we expected. Instead, let’s try using a more current model. The model of choice will be GPT-4.1 nano by OpenAI. More information on this model can be found here. Note that to use this model (and similar models), you need to purchase an API key from OpenAI. Be careful in integrating your API key with your code since others can also use your API key. This can result in your credit card being overcharged from the unexpected token usage. Read online documentation on how to store and use your API key before you purchase one. As mentioned before, these models are stateless and do not inherently retain memory between API calls. They only know what you asked it in that moment, not what you asked before or what it previously told you. In other words, even though we are using GPT-4.1 nano from OpenAI, we would still need to build the eco-system (memory, prompt, etc.) around it to be able to use it as a chatbot. The code to call this model is similar to the one used for the local model (with slight variations) so I will not go over it. See the file model_openai.py for more information. Let’s ask the same questions as before and see how well the model responds: We now see that the chatbot has context! It remembered that we were talking about going to Los Angeles and it gave us food options there. The driving distance and time it takes to get from San Francisco to Los Angeles are correct. The Mexican places that were recommended exist and are found in Los Angeles. Note that option 5 does not finish displaying since we have a cap on the total amount of tokens that will be used for text generation (see the method generate_text in the OpenAIChat class). We’ve successfully built a working chatbot! Last but not least, let’s review the log files to ensure that they are outputting the information we want. The chat_log.jsonl file contains the following: {"time": 1770322996.336443, "user": "How long does it take to get from San Francisco to Los Angeles?", "assistant": "I have about 4 hours! It's not that easy, but you can do more than just wait for the bus and then return when we're done with your commute or whatever else matters. Just ask me if there is anything wrong during my time in LA (or even while driving on foot). If so please let me know via email at [email protected]"} {"time": 1770323030.933811, "user": "Once there, where can I get some good Mexican food?", "assistant": "In El Camino Real &amp; Cafe downtown…if they offer any type of tacos as well..they will gladly help!! There seems only one place anywhere which accepts them : http://www-foods.com/taco...a+cafeteria%20with_deluxe#post17756879"} {"time": 1770323316.100607, "user": "Memory cleared", "assistant": ""} {"time": 1770323676.5573509, "user": "How long does it take to get from San Francisco to Los Angeles?", "assistant": "The driving distance between San Francisco and Los Angeles is approximately 380 to 400 miles (610 to 640 kilometers), and it typically takes about 6 to 7 hours by car, depending on traffic and the specific starting and ending locations. If you're considering a flight, the flight duration is around 1 hour, but you should also factor in time for airport security and travel to and from the airports."} {"time": 1770323690.1769412, "user": "Once there, where can I get some good Mexican food?", "assistant": "San Francisco and Los Angeles both have fantastic options for Mexican cuisine. In Los Angeles, some highly recommended spots include:\n\n1. Guelaguetza – Known for its authentic Oaxacan dishes and delicious moles.\n2. La Casita Mexicana – Famous for its traditional Mexican flavors and creative dishes.\n3. El Chato Taco Truck – A popular spot for authentic street-style tacos.\n4. El Compadre – A lively restaurant offering classic Mexican favorites and great cocktails.\n5. Tacos"} We can see that the messages have time stamps and are split up by user and assistant. The first two messages are from the local GPT2 model and the last two are the GPT-4.1 nano model. Right before we moved on to the GPT-4.1 nano we see that the memory was cleared. So it seems like everything is logging correctly. Let’s now take a look at the conversation_state.json file: { "turns": [ "User: How long does it take to get from San Francisco to Los Angeles?", "Assistant: The driving distance between San Francisco and Los Angeles is approximately 380 to 400 miles (610 to 640 kilometers), and it typically takes about 6 to 7 hours by car, depending on traffic and the specific starting and ending locations. If you're considering a flight, the flight duration is around 1 hour, but you should also factor in time for airport security and travel to and from the airports.", "User: Once there, where can I get some good Mexican food?", "Assistant: San Francisco and Los Angeles both have fantastic options for Mexican cuisine. In Los Angeles, some highly recommended spots include:\n\n1. Guelaguetza – Known for its authentic Oaxacan dishes and delicious moles.\n2. La Casita Mexicana – Famous for its traditional Mexican flavors and creative dishes.\n3. El Chato Taco Truck – A popular spot for authentic street-style tacos.\n4. El Compadre – A lively restaurant offering classic Mexican favorites and great cocktails.\n5. Tacos" ] } This output is what should be expected since the only memory that the chatbot should have is from after it was cleared, i.e., when we moved to the GPT-4.1 nano model. Note that \n signifies a line break. One may ask what the point of creating these log files are. Logging is important for AI systems since it enables monitoring model behavior, debugging issues, and auditing user interactions for safety and compliance. Additionally, the conversation_state.json file can be used as a save state so that you can reload your previous state into a new chatbot. Even for a small project like this it is good to start thinking about building logs since mostly all commercially available software stores information in some way. Once you start dealing with actual users, more care about what you log and how you log things needs to be considered since logs may now contain sensitive personal data if not properly filtered. This is where anonymizing data starts becoming important, but this is outside the scope of this project. We have now created a chatbot that preserves memory so it functions like an actual assistant. In addition, we also went through how to build the user prompt and log the actions taken when using the chatbot. On top of that, we went over implementing classes to use API models made available through purchasing API keys. Here, we focused on OpenAI, but these concepts can be extended to any available company/model. To wrap up the chatbot, we implemented a user interface that interacts with our code. This chatbot can be scaled by adding a more complex memory setup for a longer and more efficient memort state, as well as incorporating RAG (retrieval-augmented generation) to support long-term context without token limits. The code presented in this post can be found on my Github under the basic-chatbot repo [2]. Feel free to reach out if you have any questions about what we covered this week. Next time we’ll discuss Retrieval-Augmented Generation (RAG) and how it can be integrated into the chatbot. Stay tuned! [1] Abubakar Abid, Ali Abdalla, Ali Abid, et al., Gradio: Hassle-Free Sharing and Testing of ML Models in the Wild, arXiv preprint arXiv:1906.02569, 2019. [2] Alberto J. Garcia, Basic Conversational Chatbot, 2025. Available at: https://github.com/AJG91/basic-chatbot. ⬅ Back to Home]]></summary></entry><entry><title type="html">Natural language models: GPT-2 causal text generator</title><link href="https://ajg91.github.io//posts/large-language-models/decoder-gpt-2-example/" rel="alternate" type="text/html" title="Natural language models: GPT-2 causal text generator" /><published>2025-11-30T00:00:00-08:00</published><updated>2025-11-30T00:00:00-08:00</updated><id>https://ajg91.github.io//posts/large-language-models/decoder-gpt-2-example</id><content type="html" xml:base="https://ajg91.github.io//posts/large-language-models/decoder-gpt-2-example/"><![CDATA[<p class="post-date">November 30, 2025</p>

<p>This week we will be using a decoder to generate movie reviews. First, I will walk through the fine-tuning process. Then, I will show how well the decoder autocompletes movie reviews. As a note, the fine-tuning process is almost identical to fine-tuning an encoder, so there will be a lot of similarities between this and the previous <a href="https://ajg91.github.io/posts/2025-11-16-encoder-distilbert-classifier/" title="encoder-post">post</a>.</p>

<p>The foundation to text generation is masked self-attention and autoregressive training. Let’s assume we have a sequence of tokens $T_1, T_2, \ldots, T_n$. Self-attention lets token $T_i$ see all tokens in the sequence $\sum_{k=1}^{n} T_k$. This is bidirectional self-attention (unmasked), which we discussed last post. For causal language models, this won’t work since we can only use the past tokens to predict the next. This is where masked self-attention comes into play. With masked self-attention, the learning is unidirectional (left-to-right), which means that token $T_i$ can also see all tokens in the sequence $\sum_{k=1}^{i} T_k$. The reason for using the word ‘‘masked’’ is that a mask is applied to the attention matrix to mask all future tokens converting the matrix into a lower triangular matrix. Autoregressive training is a training objective where the model predicts token $T_i$ using all previous tokens $T_1, T_2, \ldots, T_{i-1}$ by minimizing a log-likelihood objective function. These two concepts allow the model to learn sequential dependencies and apply this to text generation  <a class="citation" href="#bahdanau2014neural">[1,2,3]</a>.</p>

<p>The model we will be fine-tuning it GPT-2 (Generative Pretrained Transformer 2). This is a transformer-based neural network that has been trained on a massive amount of text found on the internet. This model is used to generate the next word in a sequence in a coherent and contextual way. As previously mentioned, the original model was developed by OpenAI in 2019 and a smaller version (~124M parameters) was made available on the Hugging Face <a href="https://huggingface.co/openai-community/gpt2" title="hf-gpt2">website</a> <a class="citation" href="#radford2019language">[4]</a>, which is the one we will be using. Our job is to fine-tune it to the specific problem we are considering, which is to generate movie reviews based on sentiment. We are <strong>not</strong> generating sentiment, but outputting a continuation of an input text that contains some form of sentiment.</p>

<p>The first step is defining our parameters. Like previously seen, our parameters will be saved in a <code class="language-plaintext highlighter-rouge">.yaml</code> file and we will use SimpleNamespace as an easy way to store and access these parameters. Here are the parameters we will be using:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
  <span class="sh">"</span><span class="s">model_name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">gpt2</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">dataset</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">imdb</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">seed</span><span class="sh">"</span><span class="p">:</span> <span class="mi">42</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">dpi</span><span class="sh">"</span><span class="p">:</span> <span class="mi">400</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">max_token_length</span><span class="sh">"</span><span class="p">:</span> <span class="mi">128</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">train_size</span><span class="sh">"</span><span class="p">:</span> <span class="mi">3000</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">val_size</span><span class="sh">"</span><span class="p">:</span> <span class="mi">500</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">lr</span><span class="sh">"</span><span class="p">:</span> <span class="mf">2e-05</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">warmup_ratio</span><span class="sh">"</span><span class="p">:</span> <span class="mf">0.1</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">epochs</span><span class="sh">"</span><span class="p">:</span> <span class="mi">3</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">train_batch_size</span><span class="sh">"</span><span class="p">:</span> <span class="mi">4</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">eval_batch_size</span><span class="sh">"</span><span class="p">:</span> <span class="mi">4</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">strategy</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">epoch</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">fig_path</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">../outputs/</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">res_path</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">../results/</span><span class="sh">"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>From here, we load the dataset, model, and tokenizer. This is done as follows:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">dataset</span> <span class="o">=</span> <span class="nf">load_dataset</span><span class="p">(</span><span class="n">cfg</span><span class="p">.</span><span class="n">dataset</span><span class="p">)</span>

<span class="n">inst</span> <span class="o">=</span> <span class="nc">CausalTextGeneration</span><span class="p">(</span><span class="n">cfg</span><span class="p">.</span><span class="n">model_name</span><span class="p">)</span>
<span class="n">dataset</span> <span class="o">=</span> <span class="n">inst</span><span class="p">.</span><span class="nf">clean_data</span><span class="p">(</span><span class="n">dataset</span><span class="p">)</span>
<span class="n">data_tokenized</span> <span class="o">=</span> <span class="n">inst</span><span class="p">.</span><span class="nf">tokenize_dataset</span><span class="p">(</span><span class="n">dataset</span><span class="p">,</span> <span class="n">cfg</span><span class="p">.</span><span class="n">max_token_length</span><span class="p">)</span>
<span class="n">data_collator</span> <span class="o">=</span> <span class="n">inst</span><span class="p">.</span><span class="nf">data_collator</span><span class="p">()</span>
</code></pre></div></div>

<p>We have wrapped all the necessary code in the <code class="language-plaintext highlighter-rouge">CausalTextGeneration</code> custom class. In this class, we can find methods to import the dataset, clean the dataset, initialize the model, tokenize the dataset, etc. This class is defined as follows (I will omit the docstrings and unused methods for brevity. See the file <code class="language-plaintext highlighter-rouge">model.py</code> for more information):</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">CausalTextGeneration</span><span class="p">():</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">model_name</span><span class="p">:</span> <span class="nb">str</span>
    <span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">model_name</span> <span class="o">=</span> <span class="n">model_name</span>
        <span class="n">self</span><span class="p">.</span><span class="n">device</span> <span class="o">=</span> <span class="nf">get_device</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">:</span> <span class="n">PreTrainedTokenizerBase</span> <span class="o">=</span> <span class="n">AutoTokenizer</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="n">model_name</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">.</span><span class="n">pad_token</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">.</span><span class="n">eos_token</span>

    <span class="k">def</span> <span class="nf">clean_data</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span>
        <span class="n">dataset</span><span class="p">:</span> <span class="n">Union</span><span class="p">[</span><span class="n">Dataset</span><span class="p">,</span> <span class="n">DatasetDict</span><span class="p">],</span>
        <span class="n">cutoff</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">2000</span>
        <span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Union</span><span class="p">[</span><span class="n">Dataset</span><span class="p">,</span> <span class="n">DatasetDict</span><span class="p">]:</span>
        <span class="n">dataset</span> <span class="o">=</span> <span class="n">dataset</span><span class="p">.</span><span class="nf">filter</span><span class="p">(</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="nf">len</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">].</span><span class="nf">strip</span><span class="p">())</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span>
        <span class="n">dataset</span> <span class="o">=</span> <span class="n">dataset</span><span class="p">.</span><span class="nf">filter</span><span class="p">(</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="nf">len</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">])</span> <span class="o">&lt;</span> <span class="n">cutoff</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">dataset</span>

    <span class="k">def</span> <span class="nf">tokenize_text</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">prompt</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> 
        <span class="n">padding</span><span class="p">:</span> <span class="n">Union</span><span class="p">[</span><span class="nb">bool</span><span class="p">,</span> <span class="nb">str</span><span class="p">]</span> <span class="o">=</span> <span class="bp">True</span><span class="p">,</span> 
        <span class="n">truncation</span><span class="p">:</span> <span class="n">Union</span><span class="p">[</span><span class="nb">bool</span><span class="p">,</span> <span class="nb">str</span><span class="p">]</span> <span class="o">=</span> <span class="bp">True</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">]:</span>
        <span class="n">inputs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">tokenizer</span><span class="p">(</span>
            <span class="n">prompt</span><span class="p">,</span>
            <span class="n">padding</span><span class="o">=</span><span class="n">padding</span><span class="p">,</span>
            <span class="n">truncation</span><span class="o">=</span><span class="n">truncation</span><span class="p">,</span>
            <span class="n">return_tensors</span><span class="o">=</span><span class="sh">"</span><span class="s">pt</span><span class="sh">"</span>
        <span class="p">)</span>
        <span class="k">return</span> <span class="nf">to_device</span><span class="p">(</span><span class="n">inputs</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
    
    <span class="k">def</span> <span class="nf">tokenize_dataset</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">dataset</span><span class="p">:</span> <span class="n">Union</span><span class="p">[</span><span class="n">Dataset</span><span class="p">,</span> <span class="n">DatasetDict</span><span class="p">],</span> 
        <span class="n">max_token_length</span><span class="p">:</span> <span class="nb">int</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Union</span><span class="p">[</span><span class="n">Dataset</span><span class="p">,</span> <span class="n">DatasetDict</span><span class="p">]:</span>
        <span class="k">def</span> <span class="nf">tokenize_fn</span><span class="p">(</span><span class="n">batch</span><span class="p">):</span>
            <span class="n">tokenized</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">tokenizer</span><span class="p">(</span>
                <span class="n">batch</span><span class="p">[</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">],</span>
                <span class="n">padding</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span>
                <span class="n">truncation</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
                <span class="n">max_length</span><span class="o">=</span><span class="n">max_token_length</span>
            <span class="p">)</span>
            <span class="k">return</span> <span class="n">tokenized</span>

        <span class="n">data_tokenized</span> <span class="o">=</span> <span class="n">dataset</span><span class="p">.</span><span class="nf">map</span><span class="p">(</span>
            <span class="n">tokenize_fn</span><span class="p">,</span>
            <span class="n">batched</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
            <span class="n">remove_columns</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">label</span><span class="sh">"</span><span class="p">],</span>
        <span class="p">)</span>
        <span class="k">return</span> <span class="n">data_tokenized</span>
    
    <span class="k">def</span> <span class="nf">data_collator</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">mlm</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">False</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="n">DataCollatorForLanguageModeling</span><span class="p">:</span>
        <span class="n">data_collator</span> <span class="o">=</span> <span class="nc">DataCollatorForLanguageModeling</span><span class="p">(</span>
            <span class="n">tokenizer</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">,</span>
            <span class="n">mlm</span><span class="o">=</span><span class="n">mlm</span>
        <span class="p">)</span>
        <span class="k">return</span> <span class="n">data_collator</span>

    <span class="k">def</span> <span class="nf">load_model</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="n">PreTrainedModel</span><span class="p">:</span>
        <span class="n">model</span> <span class="o">=</span> <span class="n">AutoModelForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
            <span class="n">self</span><span class="p">.</span><span class="n">model_name</span><span class="p">,</span>
        <span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
        <span class="n">model</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="n">pad_token_id</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">.</span><span class="n">pad_token_id</span>
        <span class="k">return</span> <span class="n">model</span>
    
    <span class="k">def</span> <span class="nf">evaluate_model</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span>
        <span class="n">prompt</span><span class="p">:</span> <span class="n">Union</span><span class="p">[</span><span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">],</span> <span class="nb">str</span><span class="p">],</span>
        <span class="n">model</span><span class="p">:</span> <span class="n">PreTrainedModel</span><span class="p">,</span>
        <span class="n">max_new_tokens</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">25</span><span class="p">,</span>
        <span class="n">do_sample</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>
        <span class="n">top_k</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">25</span><span class="p">,</span>
        <span class="n">top_p</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.95</span><span class="p">,</span>
        <span class="n">temperature</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.7</span><span class="p">,</span>
        <span class="n">repetition_penalty</span><span class="p">:</span> <span class="n">Union</span><span class="p">[</span><span class="nb">int</span><span class="p">,</span> <span class="nb">float</span><span class="p">]</span> <span class="o">=</span> <span class="mf">1.1</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Union</span><span class="p">[</span><span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">GenerateOutput</span><span class="p">]:</span>
        <span class="n">inputs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">tokenize_text</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>

        <span class="n">model</span><span class="p">.</span><span class="nf">eval</span><span class="p">()</span>
        <span class="k">with</span> <span class="n">tc</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
            <span class="n">outputs</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span>
                <span class="o">**</span><span class="n">inputs</span><span class="p">,</span> 
                <span class="n">max_new_tokens</span><span class="o">=</span><span class="n">max_new_tokens</span><span class="p">,</span>
                <span class="n">do_sample</span><span class="o">=</span><span class="n">do_sample</span><span class="p">,</span>
                <span class="n">top_k</span><span class="o">=</span><span class="n">top_k</span><span class="p">,</span>
                <span class="n">top_p</span><span class="o">=</span><span class="n">top_p</span><span class="p">,</span>
                <span class="n">temperature</span><span class="o">=</span><span class="n">temperature</span><span class="p">,</span>
                <span class="n">repetition_penalty</span><span class="o">=</span><span class="n">repetition_penalty</span><span class="p">,</span>
                <span class="n">pad_token_id</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">.</span><span class="n">pad_token_id</span>
            <span class="p">)</span>
        <span class="k">return</span> <span class="n">outputs</span>
</code></pre></div></div>

<p>I will omit the explanation of each method since most of these were explained in the previous post, but I will go over key differences. For decoders, we will use the Hugging Face class <code class="language-plaintext highlighter-rouge">AutoModelForCausalLM</code> since we are now dealing with a causal model, which means that the model depends on the previous information to generate the next word. This is the key difference between encoders and decoders. The next key difference is found in the method <code class="language-plaintext highlighter-rouge">tokenize_dataset</code>. We had previously made the default for padding <code class="language-plaintext highlighter-rouge">True</code>, but for decoders we make it <code class="language-plaintext highlighter-rouge">False</code>. The reason has to do with the length of the reviews. Since we are setting the max token length to be $128$ (see parameters), no padding will actually occur since this length is less than the total length of a review. Hence the token vector will be fully composed of tokens with no blank space and padding would be useless.</p>

<p>We are also now using a pre-defined data collator initialized in the method <code class="language-plaintext highlighter-rouge">data_collator</code>, where we import the Hugging Face class <code class="language-plaintext highlighter-rouge">DataCollatorForLanguageModeling</code>. This collator automatically takes care of padding (if needed) and masking (as well as other things) for language models. For a causal languange model in particular, we set <code class="language-plaintext highlighter-rouge">mlm=False</code>, which tells the collator that the model should be trying to predict the next token. If true, the collator would follow BERT’s stragety of data preparation. There is actually a Hugging Face class called <code class="language-plaintext highlighter-rouge">DataCollatorForCausalLM</code>, which is used primarily for text generation models, but I am working with an older version of the transformers library where it is not yet available.</p>

<p>Another difference is that we have now included an <code class="language-plaintext highlighter-rouge">evaluate_model</code> method. As one can infer, it is used to evaluate the model using new, unseen data, but now we have different parameters to consider. Let’s go over each individual parameter in <code class="language-plaintext highlighter-rouge">model.generate()</code>:</p>

<ol>
  <li>max_new_tokens = $25$
    <ul>
      <li>This is the total amount of tokens that will be used in the text generation.</li>
    </ul>
  </li>
  <li>do_sample = True
    <ul>
      <li>Tells model to sample from probability distribution instead of picking the highest-probability token.</li>
      <li>do_sample = False $\rightarrow$ deterministic.</li>
      <li>do_sample = True $\rightarrow$ random sampling.</li>
    </ul>
  </li>
  <li>top_k = $25$
    <ul>
      <li>Sorts logits, keeps top $25$ most probable tokens, and draws samples from them.</li>
      <li>Lower k $\rightarrow$ more deterministic.</li>
      <li>Higher k $\rightarrow$ more random.</li>
    </ul>
  </li>
  <li>top_p = $0.95$
    <ul>
      <li>Keeps the smallest set of tokens whose cumulative probability $\ge 0.95$.</li>
      <li>Can be used in conjunction with top_k, which amounts to picking tokens that intersect with these two conditions.</li>
      <li>Helps adapt to uncertainty by keeping the token selection dynamic since it is based on a probability mass as opposed to a fixed number.</li>
    </ul>
  </li>
  <li>temperature = $0.7$
    <ul>
      <li>Scales softmax distribution before sampling.</li>
      <li>Lower T $\rightarrow$ more deterministic.</li>
      <li>Higher T $\rightarrow$ more random.</li>
    </ul>
  </li>
  <li>repetition_penalty = $1.1$
    <ul>
      <li>Discourages the same token from reappearing by scaling all logits that have already been used by some value $&gt; 1$.</li>
      <li>This occurs when there is a lack of sampling diversity, which causes reinforcement of certain phrases/tokens.</li>
    </ul>
  </li>
</ol>

<p>Now, we look at the TrainingArguments and Trainer classes:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">training_args</span> <span class="o">=</span> <span class="nc">TrainingArguments</span><span class="p">(</span>
    <span class="n">output_dir</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">res_path</span><span class="p">,</span>
    <span class="n">overwrite_output_dir</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
    <span class="n">report_to</span><span class="o">=</span><span class="sh">"</span><span class="s">none</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">eval_strategy</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">strategy</span><span class="p">,</span>
    <span class="n">save_strategy</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">strategy</span><span class="p">,</span>
    <span class="n">logging_strategy</span><span class="o">=</span> <span class="n">cfg</span><span class="p">.</span><span class="n">strategy</span><span class="p">,</span>
    <span class="n">learning_rate</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">lr</span><span class="p">,</span>
    <span class="n">per_device_train_batch_size</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">train_batch_size</span><span class="p">,</span>
    <span class="n">per_device_eval_batch_size</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">eval_batch_size</span><span class="p">,</span>
    <span class="n">num_train_epochs</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">epochs</span><span class="p">,</span>
    <span class="n">warmup_ratio</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">warmup_ratio</span><span class="p">,</span>
    <span class="n">seed</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">seed</span>
<span class="p">)</span>
</code></pre></div></div>

<p>The TrainingArguments parameters are mostly the same as for the encoder. One big difference is the parameter <code class="language-plaintext highlighter-rouge">warmup_ratio</code>. This parameter controls the learning rate at the start of the training by slowly ramping up the learning rate. It initially starts the learning rate at $0$, then linearly increases it until it reaches the max learning rate (chosen by user) and decays from there. The warmup length is given in steps, which is taken by scaling the total steps by the warmup ratio. The purpose of this parameter is to gradually change the model weights at the start of training since the weights are not yet stable.</p>

<p>The main body of the Trainer is essentially the same as before, expect the last two lines are replaced by the data collator. In this case, we choose to not pass in any metrics since we will be calculating the perplexity of the evaluation loss (explained later). The Trainer looks like the following:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">trainer</span> <span class="o">=</span> <span class="nc">Trainer</span><span class="p">(</span>
    <span class="n">model</span><span class="o">=</span><span class="n">model</span><span class="p">,</span>
    <span class="n">args</span><span class="o">=</span><span class="n">training_args</span><span class="p">,</span>
    <span class="n">train_dataset</span><span class="o">=</span><span class="n">data_tokenized</span><span class="p">[</span><span class="sh">"</span><span class="s">train</span><span class="sh">"</span><span class="p">].</span><span class="nf">shuffle</span><span class="p">(</span><span class="n">seed</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">seed</span><span class="p">).</span><span class="nf">select</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">cfg</span><span class="p">.</span><span class="n">train_size</span><span class="p">)),</span>
    <span class="n">eval_dataset</span><span class="o">=</span><span class="n">data_tokenized</span><span class="p">[</span><span class="sh">"</span><span class="s">test</span><span class="sh">"</span><span class="p">].</span><span class="nf">shuffle</span><span class="p">(</span><span class="n">seed</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">seed</span><span class="p">).</span><span class="nf">select</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">cfg</span><span class="p">.</span><span class="n">val_size</span><span class="p">)),</span>
    <span class="n">data_collator</span><span class="o">=</span><span class="n">data_collator</span>
<span class="p">)</span>
</code></pre></div></div>

<p>As before, we pass in the model, the training arguments, the different datasets, and the collator (new!) into the trainer. These datasets are huge so we shuffe, seed, and select a subset of the train ($3000$ samples) and test ($500$ samples) set as a demonstration. In actual fine-tuning, it’s essential to use all the available data. Now, all we have to do is run <code class="language-plaintext highlighter-rouge">trainer.train()</code> and the model begins to train.</p>

<p>For our example, we get the following metrics:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Epoch 	Training Loss   Validation Loss
  1 	   3.870400 	    3.709818
  2 	   3.685000 	    3.707382
  3 	   3.607300 	    3.709974
</code></pre></div></div>

<p>While the training loss is decreasing, it is not decreasing by much. On the other hand, the validation loss starts to decrease then slightly increases. While this may seem troubling at first glance, it is actually not that bad. GPT-2 is actually a very large model, which means that fine-tuning will only slightly decrease the loss. Not to mention, we are only fine-tuning with $3000$ samples. This is such a small sample size in comparison to the data used to train the model that such a small step size in training loss is to be expected. This also partly explains the increase in validation loss during the last epoch since the sample size for the validation dataset is $500$. The change in loss is in the hundredth decimal place, which for most applications can be attributed to small fluctuations (a.k.a. noise). There is a possibility of slight overfitting, but I don’t think there is enough proof. More tests need to be considered with a larger sample size to determine if the model is indeed overfitting.</p>

<p>We can also calculate the perplexity to determine how well the model learned. Perplexity is a way to measure how well a model can predict the next token <a class="citation" href="#jozefowicz2016exploring">[5]</a>. It is calculated as the exponent of the average cross-entropy loss. In other words,</p>

\[\mathrm{perplexity} = e^{\mathrm{loss}}.\]

<p>The lower the perplexity, the less token ‘‘choices’’ the model has, hence the more accurate the model prediction. The higher the perplexity, the more token ‘‘choices’’, the more uncertain the model is of its predictions. Ideally, the perplexity has an exponential decay behavior as the model learns. Here is a plot of the perplexity for the training and validation phases:</p>

<p><img src="/assets/images/gpt_2_gen/perplexity_curve.png" alt="train_perplexity" /></p>

<p>From the plot, we see that the training perplexity does indeed follow an exponential decay behavior. On the other hand, the validation perplexity increases, which is attributed to what was already mentioned above. Note that steps is the global steps taken during the fine-tuning of the model.</p>

<p>Now that our model is fine-tuned, let’s see how well it can autocomplete movie reviews. Here are beginning of reviews that I created:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>reviews = [
    "This movie was terrible. The acting was mediocre.",
    "I highly recommend this movie to everyone.",
    "This was a great movie. I wish it was longer.",
    "I do not recommend this movie. The actors are terrible.",
]
</code></pre></div></div>

<p>In this example, I chose to make the sentiment obvious to see how well the model could understand this and expand on the review. The code that calls the <code class="language-plaintext highlighter-rouge">evaluate_model</code> method and prints out the generated text is:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">outputs</span> <span class="o">=</span> <span class="n">inst</span><span class="p">.</span><span class="nf">evaluate_model</span><span class="p">(</span><span class="n">reviews</span><span class="p">,</span> <span class="n">model</span><span class="p">)</span>

<span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">seq</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">outputs</span><span class="p">):</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Seq. </span><span class="si">{</span><span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="si">}</span><span class="s">:</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">inst</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span><span class="n">seq</span><span class="p">,</span> <span class="n">skip_special_tokens</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>Here are the outputs of the model:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Seq. 1:
This movie was terrible. The acting was mediocre.It is a very good horror film, but I think it will appeal to many people as well (especially the young ones).

Seq. 2:
I highly recommend this movie to everyone.

If you want a good laugh, go with the story and characters of The Sopranos. If not then read

Seq. 3:
This was a great movie. I wish it was longer. It is not as good as the first one, but this time you can see why people are so angry and upset about what

Seq. 4:
I do not recommend this movie. The actors are terrible. They had an awful time with the acting in it, and I have yet to see a single scene where they did anything other
</code></pre></div></div>

<p>Overall, the expanded reviews generated by the model seem to somewhat fit the sentiment of the input reviews. There are no issues with Sequence 1 and 4, but we should analyze the other two. Sequence 2 seems to deviate from the inital sentiment. It goes on to talk about The Sopranos even though the input text did not mention anything about that show. Sequence 3 seems to not quite understand what sentiment it is supposed to show, but this is because the sentence ‘‘I wish it was longer.’’ can be interpreted as a negative sentiment even though the first sentence has a positive sentiment. So the output seems to make sense.</p>

<p>Let’s try some harder reviews.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>reviews = [
    "Wow! I was suprised by how much ",
    "This film was such a surprise. ",
    "I never thought I would ",
    "This movie makes me want to yell "
]
</code></pre></div></div>

<p>As you can tell, these reviews are more difficult to determine sentiment from. They are ambiguous as to whether they think the movie was good or bad. Let’s see how well the model categorizes them:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Seq. 1:
Wow! I was suprised by how much vernacular this film used to be, and also the way it kept me guessing. For starters: How would you like a

Seq. 2:
This film was such a surprise. A beautiful movie with some great characters, the director and actors had wonderful chemistry to create this story that is very entertaining - even

Seq. 3:
I never thought I would This movie was so bad, but it is. It's not that BAD though! Just an awful story with no redeeming

Seq. 4:
This movie makes me want to yell The Devil is a Bad Idea. It's just terrible, it has no suspense and the plot doesn't move at all!
</code></pre></div></div>

<p>In this case, the model does a decent job in generating reviews with varying sentiment. This may be because the second batch gave the model more flexibility in producing reviews that kept to one sentiment, as opposed to giving it a sentiment and making it work off that. Sequence 3 and 4 do seem to have fluidity issues, but they still kept a consistent sentiment. Overall, the text generation for both batches were pretty good for the amount of data we used to fine-tune the model.</p>

<p>In general, the model’s performance can be improved by fine-tuning with a larger dataset. I purposely chose to cap the training set at $3000$ samples because my GPU kept running out of memory when running for a larger sample size over $3$ epochs. This can potentially lead to issues since it is known that fine-tuning with small datasets can potentially lead to overfitting or ‘‘catastrophic forgetting’’, which is where the model loses previously learned information <a class="citation" href="#howard2018universal">[6]</a>. Another way to improve performance is to increase <code class="language-plaintext highlighter-rouge">max_token_len</code>. I chose $128$, but ideally, one should choose $256$ or $512$. The reason I had to go with $128$ was also because of my GPU. I did try running the fine-tuning on my CPU with larger sample sizes and/or max token lengths, but it was not feasible due to time constraints. Another test that can be done is training with more epochs to get more steps on the perplexity curves. This could help rule out the model overfitting. Of course, one can also experiment with varying the learning rate, batch size, and warmup ratio, or include weight decay.</p>

<p>As a sidenote, a common way to avoid catastrophic forgetting is using  parameter-efficient fine-tuning techniques such as Low-Rank Adaptation (LoRA) <a class="citation" href="#hu2021lora">[7]</a>. This technique works by freezing the model weights and fine-tuning with a batch of small low-rank matrices (called LoRA adapter) instead of the full model parameter set, making the fine-tuning process much faster and more memory-efficient. This drastically reduces the number of parameters that need to be trained, as well as keeps the base model unchanged. Afterwards, if one chooses to change the behavior of the model, the LoRA adapter can be swapped out and the base model can once again be fine-tuned with a different LoRA adapter.</p>

<p>In summary, we have learned how to fine-tuned a GPT-2 model to understand how reviews sound (good or bad) and determine what kind of sentence it should generate in continuation. We also learned about perplexity and how it can be used to describe the effectiveness of a model to generate accurate text. Overall, the model performed well on reviews that had pre-defined and obvious sentiment, but did not do so well with ambiguous reviews, which was expected. There is room for increasing the model’s capability of producing better review continuations, as well as making better conclusions on ambiguous inputs.</p>

<p>The code and plots presented in this post can be found on my Github under the <a href="https://github.com/AJG91/gpt-2-gen" title="gpt-2-gen-repo">gpt-2-gen</a> repo <a class="citation" href="#Garcia2025GPT2Gen">[8]</a>. Feel free to reach out if you have any questions about what we covered this week. I’ll be taking a few weeks off, but will come back with more about building and employing decoders. Stay tuned!</p>

<ol class="bibliography"><li><span id="bahdanau2014neural">[1] Dzmitry Bahdanau, Kyunghyun Cho, &amp; Yoshua Bengio, <i>Neural Machine Translation by Jointly Learning to Align and Translate</i>, arXiv preprint arXiv:1409.0473, 2014. </span></li>
<li><span id="vaswani2017attention">[2] Ashish Vaswani, Noam Shazeer, Niki Parmar, et al., <i>Attention is All you Need</i>, Advances in Neural Information Processing Systems, 2017. </span></li>
<li><span id="bengio2003neural">[3] Yoshua Bengio, Réjean Ducharme, Pascal Vincent, &amp; Christian Jauvin, <i>A Neural Probabilistic Language Model</i>, Journal of Machine Learning Research, 2003. </span></li>
<li><span id="radford2019language">[4] Alec Radford, Jeffrey Wu, Rewon Child, et al., <i>Language models are unsupervised multitask learners</i>, OpenAI blog, 2019. </span></li>
<li><span id="jozefowicz2016exploring">[5] Rafal Jozefowicz, Oriol Vinyals, Mike Schuster, et al., <i>Exploring the Limits of Language Modeling</i>, arXiv preprint arXiv:1602.02410, 2016. </span></li>
<li><span id="howard2018universal">[6] Jeremy Howard &amp; Sebastian Ruder, <i>Universal Language Model Fine-tuning for Text Classification</i>, Proceedings of the 56th Annual Meeting of the Association for Computational Linguistics (Volume 1: Long Papers), 2018. DOI: 10.18653/v1/P18-1031.</span></li>
<li><span id="hu2021lora">[7] Edward J. Hu, Yelong Shen, Phillip Wallis, et al., <i>LoRA: Low-Rank Adaptation of Large Language Models</i>, arXiv preprint arXiv:2106.09685, 2021. </span></li>
<li><span id="Garcia2025GPT2Gen">[8] Alberto J. Garcia, <i>GPT-Style Language Model</i>, 2025. Available at: https://github.com/AJG91/gpt-2-gen.</span></li></ol>
<p><a href="/" class="btn">⬅ Back to Home</a></p>]]></content><author><name></name></author><category term="large-language-models" /><summary type="html"><![CDATA[November 30, 2025 This week we will be using a decoder to generate movie reviews. First, I will walk through the fine-tuning process. Then, I will show how well the decoder autocompletes movie reviews. As a note, the fine-tuning process is almost identical to fine-tuning an encoder, so there will be a lot of similarities between this and the previous post. The foundation to text generation is masked self-attention and autoregressive training. Let’s assume we have a sequence of tokens $T_1, T_2, \ldots, T_n$. Self-attention lets token $T_i$ see all tokens in the sequence $\sum_{k=1}^{n} T_k$. This is bidirectional self-attention (unmasked), which we discussed last post. For causal language models, this won’t work since we can only use the past tokens to predict the next. This is where masked self-attention comes into play. With masked self-attention, the learning is unidirectional (left-to-right), which means that token $T_i$ can also see all tokens in the sequence $\sum_{k=1}^{i} T_k$. The reason for using the word ‘‘masked’’ is that a mask is applied to the attention matrix to mask all future tokens converting the matrix into a lower triangular matrix. Autoregressive training is a training objective where the model predicts token $T_i$ using all previous tokens $T_1, T_2, \ldots, T_{i-1}$ by minimizing a log-likelihood objective function. These two concepts allow the model to learn sequential dependencies and apply this to text generation [8,8,8]. The model we will be fine-tuning it GPT-2 (Generative Pretrained Transformer 2). This is a transformer-based neural network that has been trained on a massive amount of text found on the internet. This model is used to generate the next word in a sequence in a coherent and contextual way. As previously mentioned, the original model was developed by OpenAI in 2019 and a smaller version (~124M parameters) was made available on the Hugging Face website [8], which is the one we will be using. Our job is to fine-tune it to the specific problem we are considering, which is to generate movie reviews based on sentiment. We are not generating sentiment, but outputting a continuation of an input text that contains some form of sentiment. The first step is defining our parameters. Like previously seen, our parameters will be saved in a .yaml file and we will use SimpleNamespace as an easy way to store and access these parameters. Here are the parameters we will be using: { "model_name": "gpt2", "dataset": "imdb", "seed": 42, "dpi": 400, "max_token_length": 128, "train_size": 3000, "val_size": 500, "lr": 2e-05, "warmup_ratio": 0.1, "epochs": 3, "train_batch_size": 4, "eval_batch_size": 4, "strategy": "epoch", "fig_path": "../outputs/", "res_path": "../results/" } From here, we load the dataset, model, and tokenizer. This is done as follows: dataset = load_dataset(cfg.dataset) inst = CausalTextGeneration(cfg.model_name) dataset = inst.clean_data(dataset) data_tokenized = inst.tokenize_dataset(dataset, cfg.max_token_length) data_collator = inst.data_collator() We have wrapped all the necessary code in the CausalTextGeneration custom class. In this class, we can find methods to import the dataset, clean the dataset, initialize the model, tokenize the dataset, etc. This class is defined as follows (I will omit the docstrings and unused methods for brevity. See the file model.py for more information): class CausalTextGeneration(): def __init__( self, model_name: str ): self.model_name = model_name self.device = get_device() self.tokenizer: PreTrainedTokenizerBase = AutoTokenizer.from_pretrained(model_name) self.tokenizer.pad_token = self.tokenizer.eos_token def clean_data( self, dataset: Union[Dataset, DatasetDict], cutoff: int = 2000 ) -&gt; Union[Dataset, DatasetDict]: dataset = dataset.filter(lambda x: len(x["text"].strip()) &gt; 0) dataset = dataset.filter(lambda x: len(x["text"]) &lt; cutoff) return dataset def tokenize_text( self, prompt: str, padding: Union[bool, str] = True, truncation: Union[bool, str] = True ) -&gt; dict[str, tc.Tensor]: inputs = self.tokenizer( prompt, padding=padding, truncation=truncation, return_tensors="pt" ) return to_device(inputs, self.device) def tokenize_dataset( self, dataset: Union[Dataset, DatasetDict], max_token_length: int ) -&gt; Union[Dataset, DatasetDict]: def tokenize_fn(batch): tokenized = self.tokenizer( batch["text"], padding=False, truncation=True, max_length=max_token_length ) return tokenized data_tokenized = dataset.map( tokenize_fn, batched=True, remove_columns=["text", "label"], ) return data_tokenized def data_collator( self, mlm: bool = False ) -&gt; DataCollatorForLanguageModeling: data_collator = DataCollatorForLanguageModeling( tokenizer=self.tokenizer, mlm=mlm ) return data_collator def load_model( self, ) -&gt; PreTrainedModel: model = AutoModelForCausalLM.from_pretrained( self.model_name, ).to(self.device) model.config.pad_token_id = self.tokenizer.pad_token_id return model def evaluate_model( self, prompt: Union[list[str], str], model: PreTrainedModel, max_new_tokens: int = 25, do_sample: bool = True, top_k: int = 25, top_p: float = 0.95, temperature: float = 0.7, repetition_penalty: Union[int, float] = 1.1 ) -&gt; Union[tc.Tensor, GenerateOutput]: inputs = self.tokenize_text(prompt) model.eval() with tc.no_grad(): outputs = model.generate( **inputs, max_new_tokens=max_new_tokens, do_sample=do_sample, top_k=top_k, top_p=top_p, temperature=temperature, repetition_penalty=repetition_penalty, pad_token_id=self.tokenizer.pad_token_id ) return outputs I will omit the explanation of each method since most of these were explained in the previous post, but I will go over key differences. For decoders, we will use the Hugging Face class AutoModelForCausalLM since we are now dealing with a causal model, which means that the model depends on the previous information to generate the next word. This is the key difference between encoders and decoders. The next key difference is found in the method tokenize_dataset. We had previously made the default for padding True, but for decoders we make it False. The reason has to do with the length of the reviews. Since we are setting the max token length to be $128$ (see parameters), no padding will actually occur since this length is less than the total length of a review. Hence the token vector will be fully composed of tokens with no blank space and padding would be useless. We are also now using a pre-defined data collator initialized in the method data_collator, where we import the Hugging Face class DataCollatorForLanguageModeling. This collator automatically takes care of padding (if needed) and masking (as well as other things) for language models. For a causal languange model in particular, we set mlm=False, which tells the collator that the model should be trying to predict the next token. If true, the collator would follow BERT’s stragety of data preparation. There is actually a Hugging Face class called DataCollatorForCausalLM, which is used primarily for text generation models, but I am working with an older version of the transformers library where it is not yet available. Another difference is that we have now included an evaluate_model method. As one can infer, it is used to evaluate the model using new, unseen data, but now we have different parameters to consider. Let’s go over each individual parameter in model.generate(): max_new_tokens = $25$ This is the total amount of tokens that will be used in the text generation. do_sample = True Tells model to sample from probability distribution instead of picking the highest-probability token. do_sample = False $\rightarrow$ deterministic. do_sample = True $\rightarrow$ random sampling. top_k = $25$ Sorts logits, keeps top $25$ most probable tokens, and draws samples from them. Lower k $\rightarrow$ more deterministic. Higher k $\rightarrow$ more random. top_p = $0.95$ Keeps the smallest set of tokens whose cumulative probability $\ge 0.95$. Can be used in conjunction with top_k, which amounts to picking tokens that intersect with these two conditions. Helps adapt to uncertainty by keeping the token selection dynamic since it is based on a probability mass as opposed to a fixed number. temperature = $0.7$ Scales softmax distribution before sampling. Lower T $\rightarrow$ more deterministic. Higher T $\rightarrow$ more random. repetition_penalty = $1.1$ Discourages the same token from reappearing by scaling all logits that have already been used by some value $&gt; 1$. This occurs when there is a lack of sampling diversity, which causes reinforcement of certain phrases/tokens. Now, we look at the TrainingArguments and Trainer classes: training_args = TrainingArguments( output_dir=cfg.res_path, overwrite_output_dir=True, report_to="none", eval_strategy=cfg.strategy, save_strategy=cfg.strategy, logging_strategy= cfg.strategy, learning_rate=cfg.lr, per_device_train_batch_size=cfg.train_batch_size, per_device_eval_batch_size=cfg.eval_batch_size, num_train_epochs=cfg.epochs, warmup_ratio=cfg.warmup_ratio, seed=cfg.seed ) The TrainingArguments parameters are mostly the same as for the encoder. One big difference is the parameter warmup_ratio. This parameter controls the learning rate at the start of the training by slowly ramping up the learning rate. It initially starts the learning rate at $0$, then linearly increases it until it reaches the max learning rate (chosen by user) and decays from there. The warmup length is given in steps, which is taken by scaling the total steps by the warmup ratio. The purpose of this parameter is to gradually change the model weights at the start of training since the weights are not yet stable. The main body of the Trainer is essentially the same as before, expect the last two lines are replaced by the data collator. In this case, we choose to not pass in any metrics since we will be calculating the perplexity of the evaluation loss (explained later). The Trainer looks like the following: trainer = Trainer( model=model, args=training_args, train_dataset=data_tokenized["train"].shuffle(seed=cfg.seed).select(range(cfg.train_size)), eval_dataset=data_tokenized["test"].shuffle(seed=cfg.seed).select(range(cfg.val_size)), data_collator=data_collator ) As before, we pass in the model, the training arguments, the different datasets, and the collator (new!) into the trainer. These datasets are huge so we shuffe, seed, and select a subset of the train ($3000$ samples) and test ($500$ samples) set as a demonstration. In actual fine-tuning, it’s essential to use all the available data. Now, all we have to do is run trainer.train() and the model begins to train. For our example, we get the following metrics: Epoch Training Loss Validation Loss 1 3.870400 3.709818 2 3.685000 3.707382 3 3.607300 3.709974 While the training loss is decreasing, it is not decreasing by much. On the other hand, the validation loss starts to decrease then slightly increases. While this may seem troubling at first glance, it is actually not that bad. GPT-2 is actually a very large model, which means that fine-tuning will only slightly decrease the loss. Not to mention, we are only fine-tuning with $3000$ samples. This is such a small sample size in comparison to the data used to train the model that such a small step size in training loss is to be expected. This also partly explains the increase in validation loss during the last epoch since the sample size for the validation dataset is $500$. The change in loss is in the hundredth decimal place, which for most applications can be attributed to small fluctuations (a.k.a. noise). There is a possibility of slight overfitting, but I don’t think there is enough proof. More tests need to be considered with a larger sample size to determine if the model is indeed overfitting. We can also calculate the perplexity to determine how well the model learned. Perplexity is a way to measure how well a model can predict the next token [8]. It is calculated as the exponent of the average cross-entropy loss. In other words, \[\mathrm{perplexity} = e^{\mathrm{loss}}.\] The lower the perplexity, the less token ‘‘choices’’ the model has, hence the more accurate the model prediction. The higher the perplexity, the more token ‘‘choices’’, the more uncertain the model is of its predictions. Ideally, the perplexity has an exponential decay behavior as the model learns. Here is a plot of the perplexity for the training and validation phases: From the plot, we see that the training perplexity does indeed follow an exponential decay behavior. On the other hand, the validation perplexity increases, which is attributed to what was already mentioned above. Note that steps is the global steps taken during the fine-tuning of the model. Now that our model is fine-tuned, let’s see how well it can autocomplete movie reviews. Here are beginning of reviews that I created: reviews = [ "This movie was terrible. The acting was mediocre.", "I highly recommend this movie to everyone.", "This was a great movie. I wish it was longer.", "I do not recommend this movie. The actors are terrible.", ] In this example, I chose to make the sentiment obvious to see how well the model could understand this and expand on the review. The code that calls the evaluate_model method and prints out the generated text is: outputs = inst.evaluate_model(reviews, model) for i, seq in enumerate(outputs): print(f"Seq. {i + 1}:") print(f"{inst.tokenizer.decode(seq, skip_special_tokens=True)}") print("\n") Here are the outputs of the model: Seq. 1: This movie was terrible. The acting was mediocre.It is a very good horror film, but I think it will appeal to many people as well (especially the young ones). Seq. 2: I highly recommend this movie to everyone. If you want a good laugh, go with the story and characters of The Sopranos. If not then read Seq. 3: This was a great movie. I wish it was longer. It is not as good as the first one, but this time you can see why people are so angry and upset about what Seq. 4: I do not recommend this movie. The actors are terrible. They had an awful time with the acting in it, and I have yet to see a single scene where they did anything other Overall, the expanded reviews generated by the model seem to somewhat fit the sentiment of the input reviews. There are no issues with Sequence 1 and 4, but we should analyze the other two. Sequence 2 seems to deviate from the inital sentiment. It goes on to talk about The Sopranos even though the input text did not mention anything about that show. Sequence 3 seems to not quite understand what sentiment it is supposed to show, but this is because the sentence ‘‘I wish it was longer.’’ can be interpreted as a negative sentiment even though the first sentence has a positive sentiment. So the output seems to make sense. Let’s try some harder reviews. reviews = [ "Wow! I was suprised by how much ", "This film was such a surprise. ", "I never thought I would ", "This movie makes me want to yell " ] As you can tell, these reviews are more difficult to determine sentiment from. They are ambiguous as to whether they think the movie was good or bad. Let’s see how well the model categorizes them: Seq. 1: Wow! I was suprised by how much vernacular this film used to be, and also the way it kept me guessing. For starters: How would you like a Seq. 2: This film was such a surprise. A beautiful movie with some great characters, the director and actors had wonderful chemistry to create this story that is very entertaining - even Seq. 3: I never thought I would This movie was so bad, but it is. It's not that BAD though! Just an awful story with no redeeming Seq. 4: This movie makes me want to yell The Devil is a Bad Idea. It's just terrible, it has no suspense and the plot doesn't move at all! In this case, the model does a decent job in generating reviews with varying sentiment. This may be because the second batch gave the model more flexibility in producing reviews that kept to one sentiment, as opposed to giving it a sentiment and making it work off that. Sequence 3 and 4 do seem to have fluidity issues, but they still kept a consistent sentiment. Overall, the text generation for both batches were pretty good for the amount of data we used to fine-tune the model. In general, the model’s performance can be improved by fine-tuning with a larger dataset. I purposely chose to cap the training set at $3000$ samples because my GPU kept running out of memory when running for a larger sample size over $3$ epochs. This can potentially lead to issues since it is known that fine-tuning with small datasets can potentially lead to overfitting or ‘‘catastrophic forgetting’’, which is where the model loses previously learned information [8]. Another way to improve performance is to increase max_token_len. I chose $128$, but ideally, one should choose $256$ or $512$. The reason I had to go with $128$ was also because of my GPU. I did try running the fine-tuning on my CPU with larger sample sizes and/or max token lengths, but it was not feasible due to time constraints. Another test that can be done is training with more epochs to get more steps on the perplexity curves. This could help rule out the model overfitting. Of course, one can also experiment with varying the learning rate, batch size, and warmup ratio, or include weight decay. As a sidenote, a common way to avoid catastrophic forgetting is using parameter-efficient fine-tuning techniques such as Low-Rank Adaptation (LoRA) [8]. This technique works by freezing the model weights and fine-tuning with a batch of small low-rank matrices (called LoRA adapter) instead of the full model parameter set, making the fine-tuning process much faster and more memory-efficient. This drastically reduces the number of parameters that need to be trained, as well as keeps the base model unchanged. Afterwards, if one chooses to change the behavior of the model, the LoRA adapter can be swapped out and the base model can once again be fine-tuned with a different LoRA adapter. In summary, we have learned how to fine-tuned a GPT-2 model to understand how reviews sound (good or bad) and determine what kind of sentence it should generate in continuation. We also learned about perplexity and how it can be used to describe the effectiveness of a model to generate accurate text. Overall, the model performed well on reviews that had pre-defined and obvious sentiment, but did not do so well with ambiguous reviews, which was expected. There is room for increasing the model’s capability of producing better review continuations, as well as making better conclusions on ambiguous inputs. The code and plots presented in this post can be found on my Github under the gpt-2-gen repo [8]. Feel free to reach out if you have any questions about what we covered this week. I’ll be taking a few weeks off, but will come back with more about building and employing decoders. Stay tuned! [1] Dzmitry Bahdanau, Kyunghyun Cho, &amp; Yoshua Bengio, Neural Machine Translation by Jointly Learning to Align and Translate, arXiv preprint arXiv:1409.0473, 2014. [2] Ashish Vaswani, Noam Shazeer, Niki Parmar, et al., Attention is All you Need, Advances in Neural Information Processing Systems, 2017. [3] Yoshua Bengio, Réjean Ducharme, Pascal Vincent, &amp; Christian Jauvin, A Neural Probabilistic Language Model, Journal of Machine Learning Research, 2003. [4] Alec Radford, Jeffrey Wu, Rewon Child, et al., Language models are unsupervised multitask learners, OpenAI blog, 2019. [5] Rafal Jozefowicz, Oriol Vinyals, Mike Schuster, et al., Exploring the Limits of Language Modeling, arXiv preprint arXiv:1602.02410, 2016. [6] Jeremy Howard &amp; Sebastian Ruder, Universal Language Model Fine-tuning for Text Classification, Proceedings of the 56th Annual Meeting of the Association for Computational Linguistics (Volume 1: Long Papers), 2018. DOI: 10.18653/v1/P18-1031. [7] Edward J. Hu, Yelong Shen, Phillip Wallis, et al., LoRA: Low-Rank Adaptation of Large Language Models, arXiv preprint arXiv:2106.09685, 2021. [8] Alberto J. Garcia, GPT-Style Language Model, 2025. Available at: https://github.com/AJG91/gpt-2-gen. ⬅ Back to Home]]></summary></entry><entry><title type="html">Natural language models: DistilBERT sentiment classifier</title><link href="https://ajg91.github.io//posts/large-language-models/encoder-distilbert-classifier/" rel="alternate" type="text/html" title="Natural language models: DistilBERT sentiment classifier" /><published>2025-11-16T00:00:00-08:00</published><updated>2025-11-16T00:00:00-08:00</updated><id>https://ajg91.github.io//posts/large-language-models/encoder-distilbert-classifier</id><content type="html" xml:base="https://ajg91.github.io//posts/large-language-models/encoder-distilbert-classifier/"><![CDATA[<p class="post-date">November 16, 2025</p>

<p>This week we will be using an encoder to identifying sentiment in movie reviews. First, I will walk through the fine-tuning process. Then, I will show how we use the encoder to identify sentiment with text that the model has not seen.</p>

<p>The classification problem is binary since the sentiment can either be positive or negative. The model we will be fine-tuning, as mentioned in the last post is DistilBERT. Like already discussed, DistilBERT is an encoder that is extracted from a bigger model known as BERT (Bidirectional Encoder Representations from Transformers). Compared to BERT, DistilBERT is about $56$% of the size and $60$% faster, but only retains $97$% of the performance. As is typical with natural language processing (NLP) models, we will be fine-tuning the model weights of an already trained model with a labeled dataset in order to orient the model towards our specific problem. We will use open-source code from Hugging Face, which is a company who has developed libraries for NLP applications and are most known for their transformers <a href="https://huggingface.co/docs/transformers/en/index" title="hf-transformers">library</a> <a class="citation" href="#wolf2020huggingfacestransformersstateoftheartnatural">[1]</a>.</p>

<p>The first step is defining our parameters. Like previously seen, our parameters will be saved in a <code class="language-plaintext highlighter-rouge">.yaml</code> file and we will use SimpleNamespace as an easy way to store and access these parameters. Here are the parameters we will be using:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
  <span class="sh">"</span><span class="s">model_name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">distilbert-base-uncased</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">seed</span><span class="sh">"</span><span class="p">:</span> <span class="mi">42</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">dpi</span><span class="sh">"</span><span class="p">:</span> <span class="mi">400</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">fig_path</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">../outputs/</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">res_path</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">../results/</span><span class="sh">"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>As can be see, the model name is <code class="language-plaintext highlighter-rouge">distilbert-base-uncased</code>. The next step is to load the dataset, model, and tokenizer. In the Notebook, this is done as follows:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">data</span> <span class="o">=</span> <span class="sh">"</span><span class="s">imdb</span><span class="sh">"</span>
<span class="n">dataset</span> <span class="o">=</span> <span class="nf">load_dataset</span><span class="p">(</span><span class="n">data</span><span class="p">)</span>

<span class="n">inst</span> <span class="o">=</span> <span class="nc">ModelAndTokenizer</span><span class="p">(</span><span class="n">cfg</span><span class="p">.</span><span class="n">model_name</span><span class="p">)</span>
<span class="n">model</span> <span class="o">=</span> <span class="n">inst</span><span class="p">.</span><span class="nf">load_classification_model</span><span class="p">()</span>
<span class="n">imdb_tokenized</span> <span class="o">=</span> <span class="n">inst</span><span class="p">.</span><span class="nf">tokenize_dataset</span><span class="p">(</span><span class="n">dataset</span><span class="p">)</span>
</code></pre></div></div>

<p>Before tokenizing, we much first specify which dataset we are going to be working with. Here, we will be using with the IMDb reviews, which we load using the Hugging Face function <code class="language-plaintext highlighter-rouge">load_dataset</code>. We create an instance of our custom class <code class="language-plaintext highlighter-rouge">ModelAndTokenizer</code>. This custom class contains methods to used for loading the model, tokenizing the dataset, etc. This class is defined as follows (I will omit the docstrings and unused methods for brevity. See the file <code class="language-plaintext highlighter-rouge">model.py</code> for more information):</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ModelAndTokenizer</span><span class="p">():</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">model_name</span><span class="p">:</span> <span class="nb">str</span>
    <span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">model_name</span> <span class="o">=</span> <span class="n">model_name</span>
        <span class="n">self</span><span class="p">.</span><span class="n">device</span> <span class="o">=</span> <span class="nf">get_device</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">:</span> <span class="n">PreTrainedTokenizerBase</span> <span class="o">=</span> <span class="n">AutoTokenizer</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="n">model_name</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">tokenize_text</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">prompt</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> 
        <span class="n">padding</span><span class="p">:</span> <span class="n">Union</span><span class="p">[</span><span class="nb">bool</span><span class="p">,</span> <span class="nb">str</span><span class="p">]</span> <span class="o">=</span> <span class="bp">True</span><span class="p">,</span> 
        <span class="n">truncation</span><span class="p">:</span> <span class="n">Union</span><span class="p">[</span><span class="nb">bool</span><span class="p">,</span> <span class="nb">str</span><span class="p">]</span> <span class="o">=</span> <span class="bp">True</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">]:</span>
        <span class="n">inputs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">tokenizer</span><span class="p">(</span>
            <span class="n">prompt</span><span class="p">,</span>
            <span class="n">padding</span><span class="o">=</span><span class="n">padding</span><span class="p">,</span>
            <span class="n">truncation</span><span class="o">=</span><span class="n">truncation</span><span class="p">,</span>
            <span class="n">return_tensors</span><span class="o">=</span><span class="sh">"</span><span class="s">pt</span><span class="sh">"</span>
        <span class="p">)</span>
        <span class="k">return</span> <span class="nf">to_device</span><span class="p">(</span><span class="n">inputs</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
    
    <span class="k">def</span> <span class="nf">tokenize_dataset</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">dataset</span><span class="p">:</span> <span class="n">Union</span><span class="p">[</span><span class="n">Dataset</span><span class="p">,</span> <span class="n">DatasetDict</span><span class="p">],</span> 
        <span class="n">padding</span><span class="p">:</span> <span class="n">Union</span><span class="p">[</span><span class="nb">bool</span><span class="p">,</span> <span class="nb">str</span><span class="p">]</span> <span class="o">=</span> <span class="bp">True</span><span class="p">,</span> 
        <span class="n">truncation</span><span class="p">:</span> <span class="n">Union</span><span class="p">[</span><span class="nb">bool</span><span class="p">,</span> <span class="nb">str</span><span class="p">]</span> <span class="o">=</span> <span class="bp">True</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Union</span><span class="p">[</span><span class="n">Dataset</span><span class="p">,</span> <span class="n">DatasetDict</span><span class="p">]:</span>
        <span class="n">data_tokenized</span> <span class="o">=</span> <span class="n">dataset</span><span class="p">.</span><span class="nf">map</span><span class="p">(</span>
            <span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">self</span><span class="p">.</span><span class="nf">tokenizer</span><span class="p">(</span>
                <span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">],</span>
                <span class="n">padding</span><span class="o">=</span><span class="n">padding</span><span class="p">,</span>
                <span class="n">truncation</span><span class="o">=</span><span class="n">truncation</span>
            <span class="p">),</span> 
            <span class="n">batched</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
            <span class="n">remove_columns</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">]</span>
        <span class="p">)</span>
        <span class="k">return</span> <span class="n">data_tokenized</span>

    <span class="k">def</span> <span class="nf">load_classification_model</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">attentions</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">False</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="n">PreTrainedModel</span><span class="p">:</span>
        <span class="n">model</span> <span class="o">=</span> <span class="n">AutoModelForSequenceClassification</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
            <span class="n">self</span><span class="p">.</span><span class="n">model_name</span><span class="p">,</span>
            <span class="n">output_attentions</span><span class="o">=</span><span class="n">attentions</span>
        <span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">model</span>
</code></pre></div></div>

<p>Starting from the top, we see that the input to the class is the model name, in this case <code class="language-plaintext highlighter-rouge">distilbert-base-uncased</code>. The <code class="language-plaintext highlighter-rouge">load_classification_model</code> method is how we load the model. For encoders, we will use the Hugging Face class <code class="language-plaintext highlighter-rouge">AutoModelForSequenceClassification</code> since we will be classifying text. We use the method <code class="language-plaintext highlighter-rouge">from_pretrained()</code> to load a pre-trained model, in this case DistilBERT. This method ensures that we also load the model weights. We also include the option of outputting the attentions. These are weights that inform us which are the tokens in a sequence that are more of an influence to specific tokens in that same sequence when processing the data. One can use this information to understand model behavior, token importance, or any model patterns. We also move the model to whatever device we want to use, such as CPU, GPU, etc.</p>

<p>The next step is to tokenize the dataset, which we do using the method <code class="language-plaintext highlighter-rouge">tokenize_dataset</code>. This method maps a function to each element of the dataset using a lambda function. The lambda function is what applies the tokenizer to the datset. In this case, it takes <code class="language-plaintext highlighter-rouge">x["text"]</code>, which is raw text strings, and converts them to tokens. Each sequence is padded to be the same length and the texts that are too long are truncated. The dataset is then batched and the initial <code class="language-plaintext highlighter-rouge">x["text"]</code> column is removed (this last step is optional). The output of this method will be a tokenized, batched dataset with <code class="language-plaintext highlighter-rouge">labels</code>, <code class="language-plaintext highlighter-rouge">input_ids</code>, and <code class="language-plaintext highlighter-rouge">attention_mask</code> columns. In our case, the initial dataset was composed of train, test, and unsupervised data with the following structure:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">DatasetDict</span><span class="p">({</span>
    <span class="n">train</span><span class="p">:</span> <span class="nc">Dataset</span><span class="p">({</span>
        <span class="n">features</span><span class="p">:</span> <span class="p">[</span><span class="sh">'</span><span class="s">text</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">label</span><span class="sh">'</span><span class="p">],</span>
        <span class="n">num_rows</span><span class="p">:</span> <span class="mi">25000</span>
    <span class="p">})</span>
    <span class="n">test</span><span class="p">:</span> <span class="nc">Dataset</span><span class="p">({</span>
        <span class="n">features</span><span class="p">:</span> <span class="p">[</span><span class="sh">'</span><span class="s">text</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">label</span><span class="sh">'</span><span class="p">],</span>
        <span class="n">num_rows</span><span class="p">:</span> <span class="mi">25000</span>
    <span class="p">})</span>
    <span class="n">unsupervised</span><span class="p">:</span> <span class="nc">Dataset</span><span class="p">({</span>
        <span class="n">features</span><span class="p">:</span> <span class="p">[</span><span class="sh">'</span><span class="s">text</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">label</span><span class="sh">'</span><span class="p">],</span>
        <span class="n">num_rows</span><span class="p">:</span> <span class="mi">50000</span>
    <span class="p">})</span>
<span class="p">})</span>
</code></pre></div></div>

<p>Now our tokenized dataset has the following structure:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">DatasetDict</span><span class="p">({</span>
    <span class="n">train</span><span class="p">:</span> <span class="nc">Dataset</span><span class="p">({</span>
        <span class="n">features</span><span class="p">:</span> <span class="p">[</span><span class="sh">'</span><span class="s">label</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">input_ids</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">attention_mask</span><span class="sh">'</span><span class="p">],</span>
        <span class="n">num_rows</span><span class="p">:</span> <span class="mi">25000</span>
    <span class="p">})</span>
    <span class="n">test</span><span class="p">:</span> <span class="nc">Dataset</span><span class="p">({</span>
        <span class="n">features</span><span class="p">:</span> <span class="p">[</span><span class="sh">'</span><span class="s">label</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">input_ids</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">attention_mask</span><span class="sh">'</span><span class="p">],</span>
        <span class="n">num_rows</span><span class="p">:</span> <span class="mi">25000</span>
    <span class="p">})</span>
    <span class="n">unsupervised</span><span class="p">:</span> <span class="nc">Dataset</span><span class="p">({</span>
        <span class="n">features</span><span class="p">:</span> <span class="p">[</span><span class="sh">'</span><span class="s">label</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">input_ids</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">attention_mask</span><span class="sh">'</span><span class="p">],</span>
        <span class="n">num_rows</span><span class="p">:</span> <span class="mi">50000</span>
    <span class="p">})</span>
<span class="p">})</span>
</code></pre></div></div>

<p>At this point, our dataset is ready to be passed into the encoder.</p>

<p>The next step is to create a training configuration. We do this using the configuration class TrainingArguments where we specify how the model should be trained. In our case we use:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">training_args</span> <span class="o">=</span> <span class="nc">TrainingArguments</span><span class="p">(</span>
    <span class="n">output_dir</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">res_path</span><span class="p">,</span>
    <span class="n">overwrite_output_dir</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
    <span class="n">eval_strategy</span><span class="o">=</span><span class="sh">"</span><span class="s">epoch</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">save_strategy</span><span class="o">=</span><span class="sh">"</span><span class="s">epoch</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">logging_strategy</span><span class="o">=</span><span class="sh">"</span><span class="s">epoch</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">learning_rate</span><span class="o">=</span><span class="mf">2e-5</span><span class="p">,</span>
    <span class="n">per_device_train_batch_size</span><span class="o">=</span><span class="mi">8</span><span class="p">,</span>
    <span class="n">num_train_epochs</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span>
    <span class="n">load_best_model_at_end</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div>

<p>The first line shows that the output directory where we output the trained model to is <code class="language-plaintext highlighter-rouge">res_path</code>, which is defined in our parameters yaml file. The next line specifies that the existing file in the directory will be overwritten by this model. The evaluation will be done every epoch, the checkpoints will be saved every epoch, and metrics logging will be done for every epoch, as well. The learning rate is initialized at $2 \times 10^{-5}$ and the batch size is chosen to be $8$. Overall, the training will be done over three epochs, and once training is done the best model will be loaded. WHile there are many more arguments that can be customized, I only show the basics. I refer you to the Hugging Face <a href="https://huggingface.co/docs/transformers/v4.57.1/en/main_classes/trainer#transformers.TrainingArguments" title="training-args">documentation</a> for an exhaustive list of the available parameters.</p>

<p>Once our training configuration is complete, we need to set up the training wrapper using Trainer. The Trainer class is a high-level training API provided by Hugging Face that automates the training process. Before, we had to write the training loop, which involved telling PyTorch to calculate the loss function, zero the gradients, propagate the loss, and step the optimizer. Trainer takes care of all the heavy lifting for us under the hood. We initialize the Trainer class as follows:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">trainer</span> <span class="o">=</span> <span class="nc">Trainer</span><span class="p">(</span>
    <span class="n">model</span><span class="o">=</span><span class="n">model</span><span class="p">,</span>
    <span class="n">args</span><span class="o">=</span><span class="n">training_args</span><span class="p">,</span>
    <span class="n">train_dataset</span><span class="o">=</span><span class="n">imdb_tokenized</span><span class="p">[</span><span class="sh">"</span><span class="s">train</span><span class="sh">"</span><span class="p">].</span><span class="nf">shuffle</span><span class="p">(</span><span class="n">seed</span><span class="o">=</span><span class="mi">42</span><span class="p">).</span><span class="nf">select</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="mi">2000</span><span class="p">)),</span>
    <span class="n">eval_dataset</span><span class="o">=</span><span class="n">imdb_tokenized</span><span class="p">[</span><span class="sh">"</span><span class="s">test</span><span class="sh">"</span><span class="p">].</span><span class="nf">shuffle</span><span class="p">(</span><span class="n">seed</span><span class="o">=</span><span class="mi">42</span><span class="p">).</span><span class="nf">select</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="mi">1000</span><span class="p">)),</span>
    <span class="n">processing_class</span><span class="o">=</span><span class="n">inst</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">,</span>
    <span class="n">compute_metrics</span><span class="o">=</span><span class="n">compute_metrics</span>
<span class="p">)</span>
</code></pre></div></div>

<p>As can be seen, we pass in the model, the training arguments, the different datasets, the tokenizer, and what metrics we want to use (in our case F1 score and accuracy). These datasets are huge so we shuffe, seed, and select a subset of the train ($2000$ samples) and test ($1000$ samples) set as a demonstration. In actual fine-tuning, it’s essential to use all the available data. Now, all we have to do is run <code class="language-plaintext highlighter-rouge">trainer.train()</code> and the model begins to train.</p>

<p>For our example, we get the following metrics:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Epoch 	Training Loss   Validation Loss    Accuracy        F1
  1 	   0.430200 	    0.303589 	   0.879000 	0.878852
  2 	   0.196300 	    0.402086 	   0.884000 	0.883926
  3 	   0.115300 	    0.430588 	   0.893000 	0.893013
</code></pre></div></div>

<p>Overall the metrics look good. The training loss is decreasing per epoch, which is what we are looking for. It also seems exponential, which is a good sign of healthy training. An increasing accuracy over epochs indicates that the model is classifying more examples correctly. The F1 score is also increasing <em>and</em> mirrors the accuracy. This implies that the dataset is balanced and there is no class bias occuring. While the validation loss does increase with each epoch meaning that the model is becoming overconfident and making more incorrect predictions as it trains, the increasing accuracy indicates that the model is still generalizing well. Steps can be taken to mitigate this issue such as early stopping, dropout, and reducing the learning rate.</p>

<p>Once trained, we check to see how well the model classifies the test data by viewing the metrics and plotting a confusion matrix:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{
  "test_loss": 0.32669803500175476,
  "test_accuracy": 0.8633333333333333,
  "test_f1 score": 0.8630762209036968
}
</code></pre></div></div>

<p><img src="/assets/images/nlp_model/confusion_matrix.png" alt="huber_base" /></p>

<p>The test metrics are slightly lower, but in the same range as the train metrics, which is a good sign. Plotting a confusion matrix is nice to understand how the predictions are distributed. In this case, the confusion matrix shows that the model is mostly correct in its predictions as the metrics already showed. Our model is now ready to go!</p>

<p>Remember that we are solving a binary classifier problem where sentiment can either be positive or negative so let’s test it on random reviews I generated:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>reviews = [
    "That movie was absolutely fantastic!",
    "I will never watch this again!",
    "Highly recommend to never watch again.",
    "Fantastic movie",
    "I kept wondering when it would be over.",
    "You couldn't pay me to watch it again."
]
</code></pre></div></div>

<p>Judging from the reviews, we can conclude that the first and fourth reviews are positive sentiment, while the rest are negative. Let’s see what the model thinks. First, we tokenize the reviews. Then, we put the model in eval mode, get the outputs, and convert to probabilities:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">inputs</span> <span class="o">=</span> <span class="n">inst</span><span class="p">.</span><span class="nf">tokenize_text</span><span class="p">(</span><span class="n">reviews</span><span class="p">)</span>

<span class="n">model</span><span class="p">.</span><span class="nf">eval</span><span class="p">()</span>
<span class="k">with</span> <span class="n">tc</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
    <span class="n">outputs</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="o">**</span><span class="n">inputs</span><span class="p">)</span>
    
<span class="n">probs</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">outputs</span><span class="p">.</span><span class="n">logits</span><span class="p">.</span><span class="nf">to</span><span class="p">(</span><span class="sh">"</span><span class="s">cpu</span><span class="sh">"</span><span class="p">),</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
<span class="n">predicted_classes</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">probs</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
<span class="n">labels</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">negative</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">positive</span><span class="sh">"</span><span class="p">]</span>
</code></pre></div></div>

<p>The results are the following:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Text: 'That movie was absolutely fantastic!'
Probabilities: tensor([0.0520, 0.9480])
Predicted sentiment: positive (0.95 confidence)

Text: 'I will never watch this again!'
Probabilities: tensor([0.7030, 0.2970])
Predicted sentiment: negative (0.70 confidence)

Text: 'Highly recommend to never watch again.'
Probabilities: tensor([0.5231, 0.4769])
Predicted sentiment: negative (0.52 confidence)

Text: 'Fantastic movie.'
Probabilities: tensor([0.1731, 0.8269])
Predicted sentiment: positive (0.83 confidence)

Text: 'I kept wondering when it would be over.'
Probabilities: tensor([0.6879, 0.3121])
Predicted sentiment: negative (0.69 confidence)

Text: 'You couldn't pay me to watch it again.'
Probabilities: tensor([0.8785, 0.1215])
Predicted sentiment: negative (0.88 confidence)
</code></pre></div></div>

<p>Overall, the model performed well on the reviews. The model managed to correctly predict the sentiment for all the reviews. The first review was very clear in what it was trying to say, hence the high confidence in the positive sentiment classification. The same could be said about the last view when it comes to high confidence in negative sentiment classification.</p>

<p>The third review was borderline, but that’s because of the phrasing. It’s kind of tricky so the model may have a hard time classifying it. In fact, if we run the fine-tuning on different seeds, the third review constantly flips between negative and positive with the probabilities being borderline as they are now. It’s possible that edge cases like this are present in the dataset, which could explain some of the misclassified reviews in the off-diagonal portions of the confusion matrix plot. I would surmise that if we fine-tuned the model with the full training dataset, instead of just a subset, we may have been able to lower the values in the off-diagonal. Regardless, the values are small compared to the diagonal, so the subset chosen is good enough for our application.</p>

<p>We have managed to fine-tune a DistilBERT model to predict sentiment in movie reviews. To summarize, we have walked through the process of fine-tuning an encoder to perform a specific task, which was classify movie reviews. The process involved importing the pre-trained language model, tokenizing the dataset, setting up the training arguments and the Trainer class, fine-tuning the model, then seeing how well the model performs with unseen data. We then made up reviews, with one curveball in the mix, and the model managed to correctly classify them. Overall, we have now learned about encoders, how to fine-tune one for a specific task, and produced a model that performed well on a binary classification problem.</p>

<p>The code and confusion matrix plot presented in this post can be found on my Github under the <a href="https://github.com/AJG91/nlp-model" title="nlp-model-repo">nlp-model</a> repo <a class="citation" href="#Garcia2025NLPModel">[2]</a>. Feel free to reach out if you have any questions about what we covered this week. Next week we will move on to decoders. Stay tuned!</p>

<ol class="bibliography"><li><span id="wolf2020huggingfacestransformersstateoftheartnatural">[1] Thomas Wolf, Lysandre Debut, Victor Sanh, et al., <i>HuggingFace’s Transformers: State-of-the-art Natural Language Processing</i>, 2020. Available at: https://arxiv.org/abs/1910.03771.</span></li>
<li><span id="Garcia2025NLPModel">[2] Alberto J. Garcia, <i>NLP Model</i>, 2025. Available at: https://github.com/AJG91/nlp-model.</span></li></ol>
<p><a href="/" class="btn">⬅ Back to Home</a></p>]]></content><author><name></name></author><category term="large-language-models" /><summary type="html"><![CDATA[November 16, 2025 This week we will be using an encoder to identifying sentiment in movie reviews. First, I will walk through the fine-tuning process. Then, I will show how we use the encoder to identify sentiment with text that the model has not seen. The classification problem is binary since the sentiment can either be positive or negative. The model we will be fine-tuning, as mentioned in the last post is DistilBERT. Like already discussed, DistilBERT is an encoder that is extracted from a bigger model known as BERT (Bidirectional Encoder Representations from Transformers). Compared to BERT, DistilBERT is about $56$% of the size and $60$% faster, but only retains $97$% of the performance. As is typical with natural language processing (NLP) models, we will be fine-tuning the model weights of an already trained model with a labeled dataset in order to orient the model towards our specific problem. We will use open-source code from Hugging Face, which is a company who has developed libraries for NLP applications and are most known for their transformers library [2]. The first step is defining our parameters. Like previously seen, our parameters will be saved in a .yaml file and we will use SimpleNamespace as an easy way to store and access these parameters. Here are the parameters we will be using: { "model_name": "distilbert-base-uncased", "seed": 42, "dpi": 400, "fig_path": "../outputs/", "res_path": "../results/" } As can be see, the model name is distilbert-base-uncased. The next step is to load the dataset, model, and tokenizer. In the Notebook, this is done as follows: data = "imdb" dataset = load_dataset(data) inst = ModelAndTokenizer(cfg.model_name) model = inst.load_classification_model() imdb_tokenized = inst.tokenize_dataset(dataset) Before tokenizing, we much first specify which dataset we are going to be working with. Here, we will be using with the IMDb reviews, which we load using the Hugging Face function load_dataset. We create an instance of our custom class ModelAndTokenizer. This custom class contains methods to used for loading the model, tokenizing the dataset, etc. This class is defined as follows (I will omit the docstrings and unused methods for brevity. See the file model.py for more information): class ModelAndTokenizer(): def __init__( self, model_name: str ): self.model_name = model_name self.device = get_device() self.tokenizer: PreTrainedTokenizerBase = AutoTokenizer.from_pretrained(model_name) def tokenize_text( self, prompt: str, padding: Union[bool, str] = True, truncation: Union[bool, str] = True ) -&gt; dict[str, tc.Tensor]: inputs = self.tokenizer( prompt, padding=padding, truncation=truncation, return_tensors="pt" ) return to_device(inputs, self.device) def tokenize_dataset( self, dataset: Union[Dataset, DatasetDict], padding: Union[bool, str] = True, truncation: Union[bool, str] = True ) -&gt; Union[Dataset, DatasetDict]: data_tokenized = dataset.map( lambda x: self.tokenizer( x["text"], padding=padding, truncation=truncation ), batched=True, remove_columns=["text"] ) return data_tokenized def load_classification_model( self, attentions: bool = False ) -&gt; PreTrainedModel: model = AutoModelForSequenceClassification.from_pretrained( self.model_name, output_attentions=attentions ).to(self.device) return model Starting from the top, we see that the input to the class is the model name, in this case distilbert-base-uncased. The load_classification_model method is how we load the model. For encoders, we will use the Hugging Face class AutoModelForSequenceClassification since we will be classifying text. We use the method from_pretrained() to load a pre-trained model, in this case DistilBERT. This method ensures that we also load the model weights. We also include the option of outputting the attentions. These are weights that inform us which are the tokens in a sequence that are more of an influence to specific tokens in that same sequence when processing the data. One can use this information to understand model behavior, token importance, or any model patterns. We also move the model to whatever device we want to use, such as CPU, GPU, etc. The next step is to tokenize the dataset, which we do using the method tokenize_dataset. This method maps a function to each element of the dataset using a lambda function. The lambda function is what applies the tokenizer to the datset. In this case, it takes x["text"], which is raw text strings, and converts them to tokens. Each sequence is padded to be the same length and the texts that are too long are truncated. The dataset is then batched and the initial x["text"] column is removed (this last step is optional). The output of this method will be a tokenized, batched dataset with labels, input_ids, and attention_mask columns. In our case, the initial dataset was composed of train, test, and unsupervised data with the following structure: DatasetDict({ train: Dataset({ features: ['text', 'label'], num_rows: 25000 }) test: Dataset({ features: ['text', 'label'], num_rows: 25000 }) unsupervised: Dataset({ features: ['text', 'label'], num_rows: 50000 }) }) Now our tokenized dataset has the following structure: DatasetDict({ train: Dataset({ features: ['label', 'input_ids', 'attention_mask'], num_rows: 25000 }) test: Dataset({ features: ['label', 'input_ids', 'attention_mask'], num_rows: 25000 }) unsupervised: Dataset({ features: ['label', 'input_ids', 'attention_mask'], num_rows: 50000 }) }) At this point, our dataset is ready to be passed into the encoder. The next step is to create a training configuration. We do this using the configuration class TrainingArguments where we specify how the model should be trained. In our case we use: training_args = TrainingArguments( output_dir=cfg.res_path, overwrite_output_dir=True, eval_strategy="epoch", save_strategy="epoch", logging_strategy="epoch", learning_rate=2e-5, per_device_train_batch_size=8, num_train_epochs=3, load_best_model_at_end=True, ) The first line shows that the output directory where we output the trained model to is res_path, which is defined in our parameters yaml file. The next line specifies that the existing file in the directory will be overwritten by this model. The evaluation will be done every epoch, the checkpoints will be saved every epoch, and metrics logging will be done for every epoch, as well. The learning rate is initialized at $2 \times 10^{-5}$ and the batch size is chosen to be $8$. Overall, the training will be done over three epochs, and once training is done the best model will be loaded. WHile there are many more arguments that can be customized, I only show the basics. I refer you to the Hugging Face documentation for an exhaustive list of the available parameters. Once our training configuration is complete, we need to set up the training wrapper using Trainer. The Trainer class is a high-level training API provided by Hugging Face that automates the training process. Before, we had to write the training loop, which involved telling PyTorch to calculate the loss function, zero the gradients, propagate the loss, and step the optimizer. Trainer takes care of all the heavy lifting for us under the hood. We initialize the Trainer class as follows: trainer = Trainer( model=model, args=training_args, train_dataset=imdb_tokenized["train"].shuffle(seed=42).select(range(2000)), eval_dataset=imdb_tokenized["test"].shuffle(seed=42).select(range(1000)), processing_class=inst.tokenizer, compute_metrics=compute_metrics ) As can be seen, we pass in the model, the training arguments, the different datasets, the tokenizer, and what metrics we want to use (in our case F1 score and accuracy). These datasets are huge so we shuffe, seed, and select a subset of the train ($2000$ samples) and test ($1000$ samples) set as a demonstration. In actual fine-tuning, it’s essential to use all the available data. Now, all we have to do is run trainer.train() and the model begins to train. For our example, we get the following metrics: Epoch Training Loss Validation Loss Accuracy F1 1 0.430200 0.303589 0.879000 0.878852 2 0.196300 0.402086 0.884000 0.883926 3 0.115300 0.430588 0.893000 0.893013 Overall the metrics look good. The training loss is decreasing per epoch, which is what we are looking for. It also seems exponential, which is a good sign of healthy training. An increasing accuracy over epochs indicates that the model is classifying more examples correctly. The F1 score is also increasing and mirrors the accuracy. This implies that the dataset is balanced and there is no class bias occuring. While the validation loss does increase with each epoch meaning that the model is becoming overconfident and making more incorrect predictions as it trains, the increasing accuracy indicates that the model is still generalizing well. Steps can be taken to mitigate this issue such as early stopping, dropout, and reducing the learning rate. Once trained, we check to see how well the model classifies the test data by viewing the metrics and plotting a confusion matrix: { "test_loss": 0.32669803500175476, "test_accuracy": 0.8633333333333333, "test_f1 score": 0.8630762209036968 } The test metrics are slightly lower, but in the same range as the train metrics, which is a good sign. Plotting a confusion matrix is nice to understand how the predictions are distributed. In this case, the confusion matrix shows that the model is mostly correct in its predictions as the metrics already showed. Our model is now ready to go! Remember that we are solving a binary classifier problem where sentiment can either be positive or negative so let’s test it on random reviews I generated: reviews = [ "That movie was absolutely fantastic!", "I will never watch this again!", "Highly recommend to never watch again.", "Fantastic movie", "I kept wondering when it would be over.", "You couldn't pay me to watch it again." ] Judging from the reviews, we can conclude that the first and fourth reviews are positive sentiment, while the rest are negative. Let’s see what the model thinks. First, we tokenize the reviews. Then, we put the model in eval mode, get the outputs, and convert to probabilities: inputs = inst.tokenize_text(reviews) model.eval() with tc.no_grad(): outputs = model(**inputs) probs = tc.softmax(outputs.logits.to("cpu"), dim=-1) predicted_classes = tc.argmax(probs, dim=-1) labels = ["negative", "positive"] The results are the following: Text: 'That movie was absolutely fantastic!' Probabilities: tensor([0.0520, 0.9480]) Predicted sentiment: positive (0.95 confidence) Text: 'I will never watch this again!' Probabilities: tensor([0.7030, 0.2970]) Predicted sentiment: negative (0.70 confidence) Text: 'Highly recommend to never watch again.' Probabilities: tensor([0.5231, 0.4769]) Predicted sentiment: negative (0.52 confidence) Text: 'Fantastic movie.' Probabilities: tensor([0.1731, 0.8269]) Predicted sentiment: positive (0.83 confidence) Text: 'I kept wondering when it would be over.' Probabilities: tensor([0.6879, 0.3121]) Predicted sentiment: negative (0.69 confidence) Text: 'You couldn't pay me to watch it again.' Probabilities: tensor([0.8785, 0.1215]) Predicted sentiment: negative (0.88 confidence) Overall, the model performed well on the reviews. The model managed to correctly predict the sentiment for all the reviews. The first review was very clear in what it was trying to say, hence the high confidence in the positive sentiment classification. The same could be said about the last view when it comes to high confidence in negative sentiment classification. The third review was borderline, but that’s because of the phrasing. It’s kind of tricky so the model may have a hard time classifying it. In fact, if we run the fine-tuning on different seeds, the third review constantly flips between negative and positive with the probabilities being borderline as they are now. It’s possible that edge cases like this are present in the dataset, which could explain some of the misclassified reviews in the off-diagonal portions of the confusion matrix plot. I would surmise that if we fine-tuned the model with the full training dataset, instead of just a subset, we may have been able to lower the values in the off-diagonal. Regardless, the values are small compared to the diagonal, so the subset chosen is good enough for our application. We have managed to fine-tune a DistilBERT model to predict sentiment in movie reviews. To summarize, we have walked through the process of fine-tuning an encoder to perform a specific task, which was classify movie reviews. The process involved importing the pre-trained language model, tokenizing the dataset, setting up the training arguments and the Trainer class, fine-tuning the model, then seeing how well the model performs with unseen data. We then made up reviews, with one curveball in the mix, and the model managed to correctly classify them. Overall, we have now learned about encoders, how to fine-tune one for a specific task, and produced a model that performed well on a binary classification problem. The code and confusion matrix plot presented in this post can be found on my Github under the nlp-model repo [2]. Feel free to reach out if you have any questions about what we covered this week. Next week we will move on to decoders. Stay tuned! [1] Thomas Wolf, Lysandre Debut, Victor Sanh, et al., HuggingFace’s Transformers: State-of-the-art Natural Language Processing, 2020. Available at: https://arxiv.org/abs/1910.03771. [2] Alberto J. Garcia, NLP Model, 2025. Available at: https://github.com/AJG91/nlp-model. ⬅ Back to Home]]></summary></entry><entry><title type="html">Natural language models: An introduction to human-computer language interaction</title><link href="https://ajg91.github.io//posts/large-language-models/natural-language-processing-model/" rel="alternate" type="text/html" title="Natural language models: An introduction to human-computer language interaction" /><published>2025-11-02T00:00:00-07:00</published><updated>2025-11-02T00:00:00-07:00</updated><id>https://ajg91.github.io//posts/large-language-models/natural-language-processing-model</id><content type="html" xml:base="https://ajg91.github.io//posts/large-language-models/natural-language-processing-model/"><![CDATA[<p class="post-date">November 02, 2025</p>

<p>This week we will be discussed natural language models (NLPs) and how they process text. NLP models are an interface for computers to understand, interpret, and generate human language. They achieve this by representing words, phrases, and sentences as vectors (also known as word embeddings), and training on large amounts of these vectors (a.k.a. large amounts of text) to learn their underlying patterns and relationships. These vectors capture the different meanings that a word can have and are used to relate words. Similar words, such as “cat” and “dog” will be close to each other in the vector space, while words like “car” and “kitchen” will be further apart. In summary, these vectors tell the models how all the words in a sentence relate to each other <a class="citation" href="#Chadha2021DistilledWordVectors">[1]</a>.</p>

<p>The process of producing these vectors is important to understand. Let’s consider the phrase “I love dogs.” for simplicity. As discussed in the previous paragraph, a computer understands 1’s and 0’s, not text. So we must figure out how to convert this text to numbers. First, we must tokenize the text. This means breaking the phrase up into single units. In our example, this would be broken up into the following:</p>

<p>“I love dogs.” -&gt; [“I”, “love”, “ dogs”, “.”]</p>

<p>Notice that the spaces are not perserved and the punctuation is independent. Depending on the tokenizer, spaces may or may not be perserved. Each individual entry in the list will then be converted to a number called token IDs. These IDs are then converted into vectors using an embedding matrix. This is essentially a predefined lookup table that tells the model what vector corresponds to each token. All models have this built into them and can be found by doing something like <code class="language-plaintext highlighter-rouge">model.embeddings.word_embeddings</code>.</p>

<p>One may think, “I get that there is a table where one can see what vector corresponds to each token, but doesn’t context matter?” Yes! Context is important when building one of these models. A word being at the beginning of a phrase may have a completely different meaning when found towards the end of the phrase. Multiple things come into play. There is the concept of attention, which means that one word gets to see all other words in the sentence so that long-range relationships can be captured. The advantage to this is that the full context of a sentence can be understood, and this process can be parallelized so that all tokens are processed simultaneously. Additionally, positional embeddings also come into play. Once the vector from the lookup table is found, positional information is added to these vectors. This is to tell the model where in the phrase that specific token was at. So the word at position 0 in one phrase and at position 5 in a different phrase will get slightly different vectors. It is these vectors that are then used to fine-tune the model. An example of this is the following sentence:</p>

<p>“The dog chases the cat.”</p>

<p>If positional embeddings are not implemented, the model will not know the positions of the words since the tokens are processed in parallel so an identical sentence would be:</p>

<p>“The cat chases the dog.”</p>

<p>The meaning of the sentence has now been lost.</p>

<p>As a note, one token does not always correspond to one word. For a work like “running”, this would be split up into “run” and “ning”, hence becoming two tokens.</p>

<p>Once trained, the tasks that can be performed by NLP models range from (but not limited to): <br /></p>
<ol>
  <li>Translations</li>
  <li>Summarizing information</li>
  <li>Identifying sentiment</li>
  <li>Generating text (e.g. telling a story)</li>
</ol>

<p>Some examples of popular NLP models that you may have heard of before Large Language Models (LLMs) such as <a href="https://openai.com/index/chatgpt/" title="chat-gpt">ChatGPT</a> from OpenAI and <a href="https://gemini.google/about/" title="gemini">Gemini</a> from Google. These models are both encoders and decoders. Encoders are used for understanding text. This is useful when you talk about identifying sentiment, classification, and recognition. On the other hand, decoders are used for text generation, such as summarization and speech-to-text conversion. Fundamentally, the difference between both is that encoders are bidirectional, which means that token $T_n$ is influenced by tokens $T_{n-1}$ and $T_{n+1}$, while decoders are autoreggresive, which means that token $T_n$ is influenced solely by tokens $T &lt; T_{n}$. They can also work hand-in-hand when considering tasks such as translations and summarization because first the model has to understand the full text, then it has to generate the new text.</p>

<p>The process of working with NLPs primarily revolve around fine-tuning instead of training from scratch. The reason is that these models are pre-trained with huge amounts of general language data so that the model understands how to output language. This type of training is computationally expensive and may not be achieveable on a personal laptop. Instead, we can take a model that has already been trained on a huge amount of data and tweak it to fit our problem. This is the fine-tuning process. Fine-tuning takes a labeled dataset and a loss function, and uses them to tweak the weights in the model towards the problem you are trying to solve. Another question that may arise is: “why would we tweak the model weights instead of just fine-tuning the classifier?” This is because tweaking the model weights allows for the pretrained model to adapt to the new domain for better accuracy. If one were to just fine-tune the classifier, the model itself may not produce accurate results for the specific task. In other words, you are orienting the model to what you need it to do.</p>

<p>For this set of posts, we will be working with two different models: an encoder and a decoder. For the encoder, we will be performing sentiment classification using DistilBERT (Distilled BERT), which is available on the Hugging Face <a href="https://huggingface.co/docs/transformers/en/model_doc/distilbert" title="distilbert">website</a> <a class="citation" href="#sanh2019distilbert">[2,3]</a>. This pre-trained encoder comes from a bigger model known as BERT (Bidirectional Encoder Representations from Transformers). Compared to BERT, DistilBERT is about $56$% of the size and $60$% faster, but only retains $97$% of the performance, hence it is a compressed version of BERT. Overall, it is a great choice for a small project since it’s lightweight, fast, and accurate. Since this model is already pre-trained using large datasets, our job is to fine-tune it to the specific problem we are considering.</p>

<p>For the decoder, we will be performing text generator using GPT-2 (Generative Pretrained Transformer 2). This pre-trained decoder is a transformer-based neural network that has been trained on a massive amount of text found on the internet. It’s purpose is to generate the next word in a sentence in a coherent and contextual way. This model was developed by OpenAI in 2019 and a smaller version (~124M parameters) was made available on the Hugging Face <a href="https://huggingface.co/openai-community/gpt2" title="hf-gpt2">website</a> <a class="citation" href="#radford2019language">[4]</a>. Like the encoder, this model is already pre-trained using large datasets, so our job would be to fine-tune it to the specific problem we are considering.</p>

<p>We have now gone over what natural language models and how they process text. We have also discussed encoders versus decoders and where they are useful. We finished off by introducing the model we will use and the concept of fine-tuning. Overall, NLPs are important to the foundation of LLMs since they supply the methods and frameworks that make human-computer language interaction possible.</p>

<p>Feel free to reach out if you have any questions about what we covered this week. We will show a concrete coding example where we showcase how to fine-tune these models. Stay tuned!</p>

<ol class="bibliography"><li><span id="Chadha2021DistilledWordVectors">[1] Vinija Jain &amp; Aman Chadha, <i>Word Vectors</i>, Distilled AI, 2021. </span></li>
<li><span id="sanh2019distilbert">[2] Victor Sanh, Lysandre Debut, Julien Chaumond, &amp; Thomas Wolf, <i>DistilBERT, a distilled version of BERT: smaller, faster, cheaper and lighter</i>, arXiv preprint arXiv:1910.01108, 2019. </span></li>
<li><span id="wolf2019huggingface">[3] Thomas Wolf, Lysandre Debut, Victor Sanh, et al., <i>HuggingFace’s Transformers: State-of-the-art Natural Language Processing</i>, arXiv preprint arXiv:1910.03771, 2019. </span></li>
<li><span id="radford2019language">[4] Alec Radford, Jeffrey Wu, Rewon Child, et al., <i>Language models are unsupervised multitask learners</i>, OpenAI blog, 2019. </span></li></ol>
<p><a href="/" class="btn">⬅ Back to Home</a></p>]]></content><author><name></name></author><category term="large-language-models" /><summary type="html"><![CDATA[November 02, 2025 This week we will be discussed natural language models (NLPs) and how they process text. NLP models are an interface for computers to understand, interpret, and generate human language. They achieve this by representing words, phrases, and sentences as vectors (also known as word embeddings), and training on large amounts of these vectors (a.k.a. large amounts of text) to learn their underlying patterns and relationships. These vectors capture the different meanings that a word can have and are used to relate words. Similar words, such as “cat” and “dog” will be close to each other in the vector space, while words like “car” and “kitchen” will be further apart. In summary, these vectors tell the models how all the words in a sentence relate to each other [4]. The process of producing these vectors is important to understand. Let’s consider the phrase “I love dogs.” for simplicity. As discussed in the previous paragraph, a computer understands 1’s and 0’s, not text. So we must figure out how to convert this text to numbers. First, we must tokenize the text. This means breaking the phrase up into single units. In our example, this would be broken up into the following: “I love dogs.” -&gt; [“I”, “love”, “ dogs”, “.”] Notice that the spaces are not perserved and the punctuation is independent. Depending on the tokenizer, spaces may or may not be perserved. Each individual entry in the list will then be converted to a number called token IDs. These IDs are then converted into vectors using an embedding matrix. This is essentially a predefined lookup table that tells the model what vector corresponds to each token. All models have this built into them and can be found by doing something like model.embeddings.word_embeddings. One may think, “I get that there is a table where one can see what vector corresponds to each token, but doesn’t context matter?” Yes! Context is important when building one of these models. A word being at the beginning of a phrase may have a completely different meaning when found towards the end of the phrase. Multiple things come into play. There is the concept of attention, which means that one word gets to see all other words in the sentence so that long-range relationships can be captured. The advantage to this is that the full context of a sentence can be understood, and this process can be parallelized so that all tokens are processed simultaneously. Additionally, positional embeddings also come into play. Once the vector from the lookup table is found, positional information is added to these vectors. This is to tell the model where in the phrase that specific token was at. So the word at position 0 in one phrase and at position 5 in a different phrase will get slightly different vectors. It is these vectors that are then used to fine-tune the model. An example of this is the following sentence: “The dog chases the cat.” If positional embeddings are not implemented, the model will not know the positions of the words since the tokens are processed in parallel so an identical sentence would be: “The cat chases the dog.” The meaning of the sentence has now been lost. As a note, one token does not always correspond to one word. For a work like “running”, this would be split up into “run” and “ning”, hence becoming two tokens. Once trained, the tasks that can be performed by NLP models range from (but not limited to): Translations Summarizing information Identifying sentiment Generating text (e.g. telling a story) Some examples of popular NLP models that you may have heard of before Large Language Models (LLMs) such as ChatGPT from OpenAI and Gemini from Google. These models are both encoders and decoders. Encoders are used for understanding text. This is useful when you talk about identifying sentiment, classification, and recognition. On the other hand, decoders are used for text generation, such as summarization and speech-to-text conversion. Fundamentally, the difference between both is that encoders are bidirectional, which means that token $T_n$ is influenced by tokens $T_{n-1}$ and $T_{n+1}$, while decoders are autoreggresive, which means that token $T_n$ is influenced solely by tokens $T &lt; T_{n}$. They can also work hand-in-hand when considering tasks such as translations and summarization because first the model has to understand the full text, then it has to generate the new text. The process of working with NLPs primarily revolve around fine-tuning instead of training from scratch. The reason is that these models are pre-trained with huge amounts of general language data so that the model understands how to output language. This type of training is computationally expensive and may not be achieveable on a personal laptop. Instead, we can take a model that has already been trained on a huge amount of data and tweak it to fit our problem. This is the fine-tuning process. Fine-tuning takes a labeled dataset and a loss function, and uses them to tweak the weights in the model towards the problem you are trying to solve. Another question that may arise is: “why would we tweak the model weights instead of just fine-tuning the classifier?” This is because tweaking the model weights allows for the pretrained model to adapt to the new domain for better accuracy. If one were to just fine-tune the classifier, the model itself may not produce accurate results for the specific task. In other words, you are orienting the model to what you need it to do. For this set of posts, we will be working with two different models: an encoder and a decoder. For the encoder, we will be performing sentiment classification using DistilBERT (Distilled BERT), which is available on the Hugging Face website [4,4]. This pre-trained encoder comes from a bigger model known as BERT (Bidirectional Encoder Representations from Transformers). Compared to BERT, DistilBERT is about $56$% of the size and $60$% faster, but only retains $97$% of the performance, hence it is a compressed version of BERT. Overall, it is a great choice for a small project since it’s lightweight, fast, and accurate. Since this model is already pre-trained using large datasets, our job is to fine-tune it to the specific problem we are considering. For the decoder, we will be performing text generator using GPT-2 (Generative Pretrained Transformer 2). This pre-trained decoder is a transformer-based neural network that has been trained on a massive amount of text found on the internet. It’s purpose is to generate the next word in a sentence in a coherent and contextual way. This model was developed by OpenAI in 2019 and a smaller version (~124M parameters) was made available on the Hugging Face website [4]. Like the encoder, this model is already pre-trained using large datasets, so our job would be to fine-tune it to the specific problem we are considering. We have now gone over what natural language models and how they process text. We have also discussed encoders versus decoders and where they are useful. We finished off by introducing the model we will use and the concept of fine-tuning. Overall, NLPs are important to the foundation of LLMs since they supply the methods and frameworks that make human-computer language interaction possible. Feel free to reach out if you have any questions about what we covered this week. We will show a concrete coding example where we showcase how to fine-tune these models. Stay tuned! [1] Vinija Jain &amp; Aman Chadha, Word Vectors, Distilled AI, 2021. [2] Victor Sanh, Lysandre Debut, Julien Chaumond, &amp; Thomas Wolf, DistilBERT, a distilled version of BERT: smaller, faster, cheaper and lighter, arXiv preprint arXiv:1910.01108, 2019. [3] Thomas Wolf, Lysandre Debut, Victor Sanh, et al., HuggingFace’s Transformers: State-of-the-art Natural Language Processing, arXiv preprint arXiv:1910.03771, 2019. [4] Alec Radford, Jeffrey Wu, Rewon Child, et al., Language models are unsupervised multitask learners, OpenAI blog, 2019. ⬅ Back to Home]]></summary></entry><entry><title type="html">Multi-layer perceptron: Hyperparameter tuning</title><link href="https://ajg91.github.io//posts/machine-learning/multi-layer-perceptron-hyperparameter-tuning/" rel="alternate" type="text/html" title="Multi-layer perceptron: Hyperparameter tuning" /><published>2025-10-12T00:00:00-07:00</published><updated>2025-10-12T00:00:00-07:00</updated><id>https://ajg91.github.io//posts/machine-learning/multi-layer-perceptron-hyperparameter-tuning</id><content type="html" xml:base="https://ajg91.github.io//posts/machine-learning/multi-layer-perceptron-hyperparameter-tuning/"><![CDATA[<p class="post-date">October 12, 2025</p>

<p>This week we will learn about how to build a multi-layer perceptron (MLP), as well as hyperparameter tuning and debugging the model. Some information may be duplicate from previous posts, but I will go over it again to keep the posts on MLPs self-contained.</p>

<p>What is a multi-layer perceptron (MLP)?<br />
A MLP is a artificial feedforward neural network that consists on multiple layers including an input layer, hidden layers (at least one), and an output layer. The layers are fully connected, which means that each neuron in layer a is connected to each neuron in layer b. Each connection is assigned a weight and each neuron a bias.</p>

<p>There are two phases when training a MLP: forward propagation and backward propagation. During forward propagation, the data flows from the input layer, through all the intermediate layers, and finally through the output layer with each layer processing the outputs of the previous one. The output is then compared to the truth and the error is calculated. During backpropagation, the error flows backwards through the layers, adjusting the weights and biases using techniques such as gradient descent, which tries to find the combinations of weights and bias that minimize the error. Once this process finishes for all weights and biases, the model will once again switch to forward propagation.</p>

<p>We will be using the <a href="https://scikit-learn.org/stable/modules/generated/sklearn.datasets.fetch_california_housing.html" title="ca-housing-docs">California housing</a> dataset we discussed last week to train and evaluate our model. This dataset contains aggregated data regarding each district in California such as median income, population, and location (latitude and longitude). For more information see this <a href="https://ajg91.github.io/posts/2025-10-05-multi-layer-perceptron-data-description/" title="mlp-blog-post-#1">post</a>.</p>

<p>Since we already went over the dataset in the last post, we can jump straight into the model and experiments. Our model class contains the following constructor (minus the docstring):</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MLP</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">input_dim</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">,</span> <span class="n">dropout</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">net</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">input_dim</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Dropout</span><span class="p">(</span><span class="n">dropout</span><span class="p">),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">hidden_dim</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">hidden_dim</span><span class="p">,</span> <span class="mi">1</span><span class="p">),</span>
        <span class="p">)</span>
</code></pre></div></div>

<p>Let’s break it down:</p>
<ol>
  <li><code class="language-plaintext highlighter-rouge">nn.Sequential</code><br />
    <ul>
      <li>This simplies building a neural network by organizing the layers in a sequential order. In other words, the network knows that it has to flow sequentially through the layers in the order defined.</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.Linear(input_dim, hidden_dim)</code><br />
    <ul>
      <li>This is a fully connected linear layer with input size input_dim (output of CNN) and output size of hidden_dim neurons.<br /></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.ReLU()</code><br />
    <ul>
      <li>This is a Rectified Linear Unit activation function. It is defined as $\text{ReLU}(x) = \text{max}(0, x)$. Essentially, it zeros out the negative values while introducing non-linearity. This type of activation function is simple and fast to use compared to others such as $\text{tanh}(x)$ or sigmoid.<br /></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.Dropout()</code><br />
    <ul>
      <li>This randomly turns off neurons (equivalent to zeroing out some inputs), forcing the network to learn more general features instead of relying solely on specific neurons.</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.Linear(hidden_dim, hidden_dim)</code><br />
    <ul>
      <li>This is another fully connected linear layer, but now the input size is hidden_dim and the output size is hidden_dim. This implies this is an intermediate layer.<br /></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.ReLU()</code><br />
    <ul>
      <li>Same as before.<br /></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.Linear(hidden_dim, 1)</code><br />
    <ul>
      <li>This is another fully connected linear layer, but now the input size is input_dim and the output size is $1$, which means we want to predict one scalar output per input.<br /></li>
    </ul>
  </li>
</ol>

<p>As we can see, the model is pretty straightforward. I purposely made the <code class="language-plaintext highlighter-rouge">hidden_dim</code> and <code class="language-plaintext highlighter-rouge">dropout</code> variables because we will be varying this when tuning hyperparameters.</p>

<p>Training the model over $15$ epochs gives us the following:</p>

<p><img src="/assets/images/build_mlp/base_model/Huber_vs_epochs.png" alt="huber_base" /></p>

<p>This plot shows how the Huber loss function varies over epochs in the training and validation phase. I used the Huber loss function as a metric because it is less sensitive to outliers when compared to the Mean Square Error. It is quadratic at small distances and linear for larger values.</p>

<p>The overall structure of the plot mostly looks good. Both the training and validation decrease as the model is trained. The only issue is with the higher epochs, where the curves end up crossing. This indicates that the model is most likely overfitting. You don’t want the validation metric to start cross the training metric since this implies that the model is no longer generalizing.</p>

<p>We can in fact check where the model started overfitting by looking in the “metrics_output.log” file generated at each run. This is the latter part of the file:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Epoch</span> <span class="mi">11</span><span class="p">:</span>
<span class="n">New</span> <span class="n">best</span> <span class="n">model</span> <span class="n">at</span> <span class="n">epoch</span> <span class="mi">11</span> <span class="p">(</span><span class="n">val_loss</span><span class="o">=</span><span class="mf">0.2057</span><span class="p">)</span>
<span class="n">Train</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2138</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6054</span>
<span class="n">Validation</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2057</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6258</span>

<span class="n">Epoch</span> <span class="mi">12</span><span class="p">:</span>
<span class="n">New</span> <span class="n">best</span> <span class="n">model</span> <span class="n">at</span> <span class="n">epoch</span> <span class="mi">12</span> <span class="p">(</span><span class="n">val_loss</span><span class="o">=</span><span class="mf">0.2023</span><span class="p">)</span>
<span class="n">Train</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2071</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6200</span>
<span class="n">Validation</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2023</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6355</span>

<span class="n">Epoch</span> <span class="mi">13</span><span class="p">:</span>
<span class="n">Train</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2019</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6317</span>
<span class="n">Validation</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2024</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6378</span>

<span class="n">Epoch</span> <span class="mi">14</span><span class="p">:</span>
<span class="n">New</span> <span class="n">best</span> <span class="n">model</span> <span class="n">at</span> <span class="n">epoch</span> <span class="mi">14</span> <span class="p">(</span><span class="n">val_loss</span><span class="o">=</span><span class="mf">0.2007</span><span class="p">)</span>
<span class="n">Train</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.1977</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6416</span>
<span class="n">Validation</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2007</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6443</span>

<span class="n">Epoch</span> <span class="mi">15</span><span class="p">:</span>
<span class="n">Train</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.1939</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6510</span>
<span class="n">Validation</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2013</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6450</span>

<span class="n">Test</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2135</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6071</span>
</code></pre></div></div>

<p>Looking in this file, we see assume that the model started overfitting around epoch $13$ since this was the first instance of no new best model recorded. The solution would be to train the model for $12$ epochs instead of $15$. Essentially, the model will see the data less so it will not be able to overfit on the features. You can also include something like early stopping so that the training stops when the loss is no longer changing above some tolerance. Another technique is to use a scheduler to change the learning rate (or other hyperparameters) at scheduled intervals. This will help improve how your model generalizes.</p>

<p><img src="/assets/images/build_mlp/base_model/R2_vs_epochs.png" alt="R2_base" /></p>

<p>This next plot shows the $R^2$ score, which measures how well a regression model describes the variability of the target variable. In other words, it tells you how much of the variation in the target variable is explained by the inputs. This quantity is useful because it tells you how much of the variance in the predicted value is explained by the features or is just random noise. This value is bounded between $[-1, 1]$. Generally, a $R^2$ score closer to $1$ implies a better-fitting model. On the other hand, a value close to $0$ means that the model is not learning any of the patterns in the dataset, but can still output the average. On the opposite end of the spectrum, a score closer to $-1$ indicates a model that is doing worse than predicting the average. In fact, it is learning all the wrong things. In this case, the $R^2$ score appears to be close to $1$ so we can infer that the model is making well-informed predictions.</p>

<p>The results plots above are obtained by running the “base” model. The parameters used here are:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
<span class="sh">"</span><span class="s">model_version</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">baseline</span><span class="sh">"</span><span class="p">,</span>
<span class="sh">"</span><span class="s">epochs</span><span class="sh">"</span><span class="p">:</span> <span class="mi">15</span><span class="p">,</span>
<span class="sh">"</span><span class="s">learning_rate</span><span class="sh">"</span><span class="p">:</span> <span class="mf">0.0001</span><span class="p">,</span>
<span class="sh">"</span><span class="s">hidden_dim</span><span class="sh">"</span><span class="p">:</span> <span class="mi">64</span><span class="p">,</span>
<span class="sh">"</span><span class="s">dropout</span><span class="sh">"</span><span class="p">:</span> <span class="mf">0.0</span>
<span class="p">}</span>
</code></pre></div></div>

<p>and can be found in <code class="language-plaintext highlighter-rouge">.../experiments/base_model/params.json</code>.</p>

<p>We can now check the logs generated when the code is ran once. In this case, the code generates a file called “grad_norm_output.log” located in <code class="language-plaintext highlighter-rouge">.../experiments/base_model/</code>. This file has information of what the gradient norm was across different layers at each epoch. For example:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Epoch</span><span class="p">:</span> <span class="mi">11</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">0.</span><span class="n">weight</span><span class="p">:</span> <span class="mf">0.2753058969974518</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">11</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">0.</span><span class="n">bias</span><span class="p">:</span> <span class="mf">0.0689358115196228</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">11</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">3.</span><span class="n">weight</span><span class="p">:</span> <span class="mf">0.27505698800086975</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">11</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">3.</span><span class="n">bias</span><span class="p">:</span> <span class="mf">0.035825666040182114</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">11</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">5.</span><span class="n">weight</span><span class="p">:</span> <span class="mf">0.33901259303092957</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">11</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">5.</span><span class="n">bias</span><span class="p">:</span> <span class="mf">0.024257883429527283</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">11</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">total</span><span class="p">:</span> <span class="mf">0.5224971771240234</span>

<span class="n">Epoch</span><span class="p">:</span> <span class="mi">12</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">0.</span><span class="n">weight</span><span class="p">:</span> <span class="mf">0.2417425960302353</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">12</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">0.</span><span class="n">bias</span><span class="p">:</span> <span class="mf">0.0713915303349495</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">12</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">3.</span><span class="n">weight</span><span class="p">:</span> <span class="mf">0.22586220502853394</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">12</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">3.</span><span class="n">bias</span><span class="p">:</span> <span class="mf">0.037096235901117325</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">12</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">5.</span><span class="n">weight</span><span class="p">:</span> <span class="mf">0.281486839056015</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">12</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">net</span><span class="p">.</span><span class="mf">5.</span><span class="n">bias</span><span class="p">:</span> <span class="mf">0.041641950607299805</span>
<span class="n">Epoch</span><span class="p">:</span> <span class="mi">12</span><span class="p">,</span> <span class="n">grad_norm</span><span class="o">/</span><span class="n">total</span><span class="p">:</span> <span class="mf">0.4437284767627716</span>
</code></pre></div></div>

<p>Gradient norm checking is used to verify that the gradients calculated during backpropagation make sense. If the gradients are too small, the model is either not efficiently learning or not learning at all. If they are too big, you have the exploding gradients problem. In general, the gradients should be between $10^{-2}-10$. Anything below or above this threshold indicates issues with the model. In the example above, we can see that the values look to be in the range mentioned, so our model should be okay.</p>

<p>This does not always tell you if there are issues with the model. The gradient norms can look fine, but the model may not be working correctly. But if the gradient norms do not look fine, you know there <em>is</em> something wrong.</p>

<p>Another thing that you will see in the folder <code class="language-plaintext highlighter-rouge">.../experiments/base_model/</code> is a checkpoints folder. This folder contains the last five epochs worth of models, as well as the model with the best validation metrics. This is useful when trying to resume training the model after initial trainings have already been done. In general, this is good practice to develop since you don’t have to retrain large models if you get more data or if you want to experiment with the model at a later time.</p>

<p>Now we can move on to hyperparameter tuning. In this case, we will be using a grid search approach where we define a set of hyperparameters and values that we want to vary, create a mesh over all the values, and use this mesh to train different models with these hyperparameters. While efficient, this method tends to be slow. This is fine in our case since we will only be tuning a handful of hyperparameters over a limited set of values. In this case, we will be forming a grid for the learning rate, hidden dimension, and dropout rate using the following:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">vary_lr</span> <span class="o">=</span> <span class="p">[</span><span class="mf">0.01</span><span class="p">,</span> <span class="mf">0.001</span><span class="p">,</span> <span class="mf">0.0001</span><span class="p">]</span>
<span class="n">vary_hidden_dim</span> <span class="o">=</span> <span class="p">[</span><span class="mi">32</span><span class="p">,</span> <span class="mi">64</span><span class="p">,</span> <span class="mi">128</span><span class="p">]</span>
<span class="n">vary_dropout</span> <span class="o">=</span> <span class="p">[</span><span class="mf">0.0</span><span class="p">,</span> <span class="mf">0.3</span><span class="p">,</span> <span class="mf">0.6</span><span class="p">]</span>

<span class="n">prms_product</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="nf">product</span><span class="p">(</span><span class="n">vary_lr</span><span class="p">,</span> <span class="n">vary_hidden_dim</span><span class="p">,</span> <span class="n">vary_dropout</span><span class="p">))</span>
</code></pre></div></div>

<p>The list <code class="language-plaintext highlighter-rouge">prms_product</code> contains combinations of these hyperparameters. Like before, the models of the last five epochs are being saved, as well as the model that performed the best on the validation set. We also log the metrics and the gradient norms over the different epochs. We will now see the results of a subset of different models trained with these hyerparameters.</p>

<p>First example:<br />
params #2: (0.01, 32, 0.3)</p>

<p><img src="/assets/images/build_mlp/tests/test2/lr0.01_hdim32_do0.3/Huber_vs_epochs.png" alt="huber_test2" /></p>

<p><img src="/assets/images/build_mlp/tests/test2/lr0.01_hdim32_do0.3/R2_vs_epochs.png" alt="R2_test2" /></p>

<p>Just looking at the plots we can tell that these hyperparameters are <em>not</em> a good choice. While the training loss looks decent, the validation loss does not decrease as we increase the number of epochs. This implies that the model is not learning the dataset.</p>

<p>Second example:<br />
params #12: (0.001, 32, 0.6)</p>

<p><img src="/assets/images/build_mlp/tests/test12/lr0.001_hdim32_do0.6/Huber_vs_epochs.png" alt="huber_test12" /></p>

<p><img src="/assets/images/build_mlp/tests/test12/lr0.001_hdim32_do0.6/R2_vs_epochs.png" alt="R2_test12" /></p>

<p>While the loss may look good, the validation loss also crossed over the training loss. While we can just stop training early, the loss curves do not decay as nicely as the base model.</p>

<p>Third example:<br />
params #20: (0.0001, 32, 0.3)</p>

<p><img src="/assets/images/build_mlp/tests/test20/lr0.0001_hdim32_do0.3/Huber_vs_epochs.png" alt="huber_test20" /></p>

<p><img src="/assets/images/build_mlp/tests/test20/lr0.0001_hdim32_do0.3/R2_vs_epochs.png" alt="R2_test20" /></p>

<p>These loss curves look better than the previous two and there is no crossing of the loss. The model seems to be slowly learning over time since there is not a sharp drop in the loss. The $R^2$ score also increases and levels out nicely. These hyperparameters are a good choice. Let’s take a closer look at the metrics outputs:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Epoch</span> <span class="mi">13</span><span class="p">:</span>
<span class="n">New</span> <span class="n">best</span> <span class="n">model</span> <span class="n">at</span> <span class="n">epoch</span> <span class="mi">13</span> <span class="p">(</span><span class="n">val_loss</span><span class="o">=</span><span class="mf">0.2799</span><span class="p">)</span>
<span class="n">Train</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.3321</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.2695</span>
<span class="n">Validation</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2799</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.4258</span>

<span class="n">Epoch</span> <span class="mi">14</span><span class="p">:</span>
<span class="n">New</span> <span class="n">best</span> <span class="n">model</span> <span class="n">at</span> <span class="n">epoch</span> <span class="mi">14</span> <span class="p">(</span><span class="n">val_loss</span><span class="o">=</span><span class="mf">0.2703</span><span class="p">)</span>
<span class="n">Train</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.3234</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.2608</span>
<span class="n">Validation</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2703</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.4506</span>

<span class="n">Epoch</span> <span class="mi">15</span><span class="p">:</span>
<span class="n">New</span> <span class="n">best</span> <span class="n">model</span> <span class="n">at</span> <span class="n">epoch</span> <span class="mi">15</span> <span class="p">(</span><span class="n">val_loss</span><span class="o">=</span><span class="mf">0.2622</span><span class="p">)</span>
<span class="n">Train</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.3156</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.3327</span>
<span class="n">Validation</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2622</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.4720</span>

<span class="n">Test</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.3395</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="o">-</span><span class="mf">10.3754</span>
</code></pre></div></div>

<p>The metrics outputs seem fine, but the $R^2$ score on the test dataset is $-10.375$. Not only is it negative, but it is beyond the range we mentioned earlier in the post. If they do not perform well on the test dataset, they may not be a good choice.</p>

<p>Fourth example:<br />
params #26: (0.0001, 128, 0.3)</p>

<p><img src="/assets/images/build_mlp/tests/test26/lr0.0001_hdim128_do0.3/Huber_vs_epochs.png" alt="huber_test20" /></p>

<p><img src="/assets/images/build_mlp/tests/test26/lr0.0001_hdim128_do0.3/R2_vs_epochs.png" alt="R2_test20" /></p>

<p>These loss curves also look really nice. It seems that the model learns quickly at first with these hyperparamters, then does not change as much with increasing epochs. The $R^2$ score also increases and levels out similar to the third example. These hyperparameters are also a good choice. Let’s take a closer look at the metrics outputs:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Epoch</span> <span class="mi">13</span><span class="p">:</span>
<span class="n">New</span> <span class="n">best</span> <span class="n">model</span> <span class="n">at</span> <span class="n">epoch</span> <span class="mi">13</span> <span class="p">(</span><span class="n">val_loss</span><span class="o">=</span><span class="mf">0.1993</span><span class="p">)</span>
<span class="n">Train</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2310</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.5754</span>
<span class="n">Validation</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.1993</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6467</span>

<span class="n">Epoch</span> <span class="mi">14</span><span class="p">:</span>
<span class="n">Train</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2255</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.5897</span>
<span class="n">Validation</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2010</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6438</span>

<span class="n">Epoch</span> <span class="mi">15</span><span class="p">:</span>
<span class="n">Train</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2251</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.5873</span>
<span class="n">Validation</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.1999</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.6470</span>

<span class="n">Test</span> <span class="o">-</span> <span class="n">Huber</span><span class="p">:</span> <span class="mf">0.2179</span><span class="p">,</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span><span class="p">:</span> <span class="mf">0.3668</span>
</code></pre></div></div>

<p>The model seems to be performing well with the test set so these hyperparameters seem to be a good choice.</p>

<p>At first glance, the last two examples appear to be good choices and may be better than our initial base model. If we then check how well the model performs in the test phase, we see that the third example does not perform well on the new data. On the other hand, the fourth example outputs a decent $R^2$ score and loss, so we would choose this set of hyperparameters.</p>

<p>We also display a table with all the results:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">|</span>         <span class="n">Model</span>          <span class="o">|</span> <span class="n">Validation</span> <span class="n">loss</span> <span class="o">|</span> <span class="n">Test</span> <span class="n">loss</span> <span class="o">|</span> <span class="n">Test</span> <span class="n">R2</span><span class="o">-</span><span class="n">score</span> <span class="o">|</span>
<span class="o">|</span> <span class="o">----------------------</span> <span class="o">|</span> <span class="o">---------------</span> <span class="o">|</span> <span class="o">---------</span> <span class="o">|</span> <span class="o">-------------</span> <span class="o">|</span>
<span class="o">|</span>       <span class="n">base_model</span>       <span class="o">|</span>    <span class="mf">0.200688</span>     <span class="o">|</span> <span class="mf">0.213490</span>  <span class="o">|</span>   <span class="mf">0.607139</span>    <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">001</span><span class="n">_hdim128_do0</span><span class="p">.</span><span class="mi">0</span>  <span class="o">|</span>    <span class="mf">0.216196</span>     <span class="o">|</span> <span class="mf">0.610909</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.554772</span>   <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">001</span><span class="n">_hdim32_do0</span><span class="p">.</span><span class="mi">3</span>  <span class="o">|</span>    <span class="mf">0.205734</span>     <span class="o">|</span> <span class="mf">0.313990</span>  <span class="o">|</span>   <span class="mf">0.435274</span>    <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">0001</span><span class="n">_hdim128_do0</span><span class="p">.</span><span class="mi">6</span> <span class="o">|</span>    <span class="mf">0.215220</span>     <span class="o">|</span> <span class="mf">0.243275</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.366992</span>   <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">001</span><span class="n">_hdim128_do0</span><span class="p">.</span><span class="mi">6</span>  <span class="o">|</span>    <span class="mf">0.199085</span>     <span class="o">|</span> <span class="mf">0.375953</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.558818</span>   <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">0001</span><span class="n">_hdim32_do0</span><span class="p">.</span><span class="mi">3</span>  <span class="o">|</span>    <span class="mf">0.262168</span>     <span class="o">|</span> <span class="mf">0.339538</span>  <span class="o">|</span>  <span class="o">-</span><span class="mf">10.375393</span>   <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">0001</span><span class="n">_hdim32_do0</span><span class="p">.</span><span class="mi">6</span>  <span class="o">|</span>    <span class="mf">0.294543</span>     <span class="o">|</span> <span class="mf">0.387825</span>  <span class="o">|</span>  <span class="o">-</span><span class="mf">14.893430</span>   <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">0001</span><span class="n">_hdim128_do0</span><span class="p">.</span><span class="mi">3</span> <span class="o">|</span>    <span class="mf">0.199262</span>     <span class="o">|</span> <span class="mf">0.217917</span>  <span class="o">|</span>   <span class="mf">0.366839</span>    <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">0001</span><span class="n">_hdim32_do0</span><span class="p">.</span><span class="mi">0</span>  <span class="o">|</span>    <span class="mf">0.236734</span>     <span class="o">|</span> <span class="mf">0.313647</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">9.647883</span>   <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">001</span><span class="n">_hdim32_do0</span><span class="p">.</span><span class="mi">0</span>  <span class="o">|</span>    <span class="mf">0.226325</span>     <span class="o">|</span> <span class="mf">0.479087</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.094879</span>   <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">001</span><span class="n">_hdim128_do0</span><span class="p">.</span><span class="mi">3</span>  <span class="o">|</span>    <span class="mf">0.211276</span>     <span class="o">|</span> <span class="mf">0.545321</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.268541</span>   <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">01</span><span class="n">_hdim128_do0</span><span class="p">.</span><span class="mi">6</span>  <span class="o">|</span>    <span class="mf">0.238816</span>     <span class="o">|</span> <span class="mf">0.413160</span>  <span class="o">|</span>   <span class="mf">0.192252</span>    <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">01</span><span class="n">_hdim128_do0</span><span class="p">.</span><span class="mi">0</span>  <span class="o">|</span>    <span class="mf">0.340562</span>     <span class="o">|</span> <span class="mf">0.796216</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">1.484344</span>   <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">01</span><span class="n">_hdim64_do0</span><span class="p">.</span><span class="mi">6</span>   <span class="o">|</span>    <span class="mf">0.263552</span>     <span class="o">|</span> <span class="mf">0.396611</span>  <span class="o">|</span>   <span class="mf">0.212692</span>    <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">01</span><span class="n">_hdim32_do0</span><span class="p">.</span><span class="mi">0</span>   <span class="o">|</span>    <span class="mf">0.371635</span>     <span class="o">|</span> <span class="mf">0.866089</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">1.596811</span>   <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">01</span><span class="n">_hdim128_do0</span><span class="p">.</span><span class="mi">3</span>  <span class="o">|</span>    <span class="mf">0.299062</span>     <span class="o">|</span> <span class="mf">0.650552</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.698870</span>   <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">001</span><span class="n">_hdim32_do0</span><span class="p">.</span><span class="mi">6</span>  <span class="o">|</span>    <span class="mf">0.199160</span>     <span class="o">|</span> <span class="mf">0.269113</span>  <span class="o">|</span>   <span class="mf">0.536666</span>    <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">001</span><span class="n">_hdim64_do0</span><span class="p">.</span><span class="mi">6</span>  <span class="o">|</span>    <span class="mf">0.196005</span>     <span class="o">|</span> <span class="mf">0.351168</span>  <span class="o">|</span>   <span class="mf">0.295199</span>    <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">0001</span><span class="n">_hdim64_do0</span><span class="p">.</span><span class="mi">3</span>  <span class="o">|</span>    <span class="mf">0.227803</span>     <span class="o">|</span> <span class="mf">0.253033</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.336243</span>   <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">0001</span><span class="n">_hdim64_do0</span><span class="p">.</span><span class="mi">6</span>  <span class="o">|</span>    <span class="mf">0.246033</span>     <span class="o">|</span> <span class="mf">0.315547</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">8.151037</span>   <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">0001</span><span class="n">_hdim128_do0</span><span class="p">.</span><span class="mi">0</span> <span class="o">|</span>    <span class="mf">0.184923</span>     <span class="o">|</span> <span class="mf">0.203477</span>  <span class="o">|</span>   <span class="mf">0.591489</span>    <span class="o">|</span>
<span class="o">|</span> <span class="n">lr0</span><span class="p">.</span><span class="mi">0001</span><span class="n">_hdim64_do0</span><span class="p">.</span><span class="mi">0</span>  <span class="o">|</span>    <span class="mf">0.210855</span>     <span class="o">|</span> <span class="mf">0.221721</span>  <span class="o">|</span>   <span class="mf">0.612034</span>    <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">001</span><span class="n">_hdim64_do0</span><span class="p">.</span><span class="mi">3</span>  <span class="o">|</span>    <span class="mf">0.205427</span>     <span class="o">|</span> <span class="mf">0.478154</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.110683</span>   <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">001</span><span class="n">_hdim64_do0</span><span class="p">.</span><span class="mi">0</span>  <span class="o">|</span>    <span class="mf">0.194936</span>     <span class="o">|</span> <span class="mf">0.580364</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.837558</span>   <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">01</span><span class="n">_hdim64_do0</span><span class="p">.</span><span class="mi">0</span>   <span class="o">|</span>    <span class="mf">0.295101</span>     <span class="o">|</span> <span class="mf">0.704713</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.920688</span>   <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">01</span><span class="n">_hdim32_do0</span><span class="p">.</span><span class="mi">6</span>   <span class="o">|</span>    <span class="mf">0.232968</span>     <span class="o">|</span> <span class="mf">0.288357</span>  <span class="o">|</span>   <span class="mf">0.498615</span>    <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">01</span><span class="n">_hdim32_do0</span><span class="p">.</span><span class="mi">3</span>   <span class="o">|</span>    <span class="mf">0.286834</span>     <span class="o">|</span> <span class="mf">0.489060</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.104738</span>   <span class="o">|</span>
<span class="o">|</span>  <span class="n">lr0</span><span class="p">.</span><span class="mi">01</span><span class="n">_hdim64_do0</span><span class="p">.</span><span class="mi">3</span>   <span class="o">|</span>    <span class="mf">0.240279</span>     <span class="o">|</span> <span class="mf">0.565656</span>  <span class="o">|</span>   <span class="o">-</span><span class="mf">0.386141</span>   <span class="o">|</span>
</code></pre></div></div>

<p>This table shows a summary of the results of the hyperparameter tuning. The table contains the model name, the validation loss, test loss, and test $R^2$ score for the base model and all the combinations of hyperparameters.</p>

<p>Overall, we can use hyperparameter tuning to figure out what is the best combination of values we can use to get a well-performing model. In this post I showed the basics, but there are other hyperparameters that can be tuned, such as batch size, number of hidden layers, and optimizer momentum. In addition, one can also use techniques such as random search when working with large number of values or hyperparameter, or Bayesian optimization when evaluating the model is computationally time-consuming. When there are issues with the model, we can use gradient norm checking to get a better understanding of the behavior of the gradients across all layers and epochs. This may not always be useful, but its another tool one can use to check performance.</p>

<p>The code presented and plots displayed in this and the previous post can be found on my Github under the <a href="https://github.com/AJG91/build-mlp" title="build-mlp-repo">build-mlp</a> repo <a class="citation" href="#Garcia2025BuildMLP">[1]</a>. Feel free to reach out if you have any questions about what we covered this week. I will take a break the next few weeks and come back with natural language processing examples. Stay tuned!</p>

<ol class="bibliography"><li><span id="Garcia2025BuildMLP">[1] Alberto J. Garcia, <i>Build MLP</i>, 2025. Available at: https://github.com/AJG91/build-mlp.</span></li></ol>
<p><a href="/" class="btn">⬅ Back to Home</a></p>]]></content><author><name></name></author><category term="machine-learning" /><summary type="html"><![CDATA[October 12, 2025 This week we will learn about how to build a multi-layer perceptron (MLP), as well as hyperparameter tuning and debugging the model. Some information may be duplicate from previous posts, but I will go over it again to keep the posts on MLPs self-contained. What is a multi-layer perceptron (MLP)? A MLP is a artificial feedforward neural network that consists on multiple layers including an input layer, hidden layers (at least one), and an output layer. The layers are fully connected, which means that each neuron in layer a is connected to each neuron in layer b. Each connection is assigned a weight and each neuron a bias. There are two phases when training a MLP: forward propagation and backward propagation. During forward propagation, the data flows from the input layer, through all the intermediate layers, and finally through the output layer with each layer processing the outputs of the previous one. The output is then compared to the truth and the error is calculated. During backpropagation, the error flows backwards through the layers, adjusting the weights and biases using techniques such as gradient descent, which tries to find the combinations of weights and bias that minimize the error. Once this process finishes for all weights and biases, the model will once again switch to forward propagation. We will be using the California housing dataset we discussed last week to train and evaluate our model. This dataset contains aggregated data regarding each district in California such as median income, population, and location (latitude and longitude). For more information see this post. Since we already went over the dataset in the last post, we can jump straight into the model and experiments. Our model class contains the following constructor (minus the docstring): class MLP(nn.Module): def __init__(self, input_dim, hidden_dim, dropout): super().__init__() self.net = nn.Sequential( nn.Linear(input_dim, hidden_dim), nn.ReLU(), nn.Dropout(dropout), nn.Linear(hidden_dim, hidden_dim), nn.ReLU(), nn.Linear(hidden_dim, 1), ) Let’s break it down: nn.Sequential This simplies building a neural network by organizing the layers in a sequential order. In other words, the network knows that it has to flow sequentially through the layers in the order defined. nn.Linear(input_dim, hidden_dim) This is a fully connected linear layer with input size input_dim (output of CNN) and output size of hidden_dim neurons. nn.ReLU() This is a Rectified Linear Unit activation function. It is defined as $\text{ReLU}(x) = \text{max}(0, x)$. Essentially, it zeros out the negative values while introducing non-linearity. This type of activation function is simple and fast to use compared to others such as $\text{tanh}(x)$ or sigmoid. nn.Dropout() This randomly turns off neurons (equivalent to zeroing out some inputs), forcing the network to learn more general features instead of relying solely on specific neurons. nn.Linear(hidden_dim, hidden_dim) This is another fully connected linear layer, but now the input size is hidden_dim and the output size is hidden_dim. This implies this is an intermediate layer. nn.ReLU() Same as before. nn.Linear(hidden_dim, 1) This is another fully connected linear layer, but now the input size is input_dim and the output size is $1$, which means we want to predict one scalar output per input. As we can see, the model is pretty straightforward. I purposely made the hidden_dim and dropout variables because we will be varying this when tuning hyperparameters. Training the model over $15$ epochs gives us the following: This plot shows how the Huber loss function varies over epochs in the training and validation phase. I used the Huber loss function as a metric because it is less sensitive to outliers when compared to the Mean Square Error. It is quadratic at small distances and linear for larger values. The overall structure of the plot mostly looks good. Both the training and validation decrease as the model is trained. The only issue is with the higher epochs, where the curves end up crossing. This indicates that the model is most likely overfitting. You don’t want the validation metric to start cross the training metric since this implies that the model is no longer generalizing. We can in fact check where the model started overfitting by looking in the “metrics_output.log” file generated at each run. This is the latter part of the file: Epoch 11: New best model at epoch 11 (val_loss=0.2057) Train - Huber: 0.2138, R2-score: 0.6054 Validation - Huber: 0.2057, R2-score: 0.6258 Epoch 12: New best model at epoch 12 (val_loss=0.2023) Train - Huber: 0.2071, R2-score: 0.6200 Validation - Huber: 0.2023, R2-score: 0.6355 Epoch 13: Train - Huber: 0.2019, R2-score: 0.6317 Validation - Huber: 0.2024, R2-score: 0.6378 Epoch 14: New best model at epoch 14 (val_loss=0.2007) Train - Huber: 0.1977, R2-score: 0.6416 Validation - Huber: 0.2007, R2-score: 0.6443 Epoch 15: Train - Huber: 0.1939, R2-score: 0.6510 Validation - Huber: 0.2013, R2-score: 0.6450 Test - Huber: 0.2135, R2-score: 0.6071 Looking in this file, we see assume that the model started overfitting around epoch $13$ since this was the first instance of no new best model recorded. The solution would be to train the model for $12$ epochs instead of $15$. Essentially, the model will see the data less so it will not be able to overfit on the features. You can also include something like early stopping so that the training stops when the loss is no longer changing above some tolerance. Another technique is to use a scheduler to change the learning rate (or other hyperparameters) at scheduled intervals. This will help improve how your model generalizes. This next plot shows the $R^2$ score, which measures how well a regression model describes the variability of the target variable. In other words, it tells you how much of the variation in the target variable is explained by the inputs. This quantity is useful because it tells you how much of the variance in the predicted value is explained by the features or is just random noise. This value is bounded between $[-1, 1]$. Generally, a $R^2$ score closer to $1$ implies a better-fitting model. On the other hand, a value close to $0$ means that the model is not learning any of the patterns in the dataset, but can still output the average. On the opposite end of the spectrum, a score closer to $-1$ indicates a model that is doing worse than predicting the average. In fact, it is learning all the wrong things. In this case, the $R^2$ score appears to be close to $1$ so we can infer that the model is making well-informed predictions. The results plots above are obtained by running the “base” model. The parameters used here are: { "model_version": "baseline", "epochs": 15, "learning_rate": 0.0001, "hidden_dim": 64, "dropout": 0.0 } and can be found in .../experiments/base_model/params.json. We can now check the logs generated when the code is ran once. In this case, the code generates a file called “grad_norm_output.log” located in .../experiments/base_model/. This file has information of what the gradient norm was across different layers at each epoch. For example: Epoch: 11, grad_norm/net.0.weight: 0.2753058969974518 Epoch: 11, grad_norm/net.0.bias: 0.0689358115196228 Epoch: 11, grad_norm/net.3.weight: 0.27505698800086975 Epoch: 11, grad_norm/net.3.bias: 0.035825666040182114 Epoch: 11, grad_norm/net.5.weight: 0.33901259303092957 Epoch: 11, grad_norm/net.5.bias: 0.024257883429527283 Epoch: 11, grad_norm/total: 0.5224971771240234 Epoch: 12, grad_norm/net.0.weight: 0.2417425960302353 Epoch: 12, grad_norm/net.0.bias: 0.0713915303349495 Epoch: 12, grad_norm/net.3.weight: 0.22586220502853394 Epoch: 12, grad_norm/net.3.bias: 0.037096235901117325 Epoch: 12, grad_norm/net.5.weight: 0.281486839056015 Epoch: 12, grad_norm/net.5.bias: 0.041641950607299805 Epoch: 12, grad_norm/total: 0.4437284767627716 Gradient norm checking is used to verify that the gradients calculated during backpropagation make sense. If the gradients are too small, the model is either not efficiently learning or not learning at all. If they are too big, you have the exploding gradients problem. In general, the gradients should be between $10^{-2}-10$. Anything below or above this threshold indicates issues with the model. In the example above, we can see that the values look to be in the range mentioned, so our model should be okay. This does not always tell you if there are issues with the model. The gradient norms can look fine, but the model may not be working correctly. But if the gradient norms do not look fine, you know there is something wrong. Another thing that you will see in the folder .../experiments/base_model/ is a checkpoints folder. This folder contains the last five epochs worth of models, as well as the model with the best validation metrics. This is useful when trying to resume training the model after initial trainings have already been done. In general, this is good practice to develop since you don’t have to retrain large models if you get more data or if you want to experiment with the model at a later time. Now we can move on to hyperparameter tuning. In this case, we will be using a grid search approach where we define a set of hyperparameters and values that we want to vary, create a mesh over all the values, and use this mesh to train different models with these hyperparameters. While efficient, this method tends to be slow. This is fine in our case since we will only be tuning a handful of hyperparameters over a limited set of values. In this case, we will be forming a grid for the learning rate, hidden dimension, and dropout rate using the following: vary_lr = [0.01, 0.001, 0.0001] vary_hidden_dim = [32, 64, 128] vary_dropout = [0.0, 0.3, 0.6] prms_product = list(product(vary_lr, vary_hidden_dim, vary_dropout)) The list prms_product contains combinations of these hyperparameters. Like before, the models of the last five epochs are being saved, as well as the model that performed the best on the validation set. We also log the metrics and the gradient norms over the different epochs. We will now see the results of a subset of different models trained with these hyerparameters. First example: params #2: (0.01, 32, 0.3) Just looking at the plots we can tell that these hyperparameters are not a good choice. While the training loss looks decent, the validation loss does not decrease as we increase the number of epochs. This implies that the model is not learning the dataset. Second example: params #12: (0.001, 32, 0.6) While the loss may look good, the validation loss also crossed over the training loss. While we can just stop training early, the loss curves do not decay as nicely as the base model. Third example: params #20: (0.0001, 32, 0.3) These loss curves look better than the previous two and there is no crossing of the loss. The model seems to be slowly learning over time since there is not a sharp drop in the loss. The $R^2$ score also increases and levels out nicely. These hyperparameters are a good choice. Let’s take a closer look at the metrics outputs: Epoch 13: New best model at epoch 13 (val_loss=0.2799) Train - Huber: 0.3321, R2-score: 0.2695 Validation - Huber: 0.2799, R2-score: 0.4258 Epoch 14: New best model at epoch 14 (val_loss=0.2703) Train - Huber: 0.3234, R2-score: 0.2608 Validation - Huber: 0.2703, R2-score: 0.4506 Epoch 15: New best model at epoch 15 (val_loss=0.2622) Train - Huber: 0.3156, R2-score: 0.3327 Validation - Huber: 0.2622, R2-score: 0.4720 Test - Huber: 0.3395, R2-score: -10.3754 The metrics outputs seem fine, but the $R^2$ score on the test dataset is $-10.375$. Not only is it negative, but it is beyond the range we mentioned earlier in the post. If they do not perform well on the test dataset, they may not be a good choice. Fourth example: params #26: (0.0001, 128, 0.3) These loss curves also look really nice. It seems that the model learns quickly at first with these hyperparamters, then does not change as much with increasing epochs. The $R^2$ score also increases and levels out similar to the third example. These hyperparameters are also a good choice. Let’s take a closer look at the metrics outputs: Epoch 13: New best model at epoch 13 (val_loss=0.1993) Train - Huber: 0.2310, R2-score: 0.5754 Validation - Huber: 0.1993, R2-score: 0.6467 Epoch 14: Train - Huber: 0.2255, R2-score: 0.5897 Validation - Huber: 0.2010, R2-score: 0.6438 Epoch 15: Train - Huber: 0.2251, R2-score: 0.5873 Validation - Huber: 0.1999, R2-score: 0.6470 Test - Huber: 0.2179, R2-score: 0.3668 The model seems to be performing well with the test set so these hyperparameters seem to be a good choice. At first glance, the last two examples appear to be good choices and may be better than our initial base model. If we then check how well the model performs in the test phase, we see that the third example does not perform well on the new data. On the other hand, the fourth example outputs a decent $R^2$ score and loss, so we would choose this set of hyperparameters. We also display a table with all the results: | Model | Validation loss | Test loss | Test R2-score | | ---------------------- | --------------- | --------- | ------------- | | base_model | 0.200688 | 0.213490 | 0.607139 | | lr0.001_hdim128_do0.0 | 0.216196 | 0.610909 | -0.554772 | | lr0.001_hdim32_do0.3 | 0.205734 | 0.313990 | 0.435274 | | lr0.0001_hdim128_do0.6 | 0.215220 | 0.243275 | -0.366992 | | lr0.001_hdim128_do0.6 | 0.199085 | 0.375953 | -0.558818 | | lr0.0001_hdim32_do0.3 | 0.262168 | 0.339538 | -10.375393 | | lr0.0001_hdim32_do0.6 | 0.294543 | 0.387825 | -14.893430 | | lr0.0001_hdim128_do0.3 | 0.199262 | 0.217917 | 0.366839 | | lr0.0001_hdim32_do0.0 | 0.236734 | 0.313647 | -9.647883 | | lr0.001_hdim32_do0.0 | 0.226325 | 0.479087 | -0.094879 | | lr0.001_hdim128_do0.3 | 0.211276 | 0.545321 | -0.268541 | | lr0.01_hdim128_do0.6 | 0.238816 | 0.413160 | 0.192252 | | lr0.01_hdim128_do0.0 | 0.340562 | 0.796216 | -1.484344 | | lr0.01_hdim64_do0.6 | 0.263552 | 0.396611 | 0.212692 | | lr0.01_hdim32_do0.0 | 0.371635 | 0.866089 | -1.596811 | | lr0.01_hdim128_do0.3 | 0.299062 | 0.650552 | -0.698870 | | lr0.001_hdim32_do0.6 | 0.199160 | 0.269113 | 0.536666 | | lr0.001_hdim64_do0.6 | 0.196005 | 0.351168 | 0.295199 | | lr0.0001_hdim64_do0.3 | 0.227803 | 0.253033 | -0.336243 | | lr0.0001_hdim64_do0.6 | 0.246033 | 0.315547 | -8.151037 | | lr0.0001_hdim128_do0.0 | 0.184923 | 0.203477 | 0.591489 | | lr0.0001_hdim64_do0.0 | 0.210855 | 0.221721 | 0.612034 | | lr0.001_hdim64_do0.3 | 0.205427 | 0.478154 | -0.110683 | | lr0.001_hdim64_do0.0 | 0.194936 | 0.580364 | -0.837558 | | lr0.01_hdim64_do0.0 | 0.295101 | 0.704713 | -0.920688 | | lr0.01_hdim32_do0.6 | 0.232968 | 0.288357 | 0.498615 | | lr0.01_hdim32_do0.3 | 0.286834 | 0.489060 | -0.104738 | | lr0.01_hdim64_do0.3 | 0.240279 | 0.565656 | -0.386141 | This table shows a summary of the results of the hyperparameter tuning. The table contains the model name, the validation loss, test loss, and test $R^2$ score for the base model and all the combinations of hyperparameters. Overall, we can use hyperparameter tuning to figure out what is the best combination of values we can use to get a well-performing model. In this post I showed the basics, but there are other hyperparameters that can be tuned, such as batch size, number of hidden layers, and optimizer momentum. In addition, one can also use techniques such as random search when working with large number of values or hyperparameter, or Bayesian optimization when evaluating the model is computationally time-consuming. When there are issues with the model, we can use gradient norm checking to get a better understanding of the behavior of the gradients across all layers and epochs. This may not always be useful, but its another tool one can use to check performance. The code presented and plots displayed in this and the previous post can be found on my Github under the build-mlp repo [1]. Feel free to reach out if you have any questions about what we covered this week. I will take a break the next few weeks and come back with natural language processing examples. Stay tuned! [1] Alberto J. Garcia, Build MLP, 2025. Available at: https://github.com/AJG91/build-mlp. ⬅ Back to Home]]></summary></entry><entry><title type="html">Multi-layer perceptron: California housing dataset</title><link href="https://ajg91.github.io//posts/machine-learning/multi-layer-perceptron-data-description/" rel="alternate" type="text/html" title="Multi-layer perceptron: California housing dataset" /><published>2025-10-05T00:00:00-07:00</published><updated>2025-10-05T00:00:00-07:00</updated><id>https://ajg91.github.io//posts/machine-learning/multi-layer-perceptron-data-description</id><content type="html" xml:base="https://ajg91.github.io//posts/machine-learning/multi-layer-perceptron-data-description/"><![CDATA[<p class="post-date">October 05, 2025</p>

<p>In these next posts we will learn a little bit about hyperparameter tuning and debugging. This week I will discuss the data set we will be using and next week we will start training the model.</p>

<p>When deciding on what model and dataset to use, I realized that I previously jumped straight into convolutional neural networks without first explaining the multi-layer perceptron (MLP). Although it got some coverage in my <a href="https://ajg91.github.io/posts/2025-09-07-convolutional-neural-network-with-mnist-dataset/" title="cnn-blog-post-#2">post</a> on convolutional neural networks, I figured it would be a good idea to go back and touch on it again. I will not be extremely verbose about it since the focus of these posts is on hyperparameter tuning and debugging.</p>

<p>We will be using is the <a href="https://scikit-learn.org/stable/modules/generated/sklearn.datasets.fetch_california_housing.html" title="ca-housing-docs">California housing</a> dataset to train and evaluate our model. This dataset contains aggregated data regarding each district in California such as median income, population, and location (Latitude and longitude). Here is a snippet of what the data looks like:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: right">MedInc</th>
      <th style="text-align: right">HouseAge</th>
      <th style="text-align: right">AveRooms</th>
      <th style="text-align: right">AveBedrms</th>
      <th style="text-align: right">Population</th>
      <th style="text-align: right">AveOccup</th>
      <th style="text-align: right">Latitude</th>
      <th style="text-align: right">Longitude</th>
      <th style="text-align: right">MedHouseVal</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: right">8.3252</td>
      <td style="text-align: right">41</td>
      <td style="text-align: right">6.98413</td>
      <td style="text-align: right">1.02381</td>
      <td style="text-align: right">322</td>
      <td style="text-align: right">2.55556</td>
      <td style="text-align: right">37.88</td>
      <td style="text-align: right">-122.23</td>
      <td style="text-align: right">4.526</td>
    </tr>
    <tr>
      <td style="text-align: right">8.3014</td>
      <td style="text-align: right">21</td>
      <td style="text-align: right">6.23814</td>
      <td style="text-align: right">0.97188</td>
      <td style="text-align: right">2401</td>
      <td style="text-align: right">2.10984</td>
      <td style="text-align: right">37.86</td>
      <td style="text-align: right">-122.22</td>
      <td style="text-align: right">3.585</td>
    </tr>
    <tr>
      <td style="text-align: right">7.2574</td>
      <td style="text-align: right">52</td>
      <td style="text-align: right">8.28814</td>
      <td style="text-align: right">1.07345</td>
      <td style="text-align: right">496</td>
      <td style="text-align: right">2.80226</td>
      <td style="text-align: right">37.85</td>
      <td style="text-align: right">-122.24</td>
      <td style="text-align: right">3.521</td>
    </tr>
    <tr>
      <td style="text-align: right">5.6431</td>
      <td style="text-align: right">52</td>
      <td style="text-align: right">5.81735</td>
      <td style="text-align: right">1.07306</td>
      <td style="text-align: right">558</td>
      <td style="text-align: right">2.54795</td>
      <td style="text-align: right">37.85</td>
      <td style="text-align: right">-122.25</td>
      <td style="text-align: right">3.413</td>
    </tr>
    <tr>
      <td style="text-align: right">3.8462</td>
      <td style="text-align: right">52</td>
      <td style="text-align: right">6.28185</td>
      <td style="text-align: right">1.08108</td>
      <td style="text-align: right">565</td>
      <td style="text-align: right">2.18147</td>
      <td style="text-align: right">37.85</td>
      <td style="text-align: right">-122.25</td>
      <td style="text-align: right">3.422</td>
    </tr>
  </tbody>
</table>

<p>Let’s look at some statistics. As a note, the incomes displayed are all in tens of thousands of dollars (i.e. 1.23 = $12,300).</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>             <span class="n">MedInc</span>     <span class="n">AveBedrms</span>      <span class="n">AveOccup</span>
<span class="n">count</span>  <span class="mf">20640.000000</span>  <span class="mf">20640.000000</span>  <span class="mf">20640.000000</span>
<span class="n">mean</span>       <span class="mf">3.870671</span>      <span class="mf">1.096675</span>      <span class="mf">3.070655</span>
<span class="n">std</span>        <span class="mf">1.899822</span>      <span class="mf">0.473911</span>     <span class="mf">10.386050</span>
<span class="nb">min</span>        <span class="mf">0.499900</span>      <span class="mf">0.333333</span>      <span class="mf">0.692308</span>
<span class="mi">25</span><span class="o">%</span>        <span class="mf">2.563400</span>      <span class="mf">1.006079</span>      <span class="mf">2.429741</span>
<span class="mi">50</span><span class="o">%</span>        <span class="mf">3.534800</span>      <span class="mf">1.048780</span>      <span class="mf">2.818116</span>
<span class="mi">75</span><span class="o">%</span>        <span class="mf">4.743250</span>      <span class="mf">1.099526</span>      <span class="mf">3.282261</span>
<span class="nb">max</span>       <span class="mf">15.000100</span>     <span class="mf">34.066667</span>   <span class="mf">1243.333333</span>
</code></pre></div></div>

<p>From the table above, we see that the average income is $38,700. Additionally, the average number of bedrooms per household is 1 with an average number of occupants being 3. While some values seem reasonable, others don’t make sense. For example, the max of average number of occupants 1243. This seems unlikely and is probably caused by how the consensus data was recorded. For example, we don’t knw how college dorms, apartment complexes, and prisons were accounted for. We can remove these outliers from the dataset so that we can have a better understanding of the average household by using the interquartile range and only keeping the middle 50% of the dataset:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Q1</span> <span class="o">=</span> <span class="n">df_housing</span><span class="p">.</span><span class="nf">quantile</span><span class="p">(</span><span class="mf">0.25</span><span class="p">)</span>
<span class="n">Q3</span> <span class="o">=</span> <span class="n">df_housing</span><span class="p">.</span><span class="nf">quantile</span><span class="p">(</span><span class="mf">0.75</span><span class="p">)</span>
<span class="n">IQR</span> <span class="o">=</span> <span class="n">Q3</span> <span class="o">-</span> <span class="n">Q1</span>

<span class="n">df_housing</span> <span class="o">=</span> <span class="n">df_housing</span><span class="p">[</span><span class="o">~</span><span class="p">((</span><span class="n">df_housing</span> <span class="o">&lt;</span> <span class="p">(</span><span class="n">Q1</span> <span class="o">-</span> <span class="mf">1.5</span> <span class="o">*</span> <span class="n">IQR</span><span class="p">))</span> <span class="o">|</span> <span class="p">(</span><span class="n">df_housing</span> <span class="o">&gt;</span> <span class="p">(</span><span class="n">Q3</span> <span class="o">+</span> <span class="mf">1.5</span> <span class="o">*</span> <span class="n">IQR</span><span class="p">))).</span><span class="nf">any</span><span class="p">(</span><span class="n">axis</span><span class="o">=</span><span class="mi">1</span><span class="p">)]</span>
</code></pre></div></div>

<p>Taking a look at the statistics again gives:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>             <span class="n">MedInc</span>     <span class="n">AveBedrms</span>      <span class="n">AveOccup</span>
<span class="n">count</span>  <span class="mf">16312.000000</span>  <span class="mf">16312.000000</span>  <span class="mf">16312.000000</span>
<span class="n">mean</span>       <span class="mf">3.657930</span>      <span class="mf">1.047226</span>      <span class="mf">2.865163</span>
<span class="n">std</span>        <span class="mf">1.444641</span>      <span class="mf">0.066569</span>      <span class="mf">0.624931</span>
<span class="nb">min</span>        <span class="mf">0.536000</span>      <span class="mf">0.866013</span>      <span class="mf">1.161290</span>
<span class="mi">25</span><span class="o">%</span>        <span class="mf">2.561150</span>      <span class="mf">1.002732</span>      <span class="mf">2.437060</span>
<span class="mi">50</span><span class="o">%</span>        <span class="mf">3.494650</span>      <span class="mf">1.043733</span>      <span class="mf">2.814886</span>
<span class="mi">75</span><span class="o">%</span>        <span class="mf">4.583300</span>      <span class="mf">1.088686</span>      <span class="mf">3.245208</span>
<span class="nb">max</span>        <span class="mf">8.011300</span>      <span class="mf">1.239521</span>      <span class="mf">4.560748</span>
</code></pre></div></div>

<p>This is much more reasonable. Now, the median income is $36,579. Additionally, the average number of occupants is now ~4.5 per household and average number of bedrooms is 1. While this may seem strange, this data most likely considers spaces like studios and small units, which is probably the standard in expensive and overpopulated places like Los Angeles and San Fransciso.</p>

<p>We can plot the feature distributions to get a better idea of the underlying statistics:</p>

<p><img src="/assets/images/build_mlp/feature_distribution_housing.png" alt="feature_dist" /></p>

<p>From the plot, we see different details regarding the features. One such detail is that the median income is mostly a normal distribution, but it is slightly skewed and has a heavy tail towards the higher incomes. This means that there are people with a high salary, although not many. Looking at the latitude and longitude plots, we can discern that there are two places in California where the housing prices are towards the higher end. The median house value data is also slightly skewed and has a heavy tail towards the higher values.</p>

<p><img src="/assets/images/build_mlp/price_by_location_housing.png" alt="price_by_loc" /></p>

<p>This next plot displays the price by location, with green being the most expensive. It’s interesting to note that the points show a graphical representation of the state of California. We also see that our conclusion about the expensive houses being in two locates was indeed correct. The most expensive houses are located on the coast of Southern California (i.e. San Diego, Los Angeles) and Northern California (i.e. San Fransciso).</p>

<p><img src="/assets/images/build_mlp/price_and_population_housing.png" alt="price_and_pop" /></p>

<p>This next plot displaces price by location, but also accounts for the population size by plotting circles where the radius is determined by the total population in that region. For example, the biggest circles are located on the coast of California. This makes sense since we know that the places with the highest population sizes are San Diego, Los Angeles, and the bay area to name a few. On the other hand, as you go further inland you find that on average the population decreases.</p>

<p><img src="/assets/images/build_mlp/correlation_map_housing.png" alt="corr_map" /></p>

<p>Next, we have a correlation map which tells us how each feature correlations to each other. For example, house age has no correlation to the median income, but median income has a positive correlation to average rooms and median house values. Latitude and longitude also have a high positive correlation with each other. This most likely relates to the two spikes we see in the histogram for these features. Overall, it seems that only a small subset of features actually correlation with each other.</p>

<p>We have now gone over the data that we will be using to train our MLP. Overall, this is a standard dataset used in introduction to machine learning courses. While it is considered simple and easy to use, it is great for early demonstrations of model building and hyperparameter tuning.</p>

<p>Feel free to reach out if you have any questions about what we covered this week. Next time we will train the model, tune hyperparameters, and learn ways to debug if issues arise. Stay tuned!</p>

<p><a href="/" class="btn">⬅ Back to Home</a></p>]]></content><author><name></name></author><category term="machine-learning" /><summary type="html"><![CDATA[October 05, 2025 In these next posts we will learn a little bit about hyperparameter tuning and debugging. This week I will discuss the data set we will be using and next week we will start training the model. When deciding on what model and dataset to use, I realized that I previously jumped straight into convolutional neural networks without first explaining the multi-layer perceptron (MLP). Although it got some coverage in my post on convolutional neural networks, I figured it would be a good idea to go back and touch on it again. I will not be extremely verbose about it since the focus of these posts is on hyperparameter tuning and debugging. We will be using is the California housing dataset to train and evaluate our model. This dataset contains aggregated data regarding each district in California such as median income, population, and location (Latitude and longitude). Here is a snippet of what the data looks like: MedInc HouseAge AveRooms AveBedrms Population AveOccup Latitude Longitude MedHouseVal 8.3252 41 6.98413 1.02381 322 2.55556 37.88 -122.23 4.526 8.3014 21 6.23814 0.97188 2401 2.10984 37.86 -122.22 3.585 7.2574 52 8.28814 1.07345 496 2.80226 37.85 -122.24 3.521 5.6431 52 5.81735 1.07306 558 2.54795 37.85 -122.25 3.413 3.8462 52 6.28185 1.08108 565 2.18147 37.85 -122.25 3.422 Let’s look at some statistics. As a note, the incomes displayed are all in tens of thousands of dollars (i.e. 1.23 = $12,300). MedInc AveBedrms AveOccup count 20640.000000 20640.000000 20640.000000 mean 3.870671 1.096675 3.070655 std 1.899822 0.473911 10.386050 min 0.499900 0.333333 0.692308 25% 2.563400 1.006079 2.429741 50% 3.534800 1.048780 2.818116 75% 4.743250 1.099526 3.282261 max 15.000100 34.066667 1243.333333 From the table above, we see that the average income is $38,700. Additionally, the average number of bedrooms per household is 1 with an average number of occupants being 3. While some values seem reasonable, others don’t make sense. For example, the max of average number of occupants 1243. This seems unlikely and is probably caused by how the consensus data was recorded. For example, we don’t knw how college dorms, apartment complexes, and prisons were accounted for. We can remove these outliers from the dataset so that we can have a better understanding of the average household by using the interquartile range and only keeping the middle 50% of the dataset: Q1 = df_housing.quantile(0.25) Q3 = df_housing.quantile(0.75) IQR = Q3 - Q1 df_housing = df_housing[~((df_housing &lt; (Q1 - 1.5 * IQR)) | (df_housing &gt; (Q3 + 1.5 * IQR))).any(axis=1)] Taking a look at the statistics again gives: MedInc AveBedrms AveOccup count 16312.000000 16312.000000 16312.000000 mean 3.657930 1.047226 2.865163 std 1.444641 0.066569 0.624931 min 0.536000 0.866013 1.161290 25% 2.561150 1.002732 2.437060 50% 3.494650 1.043733 2.814886 75% 4.583300 1.088686 3.245208 max 8.011300 1.239521 4.560748 This is much more reasonable. Now, the median income is $36,579. Additionally, the average number of occupants is now ~4.5 per household and average number of bedrooms is 1. While this may seem strange, this data most likely considers spaces like studios and small units, which is probably the standard in expensive and overpopulated places like Los Angeles and San Fransciso. We can plot the feature distributions to get a better idea of the underlying statistics: From the plot, we see different details regarding the features. One such detail is that the median income is mostly a normal distribution, but it is slightly skewed and has a heavy tail towards the higher incomes. This means that there are people with a high salary, although not many. Looking at the latitude and longitude plots, we can discern that there are two places in California where the housing prices are towards the higher end. The median house value data is also slightly skewed and has a heavy tail towards the higher values. This next plot displays the price by location, with green being the most expensive. It’s interesting to note that the points show a graphical representation of the state of California. We also see that our conclusion about the expensive houses being in two locates was indeed correct. The most expensive houses are located on the coast of Southern California (i.e. San Diego, Los Angeles) and Northern California (i.e. San Fransciso). This next plot displaces price by location, but also accounts for the population size by plotting circles where the radius is determined by the total population in that region. For example, the biggest circles are located on the coast of California. This makes sense since we know that the places with the highest population sizes are San Diego, Los Angeles, and the bay area to name a few. On the other hand, as you go further inland you find that on average the population decreases. Next, we have a correlation map which tells us how each feature correlations to each other. For example, house age has no correlation to the median income, but median income has a positive correlation to average rooms and median house values. Latitude and longitude also have a high positive correlation with each other. This most likely relates to the two spikes we see in the histogram for these features. Overall, it seems that only a small subset of features actually correlation with each other. We have now gone over the data that we will be using to train our MLP. Overall, this is a standard dataset used in introduction to machine learning courses. While it is considered simple and easy to use, it is great for early demonstrations of model building and hyperparameter tuning. Feel free to reach out if you have any questions about what we covered this week. Next time we will train the model, tune hyperparameters, and learn ways to debug if issues arise. Stay tuned! ⬅ Back to Home]]></summary></entry><entry><title type="html">Convolutional neural networks: MNIST dataset with machine learning pipeline</title><link href="https://ajg91.github.io//posts/machine-learning/convolutional-neural-network-with-mnist-pipeline/" rel="alternate" type="text/html" title="Convolutional neural networks: MNIST dataset with machine learning pipeline" /><published>2025-09-14T00:00:00-07:00</published><updated>2025-09-14T00:00:00-07:00</updated><id>https://ajg91.github.io//posts/machine-learning/convolutional-neural-network-with-mnist-pipeline</id><content type="html" xml:base="https://ajg91.github.io//posts/machine-learning/convolutional-neural-network-with-mnist-pipeline/"><![CDATA[<p class="post-date">September 14, 2025</p>

<p>Last week we went over how to construct a convolutional neural network (CNN) and applied the model to the MNIST dataset. This week I will show you how to modify the code to create a machine learning pipeline and well-documented. As before, here is the documentation for the function that we will be using to load the <a href="https://www.openml.org/search?type=data&amp;sort=runs&amp;id=554&amp;status=active" title="MNIST-docs">MNIST</a> dataset.</p>

<p>The process of refactoring and creating a pipeline is important because, if done correctly, it can save time and reduce errors. A code pipeline creates an automated and efficient system. By standardizing the workflow, the amount of errors will be reduced and, if there is an error, it will be easier to pinpoint and fix. It also makes starting future projects easier by allowing the use of the different components of the pipeline along different projects, thus speeding up the coding process and not reinventing the wheel. Another key feature of creating code pipelines is that it makes it simple to try different algorithms, hyperparameters, or preprocessing steps by keeping the workflow consistent.</p>

<p>The importance of documenting your code is something that cannot be stressed enough. Oftentimes, one finds that they come back to different pieces of code that they wrote a few months and have to take time to figure out what they did, which can be time consuming. The lack of documentation can be even more stressful when working with other people’s code that isn’t documented. Today I will show you how I document my code. I will also show a cool way to store parameters. I will be using the same CNN, same dataset, and same random seed to make sure that our changes do not affect the output.</p>

<p>First, let’s look at how to store parameters. I will present one way, but there are many different ways. In my case, I use the function <code class="language-plaintext highlighter-rouge">SimpleNamespace</code> and a yaml file.</p>

<p>Here’s the yaml file I am using in this particular case:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">path</span><span class="p">:</span> <span class="sh">'</span><span class="s">../outputs/</span><span class="sh">'</span>
<span class="n">seed</span><span class="p">:</span> <span class="mi">42</span>

<span class="n">split_size</span><span class="p">:</span> <span class="mf">0.1</span>
<span class="n">batch_size</span><span class="p">:</span> <span class="mi">64</span>
<span class="n">learning_rate</span><span class="p">:</span> <span class="mf">0.001</span>
<span class="n">epochs</span><span class="p">:</span> <span class="mi">3</span>

<span class="n">input_size</span><span class="p">:</span> <span class="mi">1</span>
<span class="n">output_size</span><span class="p">:</span> <span class="mi">10</span>
<span class="n">hidden_size</span><span class="p">:</span> <span class="mi">128</span>
</code></pre></div></div>

<p>The first thing we see is the path where we will be outputting the plots to. We also have the seed to seed all random number generators. The next batch are parameters used for splitting the dataset, the learning rate for the optimizer, and the number of times we will train the model. The last batch of parameters indicate the different sizes that we need to build our CNN.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">types</span> <span class="kn">import</span> <span class="n">SimpleNamespace</span>

<span class="n">dpi</span> <span class="o">=</span> <span class="mi">400</span>
<span class="n">dataset</span> <span class="o">=</span> <span class="sh">'</span><span class="s">mnist</span><span class="sh">'</span>

<span class="n">config</span> <span class="o">=</span> <span class="nf">load_config</span><span class="p">(</span><span class="n">path</span><span class="o">=</span><span class="sa">f</span><span class="sh">'</span><span class="s">../config_</span><span class="si">{</span><span class="n">dataset</span><span class="si">}</span><span class="s">.yaml</span><span class="sh">'</span><span class="p">)</span>
<span class="n">cfg</span> <span class="o">=</span> <span class="nc">SimpleNamespace</span><span class="p">(</span><span class="o">**</span><span class="n">config</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">(</span><span class="nf">vars</span><span class="p">(</span><span class="n">cfg</span><span class="p">),</span> <span class="n">indent</span><span class="o">=</span><span class="mi">2</span><span class="p">))</span>

<span class="nf">set_seed</span><span class="p">(</span><span class="n">cfg</span><span class="p">.</span><span class="n">seed</span><span class="p">)</span>
<span class="n">device</span> <span class="o">=</span> <span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span> <span class="k">if</span> <span class="n">tc</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">is_available</span><span class="p">()</span> <span class="k">else</span> <span class="sh">"</span><span class="s">cpu</span><span class="sh">"</span>
</code></pre></div></div>

<p>We load the <code class="language-plaintext highlighter-rouge">yaml</code> file into the variable <code class="language-plaintext highlighter-rouge">config</code> as a dictionary. This variable is then passed into the function <code class="language-plaintext highlighter-rouge">SimpleNamespace</code>. The double asterisk unpacks the key-value pairs as keyword arguments into the variable <code class="language-plaintext highlighter-rouge">cfg</code>, allowing us to call the parameters as attributes of <code class="language-plaintext highlighter-rouge">cfg</code>. This is pretty neat! Now all the parameters are stored in one place. All you have to do is call them from <code class="language-plaintext highlighter-rouge">cfg</code> as is shown in the function call <code class="language-plaintext highlighter-rouge">set_seed(cfg.seed)</code>. We also specify the device that will be used, writing the code in such a way so that CUDA is the desired choice if available.</p>

<p>Next, we initialize the model, optimizer, loss function, and get our train, validation, and test dataloaders.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cnn</span> <span class="o">=</span> <span class="nc">SimpleCNN</span><span class="p">(</span><span class="n">cfg</span><span class="p">.</span><span class="n">input_size</span><span class="p">,</span> <span class="n">cfg</span><span class="p">.</span><span class="n">output_size</span><span class="p">,</span> <span class="n">cfg</span><span class="p">.</span><span class="n">hidden_size</span><span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">device</span><span class="p">)</span>
<span class="n">optimizer</span> <span class="o">=</span> <span class="n">optim</span><span class="p">.</span><span class="nc">AdamW</span><span class="p">(</span><span class="n">cnn</span><span class="p">.</span><span class="nf">parameters</span><span class="p">(),</span> <span class="n">lr</span><span class="o">=</span><span class="n">cfg</span><span class="p">.</span><span class="n">learning_rate</span><span class="p">)</span>
<span class="n">loss_fn</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">CrossEntropyLoss</span><span class="p">()</span>

<span class="n">train</span><span class="p">,</span> <span class="n">val</span><span class="p">,</span> <span class="n">test</span> <span class="o">=</span> <span class="nf">get_dataloaders</span><span class="p">(</span><span class="n">dataset</span><span class="p">,</span> <span class="n">cfg</span><span class="p">.</span><span class="n">split_size</span><span class="p">,</span> <span class="n">cfg</span><span class="p">.</span><span class="n">batch_size</span><span class="p">,</span> <span class="n">cfg</span><span class="p">.</span><span class="n">seed</span><span class="p">)</span>
</code></pre></div></div>

<p>Overall, this should be self-explanatory since it’s the same process as shown in last week’s post, but now we have a function for getting the dataloaders. Let’s look at this function closely. I will use this function and the subsequent classes used in <code class="language-plaintext highlighter-rouge">get_dataloaders</code> to explain creating a machine learning pipeline and documenting the code. I won’t go over every detail, but will focus on what I feel is important to understand. I will start out by showing each individual component of <code class="language-plaintext highlighter-rouge">get_dataloaders</code>, then show the full function at the end. For subsequent classes, I will only show the relevant subset of the class. Let’s get started:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">get_dataloaders</span><span class="p">(</span>
    <span class="n">dataset</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span>
    <span class="n">split_size</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.1</span><span class="p">,</span>
    <span class="n">batch_size</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">64</span><span class="p">,</span>
    <span class="n">seed</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">42</span><span class="p">,</span>
    <span class="o">**</span><span class="n">kwargs</span>
<span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Tuple</span><span class="p">[</span><span class="n">DataLoader</span><span class="p">,</span> <span class="n">DataLoader</span><span class="p">,</span> <span class="n">DataLoader</span><span class="p">]:</span>
</code></pre></div></div>

<p>First, let’s look at the header. If you contrast the inputs with my previous codes, you see that I now show the data types of the inputs. This is called <em>type hinting</em>. It allows one to quickly understand what kind of inputs the function expects. For example, I can have a function header such as</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">add</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">):</span>
</code></pre></div></div>

<p>While we can probably guess that we are adding numbers, what if the function looked like the following:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">add</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">):</span>
    <span class="k">return</span> <span class="n">x</span><span class="p">[</span><span class="n">a</span><span class="p">]</span> <span class="o">+</span> <span class="n">x</span><span class="p">[</span><span class="n">b</span><span class="p">]</span>
</code></pre></div></div>

<p>if we would have passed floats into <code class="language-plaintext highlighter-rouge">a</code> and <code class="language-plaintext highlighter-rouge">b</code>, it would have resulted in an error. Imagine instead we wrote:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">add</span><span class="p">(</span><span class="n">x</span><span class="p">:</span> <span class="n">numpy</span><span class="p">.</span><span class="n">ndarray</span><span class="p">,</span> <span class="n">a</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">b</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span>
    <span class="k">return</span> <span class="n">x</span><span class="p">[</span><span class="n">a</span><span class="p">]</span> <span class="o">+</span> <span class="n">x</span><span class="p">[</span><span class="n">b</span><span class="p">]</span>
</code></pre></div></div>

<p>Now we know that <code class="language-plaintext highlighter-rouge">x</code> is a numpy array, <code class="language-plaintext highlighter-rouge">a</code> is an integer, <code class="language-plaintext highlighter-rouge">b</code> is an integer, and the resulting output is a floating point. While we may still need to look at the documentation to get more information on the function, at least we wouldn’t have made the initial error of passing in floats.</p>

<p>Looking back at the <code class="language-plaintext highlighter-rouge">get_dataloaders</code> function, we see that <code class="language-plaintext highlighter-rouge">dataset</code> is a string. If there was no type hinting we might have thought it was the actual dataset that was being passed into the function. We can also see that <code class="language-plaintext highlighter-rouge">split_size</code> is a float and has a default value, and <code class="language-plaintext highlighter-rouge">batch_size</code> and <code class="language-plaintext highlighter-rouge">seed</code> are integers with default values, as well. The expected output of the function is also shown and is a tuple composed of three dataloaders. At a glance, we immediately have more information about the function compared to if it did not have type hinting.</p>

<p>From here, we look at the docstring, which is the documentation:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="sh">"""</span><span class="s">
Create PyTorch DataLoaders for training, validation, and test sets.

Parameters
----------
dataset : str
    Name of the dataset to load.
split_size : float, optional (default=0.1)
    The fraction of data that will be split off for the validation/test set.
batch_size : int, optional (default=64)
    Number of samples per batch.
seed : int, optional (default=42)
    Random seed for reproducible splitting of the dataset.
**kwargs
    Additional keyword arguments.

Returns
-------
train_loader : DataLoader
    DataLoader for the training set, with shuffling enabled.
val_loader : DataLoader
    DataLoader for the validation set, without shuffling.
test_loader : DataLoader
    DataLoader for the test set, without shuffling.
</span><span class="sh">"""</span>
</code></pre></div></div>

<p>Basic documentation is broken down into three parts: description, inputs, and outputs. One can get more sophisticated with the docstring if one wishes, but let’s keep it basic for now. The description of this function is short and to the point. The function creates PyTorch DataLoaders for training, validation, and test sets. The input section is given by <strong>“Parameters”</strong> and shows the data type and gives a short description of each input variable and whether or not they have a default value (find optional in docstring). Here, I don’t have arrays (or tensors), but if one does it is always good to include the expected shape of the array in the short description. The final section is given by <strong>“Returns”</strong> and displays the outputs of the function with the data type and short description of each output variable just like with the inputs.</p>

<p>At this point we know exactly what the function does, the different data types involved, and the inputs and outputs of the function. Now, all that’s left is the code itself so that we can capture all the details of the function.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">data</span> <span class="o">=</span> <span class="nc">PreprocessDataset</span><span class="p">(</span><span class="n">dataset</span><span class="p">,</span> <span class="n">split_size</span><span class="p">,</span> <span class="n">rand_seed</span><span class="o">=</span><span class="n">seed</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">)</span>
<span class="nf">return </span><span class="p">(</span>
    <span class="nc">DataLoader</span><span class="p">(</span><span class="nc">TensorDataset</span><span class="p">(</span><span class="n">data</span><span class="p">.</span><span class="n">X_train</span><span class="p">,</span> <span class="n">data</span><span class="p">.</span><span class="n">y_train</span><span class="p">),</span> <span class="n">batch_size</span><span class="o">=</span><span class="n">batch_size</span><span class="p">,</span> <span class="n">shuffle</span><span class="o">=</span><span class="bp">True</span><span class="p">),</span>
    <span class="nc">DataLoader</span><span class="p">(</span><span class="nc">TensorDataset</span><span class="p">(</span><span class="n">data</span><span class="p">.</span><span class="n">X_val</span><span class="p">,</span> <span class="n">data</span><span class="p">.</span><span class="n">y_val</span><span class="p">),</span> <span class="n">batch_size</span><span class="o">=</span><span class="n">batch_size</span><span class="p">),</span>
    <span class="nc">DataLoader</span><span class="p">(</span><span class="nc">TensorDataset</span><span class="p">(</span><span class="n">data</span><span class="p">.</span><span class="n">X_test</span><span class="p">,</span> <span class="n">data</span><span class="p">.</span><span class="n">y_test</span><span class="p">),</span> <span class="n">batch_size</span><span class="o">=</span><span class="n">batch_size</span><span class="p">)</span>
<span class="p">)</span>
</code></pre></div></div>

<p>Here, we see that we call <code class="language-plaintext highlighter-rouge">PreprocessDataset</code> with the dataset, which returns an object <code class="language-plaintext highlighter-rouge">data</code> with the training, validation, and test data as attributes. These attributes are then passed into <code class="language-plaintext highlighter-rouge">TensorDataset</code> and <code class="language-plaintext highlighter-rouge">DataLoader</code> like in my previous post. The order of the dataloaders in the output tuple are <code class="language-plaintext highlighter-rouge">(train, validation, test)</code>. Combining the header, docstring, and code gives us the full function:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">get_dataloaders</span><span class="p">(</span>
    <span class="n">dataset</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span>
    <span class="n">split_size</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.1</span><span class="p">,</span>
    <span class="n">batch_size</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">64</span><span class="p">,</span>
    <span class="n">seed</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">42</span><span class="p">,</span>
    <span class="o">**</span><span class="n">kwargs</span>
<span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Tuple</span><span class="p">[</span><span class="n">DataLoader</span><span class="p">,</span> <span class="n">DataLoader</span><span class="p">,</span> <span class="n">DataLoader</span><span class="p">]:</span>
    <span class="sh">"""</span><span class="s">
    Create PyTorch DataLoaders for training, validation, and test sets.

    Parameters
    ----------
    dataset : str
        Name of the dataset to load.
    split_size : float, optional (default=0.1)
        The fraction of data that will be split off for the validation/test set.
    batch_size : int, optional (default=64)
        Number of samples per batch.
    seed : int, optional (default=42)
        Random seed for reproducible splitting of the dataset.
    **kwargs
        Additional keyword arguments.

    Returns
    -------
    train_loader : DataLoader
        DataLoader for the training set, with shuffling enabled.
    val_loader : DataLoader
        DataLoader for the validation set, without shuffling.
    test_loader : DataLoader
        DataLoader for the test set, without shuffling.
    </span><span class="sh">"""</span>
    <span class="n">data</span> <span class="o">=</span> <span class="nc">PreprocessDataset</span><span class="p">(</span><span class="n">dataset</span><span class="p">,</span> <span class="n">split_size</span><span class="p">,</span> <span class="n">rand_seed</span><span class="o">=</span><span class="n">seed</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">)</span>
    <span class="nf">return </span><span class="p">(</span>
        <span class="nc">DataLoader</span><span class="p">(</span><span class="nc">TensorDataset</span><span class="p">(</span><span class="n">data</span><span class="p">.</span><span class="n">X_train</span><span class="p">,</span> <span class="n">data</span><span class="p">.</span><span class="n">y_train</span><span class="p">),</span> <span class="n">batch_size</span><span class="o">=</span><span class="n">batch_size</span><span class="p">,</span> <span class="n">shuffle</span><span class="o">=</span><span class="bp">True</span><span class="p">),</span>
        <span class="nc">DataLoader</span><span class="p">(</span><span class="nc">TensorDataset</span><span class="p">(</span><span class="n">data</span><span class="p">.</span><span class="n">X_val</span><span class="p">,</span> <span class="n">data</span><span class="p">.</span><span class="n">y_val</span><span class="p">),</span> <span class="n">batch_size</span><span class="o">=</span><span class="n">batch_size</span><span class="p">),</span>
        <span class="nc">DataLoader</span><span class="p">(</span><span class="nc">TensorDataset</span><span class="p">(</span><span class="n">data</span><span class="p">.</span><span class="n">X_test</span><span class="p">,</span> <span class="n">data</span><span class="p">.</span><span class="n">y_test</span><span class="p">),</span> <span class="n">batch_size</span><span class="o">=</span><span class="n">batch_size</span><span class="p">)</span>
    <span class="p">)</span>
</code></pre></div></div>

<p>It’s definitely more verbose than if I just included the code portion, but there is now so much information embedded in it that will be useful when it comes time to use this function!</p>

<p>Let’s now take a look at a subset of the class <code class="language-plaintext highlighter-rouge">PreprocessDataset</code>:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">PreprocessDataset</span><span class="p">(</span><span class="n">Dataset</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Preprocess a dataset and provide train, validation, and test splits as PyTorch tensors.

    Attributes
    ----------
    X_train : tc.Tensor
        Training input features.
    y_train : tc.Tensor
        Training labels.
    X_val : tc.Tensor
        Validation input features.
    y_val : tc.Tensor
        Validation labels.
    X_test : tc.Tensor
        Test input features.
    y_test : tc.Tensor
        Test labels.

    Parameters
    ----------
    dataset : str
        Name of the dataset to load.
        Options: </span><span class="sh">'</span><span class="s">MNIST</span><span class="sh">'</span><span class="s">, </span><span class="sh">'</span><span class="s">MEDMNIST</span><span class="sh">'</span><span class="s">
    split_size : float, optional (default=0.1)
        The fraction of data that will be split off for the validation/test set.
    rand_seed : int, optional (default=42)
        Random seed for reproducible splitting.
    **kwargs
        Additional keyword arguments.
    </span><span class="sh">"""</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">dataset</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> 
        <span class="n">split_size</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.1</span><span class="p">,</span> 
        <span class="n">rand_seed</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">42</span><span class="p">,</span> 
        <span class="o">**</span><span class="n">kwargs</span>
    <span class="p">):</span>
        <span class="n">loader</span> <span class="o">=</span> <span class="nc">LoadDatasets</span><span class="p">(</span><span class="n">dataset</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">)</span>
        <span class="n">X</span><span class="p">,</span> <span class="n">y</span> <span class="o">=</span> <span class="n">loader</span><span class="p">.</span><span class="n">X</span> <span class="o">/</span> <span class="mf">255.0</span><span class="p">,</span> <span class="n">loader</span><span class="p">.</span><span class="n">y</span>
        
        <span class="n">X_train</span><span class="p">,</span> <span class="n">X_val</span><span class="p">,</span> <span class="n">y_train</span><span class="p">,</span> <span class="n">y_val</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_split_dataset</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">split_size</span><span class="p">,</span> <span class="n">rand_seed</span><span class="p">)</span>
        <span class="n">X_train</span><span class="p">,</span> <span class="n">X_test</span><span class="p">,</span> <span class="n">y_train</span><span class="p">,</span> <span class="n">y_test</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_split_dataset</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">,</span> <span class="n">split_size</span><span class="p">,</span> <span class="n">rand_seed</span><span class="p">)</span>

        <span class="n">self</span><span class="p">.</span><span class="n">X_train</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">y_train</span> <span class="o">=</span> <span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span>
        <span class="n">self</span><span class="p">.</span><span class="n">X_val</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">y_val</span> <span class="o">=</span> <span class="n">X_val</span><span class="p">,</span> <span class="n">y_val</span>
        <span class="n">self</span><span class="p">.</span><span class="n">X_test</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">y_test</span> <span class="o">=</span> <span class="n">X_test</span><span class="p">,</span> <span class="n">y_test</span>

    <span class="k">def</span> <span class="nf">_split_dataset</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> 
        <span class="n">X</span><span class="p">:</span> <span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> 
        <span class="n">y</span><span class="p">:</span> <span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> 
        <span class="n">split_size</span><span class="p">:</span> <span class="nb">float</span><span class="p">,</span> 
        <span class="n">rand_seed</span><span class="p">:</span> <span class="nb">int</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Tuple</span><span class="p">[</span><span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">]:</span>
        <span class="sh">"""</span><span class="s">
        Split the dataset into train/test or train/validation subsets.

        Parameters
        ----------
        X : tc.Tensor
            Input features.
        y : tc.Tensor
            Target labels.
        split_size : float
            The fraction of data that will be split off for the validation/test set.
        rand_seed : int
            Random seed for reproducibility.

        Returns
        -------
        X_train : tc.Tensor
            Features of the first split.
        X_test : tc.Tensor
            Features of the second split.
        y_train : tc.Tensor
            Labels of the first split.
        y_test : tc.Tensor
            Labels of the second split.
        </span><span class="sh">"""</span>
        <span class="k">return</span> <span class="nf">train_test_split</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">test_size</span><span class="o">=</span><span class="n">split_size</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="n">rand_seed</span><span class="p">)</span>
</code></pre></div></div>

<p>The main docstring for a class documents the constructor. Then, each individual function has it’s own docstring. Looking at the constructor docstring, you will see that we have now included <strong>“Attributes”</strong>. These are instance attributes that belong to the class objects. This essentially means you can do things like:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">data</span> <span class="o">=</span> <span class="nc">PreprocessDataset</span><span class="p">(</span><span class="sh">'</span><span class="s">mnist</span><span class="sh">'</span><span class="p">)</span>
<span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span> <span class="o">=</span> <span class="n">data</span><span class="p">.</span><span class="n">X_train</span><span class="p">,</span> <span class="n">data</span><span class="p">.</span><span class="n">y_train</span>
</code></pre></div></div>

<p>where data is the object and <code class="language-plaintext highlighter-rouge">X_train</code> and <code class="language-plaintext highlighter-rouge">y_train</code> are the attributes. In the case of this class, we have six instance attributes, which are our data sets. The docstring for these attributes is written in the same way as <strong>“Parameters”</strong> and <strong>“Returns”</strong>*. Note that the docstring says that the variable <code class="language-plaintext highlighter-rouge">datasets</code> can either be ‘MNIST’ or ‘MEDMNIST’. More about this later. One can also choose to give a brief summary for each method in the class in the constructor docstring, but I will not be doing this.</p>

<p>In the constructor, we instance an object <code class="language-plaintext highlighter-rouge">loader</code> from the <code class="language-plaintext highlighter-rouge">LoadDatasets</code> class (which we will see in a bit). The instance attributes tied to the <code class="language-plaintext highlighter-rouge">loader</code> object are the <code class="language-plaintext highlighter-rouge">x</code> and <code class="language-plaintext highlighter-rouge">y</code> datasets. We then use these and call the method <code class="language-plaintext highlighter-rouge">_split_dataset</code>, which will now give us our train, validation, and test sets. The underscore in the method name indicates that this is meant to be an <em>internal</em> method that should not be accessed from outside the class. Python does not enforce this so it can still be called from outside the class if you so wish, but if you find yourself purposely writing underscores to call methods outside of the class constructor or other methods you may want to rethink your coding pipeline. For the last part of the constructor, we turn the different data sets into instance attributes. This is easily done by writing <code class="language-plaintext highlighter-rouge">self.X_train = X_train</code> for all variables you want to save.</p>

<p>At this point you may be thinking to yourself, “This is so complicated!! Why are we doing all this if our previous code, without functions and classes, worked just fine?” As Yoda would say, “patience you must have, my young Padawan.” Let’s look at the last class, <code class="language-plaintext highlighter-rouge">LoadDatasets</code> and hopefully everything becomes clear.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">LoadDatasets</span><span class="p">():</span>
    <span class="sh">"""</span><span class="s">
    Load and return datasets as PyTorch tensors.

    Attributes
    ----------
    dataset : str
        Name of the dataset to load (e.g., </span><span class="sh">'</span><span class="s">MNIST</span><span class="sh">'</span><span class="s">, </span><span class="sh">'</span><span class="s">MEDMNIST</span><span class="sh">'</span><span class="s">).
    X : tc.Tensor
        Input features of the dataset as a PyTorch tensor.
        shape: (num_samples, channels, height, width)
    y : tc.Tensor
        Target labels as a PyTorch tensor.

    Parameters
    ----------
    dataset : str
        The name of the dataset to load.
        Options: </span><span class="sh">'</span><span class="s">MNIST</span><span class="sh">'</span><span class="s"> or </span><span class="sh">'</span><span class="s">MEDMNIST</span><span class="sh">'</span><span class="s">
    **kwargs
        Additional keyword arguments passed to the specific dataset loader methods.

    Raises
    ------
    ValueError
        If an unsupported dataset name is provided.
    </span><span class="sh">"""</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">dataset</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">dataset</span> <span class="o">=</span> <span class="n">dataset</span><span class="p">.</span><span class="nf">upper</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">loaders</span> <span class="o">=</span> <span class="p">{</span>
            <span class="sh">'</span><span class="s">MNIST</span><span class="sh">'</span><span class="p">:</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">self</span><span class="p">.</span><span class="nf">load_mnist</span><span class="p">(</span><span class="o">**</span><span class="n">kwargs</span><span class="p">),</span>
            <span class="sh">'</span><span class="s">MEDMNIST</span><span class="sh">'</span><span class="p">:</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">self</span><span class="p">.</span><span class="nf">load_medmnist</span><span class="p">(</span><span class="o">**</span><span class="n">kwargs</span><span class="p">),</span>
        <span class="p">}</span>

        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">dataset</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">loaders</span><span class="p">:</span>
            <span class="k">raise</span> <span class="nc">ValueError</span><span class="p">(</span><span class="sa">f</span><span class="sh">'</span><span class="s">Dataset not supported -&gt; </span><span class="si">{</span><span class="n">dataset</span><span class="si">}</span><span class="s">. </span><span class="sh">'</span>
                             <span class="sa">f</span><span class="sh">'</span><span class="s">Available: </span><span class="si">{</span><span class="nf">list</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">loaders</span><span class="p">.</span><span class="nf">keys</span><span class="p">())</span><span class="si">}</span><span class="sh">'</span><span class="p">)</span>

        <span class="n">self</span><span class="p">.</span><span class="n">X</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">y</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">loaders</span><span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">dataset</span><span class="p">]()</span>
        
    <span class="k">def</span> <span class="nf">load_mnist</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">version</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">1</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Tuple</span><span class="p">[</span><span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">]:</span>
        <span class="sh">"""</span><span class="s">
        Load the MNIST dataset and convert it to PyTorch tensors.

        Parameters
        ----------
        version : int, optional (default=1)
            The version of the MNIST dataset from OpenML.

        Returns
        -------
        X : tc.Tensor
            MNIST images as a float32 tensor normalized to [0, 1].
            Shape: (num_samples, 1, 28, 28)
        y : tc.Tensor
            MNIST labels as int64 tensor.
            Shape: (num_samples,)
        </span><span class="sh">"""</span>
        <span class="kn">from</span> <span class="n">sklearn.datasets</span> <span class="kn">import</span> <span class="n">fetch_openml</span>
        
        <span class="n">X</span><span class="p">,</span> <span class="n">y</span> <span class="o">=</span> <span class="nf">fetch_openml</span><span class="p">(</span><span class="sh">'</span><span class="s">mnist_784</span><span class="sh">'</span><span class="p">,</span> <span class="n">version</span><span class="o">=</span><span class="n">version</span><span class="p">,</span> <span class="n">return_X_y</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">as_frame</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">X</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">tc</span><span class="p">.</span><span class="n">float32</span><span class="p">)</span>
        <span class="n">y</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">from_numpy</span><span class="p">(</span><span class="n">y</span><span class="p">.</span><span class="nf">astype</span><span class="p">(</span><span class="sh">'</span><span class="s">int64</span><span class="sh">'</span><span class="p">))</span>
        <span class="k">return</span> <span class="n">X</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">28</span><span class="p">,</span> <span class="mi">28</span><span class="p">),</span> <span class="n">y</span>
        
    <span class="k">def</span> <span class="nf">load_medmnist</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">flag</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">'</span><span class="s">bloodmnist</span><span class="sh">'</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Tuple</span><span class="p">[</span><span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">tc</span><span class="p">.</span><span class="n">Tensor</span><span class="p">]:</span>
        <span class="sh">"""</span><span class="s">
        Load a MedMNIST dataset and convert it to PyTorch tensors.

        Parameters
        ----------
        flag : str, optional (default=</span><span class="sh">'</span><span class="s">bloodmnist</span><span class="sh">'</span><span class="s">)
            Specifies which MedMNIST dataset to load.
            Must be one of the keys in `medmnist.INFO`.

        Returns
        -------
        X : tc.Tensor
            MedMNIST images as a float32 tensor normalized to [0, 1].
            Shape: (num_samples, channels, height, width)
        y : tc.Tensor
            MedMNIST labels as long tensors.
            Shape: (num_samples,)

        Raises
        ------
        ValueError
            If the specified `flag` does not correspond to a valid MedMNIST dataset.
        </span><span class="sh">"""</span>
        <span class="kn">import</span> <span class="n">medmnist</span>
        <span class="kn">from</span> <span class="n">medmnist</span> <span class="kn">import</span> <span class="n">INFO</span>
        
        <span class="k">if</span> <span class="n">flag</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">INFO</span><span class="p">:</span>
            <span class="k">raise</span> <span class="nc">ValueError</span><span class="p">(</span><span class="sa">f</span><span class="sh">'</span><span class="s">Invalid MedMNIST flag -&gt; </span><span class="si">{</span><span class="n">flag</span><span class="si">}</span><span class="s">. </span><span class="sh">'</span>
                             <span class="sa">f</span><span class="sh">'</span><span class="s">Available datasets: </span><span class="si">{</span><span class="nf">list</span><span class="p">(</span><span class="n">INFO</span><span class="p">.</span><span class="nf">keys</span><span class="p">())</span><span class="si">}</span><span class="sh">"'</span><span class="p">)</span>

        <span class="nf">print</span><span class="p">(</span><span class="n">INFO</span><span class="p">[</span><span class="n">flag</span><span class="p">][</span><span class="sh">'</span><span class="s">label</span><span class="sh">'</span><span class="p">])</span>
        <span class="n">DataClass</span> <span class="o">=</span> <span class="nf">getattr</span><span class="p">(</span><span class="n">medmnist</span><span class="p">,</span> <span class="n">INFO</span><span class="p">[</span><span class="n">flag</span><span class="p">][</span><span class="sh">'</span><span class="s">python_class</span><span class="sh">'</span><span class="p">])</span>
        
        <span class="n">train_ds</span> <span class="o">=</span> <span class="nc">DataClass</span><span class="p">(</span><span class="n">split</span><span class="o">=</span><span class="sh">'</span><span class="s">train</span><span class="sh">'</span><span class="p">,</span> <span class="n">download</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">val_ds</span> <span class="o">=</span> <span class="nc">DataClass</span><span class="p">(</span><span class="n">split</span><span class="o">=</span><span class="sh">'</span><span class="s">val</span><span class="sh">'</span><span class="p">,</span> <span class="n">download</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">test_ds</span> <span class="o">=</span> <span class="nc">DataClass</span><span class="p">(</span><span class="n">split</span><span class="o">=</span><span class="sh">'</span><span class="s">test</span><span class="sh">'</span><span class="p">,</span> <span class="n">download</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        
        <span class="n">X</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">concatenate</span><span class="p">([</span><span class="n">train_ds</span><span class="p">.</span><span class="n">imgs</span><span class="p">,</span> <span class="n">val_ds</span><span class="p">.</span><span class="n">imgs</span><span class="p">,</span> <span class="n">test_ds</span><span class="p">.</span><span class="n">imgs</span><span class="p">],</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
        <span class="n">y</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">concatenate</span><span class="p">([</span><span class="n">train_ds</span><span class="p">.</span><span class="n">labels</span><span class="p">,</span> <span class="n">val_ds</span><span class="p">.</span><span class="n">labels</span><span class="p">,</span> <span class="n">test_ds</span><span class="p">.</span><span class="n">labels</span><span class="p">],</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
        
        <span class="n">X</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">tc</span><span class="p">.</span><span class="n">float32</span><span class="p">)</span>
        <span class="n">y</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">y</span><span class="p">).</span><span class="nf">long</span><span class="p">().</span><span class="nf">squeeze</span><span class="p">()</span>
        <span class="k">return</span> <span class="n">X</span><span class="p">.</span><span class="nf">permute</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">),</span> <span class="n">y</span>
</code></pre></div></div>

<p>In the docstring for this class I have included yet <em>another</em> section, <strong>“Raises”</strong>. By seeing this we know that somewhere in the constructor there will be a piece of code that will potentially raise an error if something is wrong. The docstring says that this will happen if an unsupported dataset name is provided. Aha! So If I pass in an incorrect string or a string that does not match a known dataset in the class an error will be raised.</p>

<p>Looking at the constructor, we see that the dataset string is capitalized. This is to prevent case sensitivity raising an error. The dataset name is passed into a dictionary that calls the method that corresponds to the dataset being asked for. We also see that the methods are called via lambda function. The reason is so that if we have parameters that we need to pass into the dataset methods we can do it as a keyword argument (a.k.a. kwargs). For example, take <a href="https://medmnist.com/" title="MEDMNIST-docs">MEDMNIST</a>, which is a library of medical datasets. An input to the method is the specific MEDMNIST dataset that we want. In this case, the default option is “bloodmnist”. If instead we wanted ChestMNIST, then we pass in “chestmnist” as a keyword argument into <code class="language-plaintext highlighter-rouge">get_dataloaders</code>. If no keyword arguments are passed in, then the lambda function is treated as if it was a regular method call.</p>

<p>So let’s review the function and classes used for obtaining the dataloaders. The <code class="language-plaintext highlighter-rouge">get_dataloaders</code> function calls the <code class="language-plaintext highlighter-rouge">PreprocessDataset</code> class. This class is solely used for grabbing the already loaded features and targets, normalizing, and splitting them into train, validation, and test sets. The <code class="language-plaintext highlighter-rouge">PreprocessDataset</code> class calls the <code class="language-plaintext highlighter-rouge">LoadDatasets</code> class, which is solely used for loading the features and targets from their respective Python package. The classes <strong>do not</strong> care about the inner workings of each other. <code class="language-plaintext highlighter-rouge">PreprocessDataset</code> only cares that there is a class with the feature and target variables as attributes. On the other hand, <code class="language-plaintext highlighter-rouge">LoadDatasets</code> can be called independently from everything else if one only wants the feature and target variables. This way of modularizing code helps once projects start getting big and pipelines get complicated. It keeps everything in such a way so that you can swap out functions or classes with ease. You don’t have everything tangled up together, which can result in headaches when making changes.</p>

<p>Another cool thing is that adding datasets to <code class="language-plaintext highlighter-rouge">LoadDatasets</code> is a breeze. All you have to do is write a function in the class that outputs the feature and target variables with the shape $\text{X.shape} = (\text{num_samples}, \text{channels}, \text{height}, \text{width})$ and $\text{y.shape} = (\text{num_samples},)$ and add the dataset to the dictionary in the constructor. From there, getting the dataloaders for this new dataset is as easy as calling <code class="language-plaintext highlighter-rouge">get_dataloaders</code> and passing in the appropriate name. You don’t have to copy over you preprocessing pipeline or the <code class="language-plaintext highlighter-rouge">train_test_split</code> function.</p>

<p>I won’t go over the functions <code class="language-plaintext highlighter-rouge">train_model</code> and <code class="language-plaintext highlighter-rouge">evaluate_model</code> since they are pretty self-explanatory and the bulk of it was covered last week. What I do want to show are the train, validation, and test results:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Epoch</span> <span class="mi">1</span><span class="p">,</span> <span class="n">Train</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.88814815</span><span class="p">,</span> <span class="n">Validation</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.94385714</span>
<span class="n">Epoch</span> <span class="mi">2</span><span class="p">,</span> <span class="n">Train</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.95839506</span><span class="p">,</span> <span class="n">Validation</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.96385714</span>
<span class="n">Epoch</span> <span class="mi">3</span><span class="p">,</span> <span class="n">Train</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.97007055</span><span class="p">,</span> <span class="n">Validation</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.96928571</span>
<span class="n">Test</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.97190476</span>
</code></pre></div></div>

<p>We note that they are the same as last week! This means that our code refactoring did not fundamentally change anything in the model, which is great validation that we did not introduce a bug into the code. The misclassified images are also the same:</p>

<p><img src="/assets/images/build_cnn/misclassified_images_mnist_pipeline_code.png" alt="mnist_misclassified" /></p>

<p>which is to be expected since we are seeding all the random number generators. All is well. The cool thing is that all the training and evaluation code is agnostic to the dataset, which means that you can use it on other datasets with very minimal tweaks!</p>

<p>The functions used to produce the plots are also written in such a way as to allow flexibility when using different datasets. I will only go over one of the functions:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">plot_images</span><span class="p">(</span>
    <span class="n">loader</span><span class="p">:</span> <span class="n">tc</span><span class="p">.</span><span class="n">utils</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="n">DataLoader</span><span class="p">,</span> 
    <span class="n">path</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> 
    <span class="n">dpi</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> 
    <span class="n">data</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span>
    <span class="n">size</span><span class="p">:</span> <span class="nb">list</span> <span class="o">=</span> <span class="p">[</span><span class="mi">2</span><span class="p">,</span> <span class="mi">5</span><span class="p">],</span> 
    <span class="n">figsize</span><span class="p">:</span> <span class="n">Tuple</span> <span class="o">=</span> <span class="p">(</span><span class="mi">14</span><span class="p">,</span> <span class="mi">6</span><span class="p">),</span>
    <span class="n">cmap</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="bp">None</span>
<span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Plots a subset of the dataset images.

    Parameters
    ----------
    loader : tc.utils.data.DataLoader
        DataLoader for the training set.
    path : str
        String to the directory where the plot is saved.
    dpi : int
        Dots per inch.
        A higher dpi results in a sharper image.
    data : str
        The name of the dataset being plotted.
        This is used to label the plot when saving to directory.
    size : list, optional (default=[2, 5])
        Specifies the number of rows and columns.
    figsize : Tuple, optional (default=[14, 6])
        Specifies the figure size.
    cmap : str, optional (default=None)
        Colormap for 2D images.
    </span><span class="sh">"""</span>
    <span class="n">images</span><span class="p">,</span> <span class="n">labels</span> <span class="o">=</span> <span class="nf">next</span><span class="p">(</span><span class="nf">iter</span><span class="p">(</span><span class="n">loader</span><span class="p">))</span>

    <span class="n">fig</span><span class="p">,</span> <span class="n">axes</span> <span class="o">=</span> <span class="n">plt</span><span class="p">.</span><span class="nf">subplots</span><span class="p">(</span><span class="n">size</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">size</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="n">figsize</span><span class="o">=</span><span class="n">figsize</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">ax</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">axes</span><span class="p">.</span><span class="n">flat</span><span class="p">):</span>
        <span class="n">img</span> <span class="o">=</span> <span class="n">images</span><span class="p">[</span><span class="n">i</span><span class="p">].</span><span class="nf">permute</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">img</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span> <span class="o">==</span> <span class="mi">1</span><span class="p">:</span>
            <span class="n">img</span> <span class="o">=</span> <span class="n">img</span><span class="p">.</span><span class="nf">squeeze</span><span class="p">()</span>
            <span class="n">cmap</span> <span class="o">=</span> <span class="sh">'</span><span class="s">gray</span><span class="sh">'</span>
            
        <span class="n">ax</span><span class="p">.</span><span class="nf">imshow</span><span class="p">(</span><span class="n">img</span><span class="p">,</span> <span class="n">cmap</span><span class="o">=</span><span class="n">cmap</span><span class="p">)</span>
        <span class="n">ax</span><span class="p">.</span><span class="nf">set_title</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Label: </span><span class="si">{</span><span class="n">labels</span><span class="p">[</span><span class="n">i</span><span class="p">].</span><span class="nf">item</span><span class="p">()</span><span class="si">}</span><span class="sh">"</span><span class="p">,</span> <span class="n">fontsize</span><span class="o">=</span><span class="mi">16</span><span class="p">)</span>
        <span class="n">ax</span><span class="p">.</span><span class="nf">axis</span><span class="p">(</span><span class="sh">'</span><span class="s">off</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">plt</span><span class="p">.</span><span class="nf">show</span><span class="p">();</span>
    
    <span class="n">fig</span><span class="p">.</span><span class="nf">set_constrained_layout_pads</span><span class="p">(</span><span class="n">w_pad</span><span class="o">=</span><span class="mf">0.01</span><span class="p">,</span> <span class="n">h_pad</span><span class="o">=</span><span class="mf">0.01</span><span class="p">)</span>
    <span class="n">fig</span><span class="p">.</span><span class="nf">savefig</span><span class="p">(</span><span class="n">path</span> <span class="o">+</span> <span class="sa">f</span><span class="sh">'</span><span class="s">example_image_</span><span class="si">{</span><span class="n">data</span><span class="si">}</span><span class="s">.png</span><span class="sh">'</span><span class="p">,</span> <span class="n">bbox_inches</span><span class="o">=</span><span class="sh">'</span><span class="s">tight</span><span class="sh">'</span><span class="p">,</span> <span class="n">dpi</span><span class="o">=</span><span class="n">dpi</span><span class="p">)</span>
</code></pre></div></div>

<p>I won’t belabor the type hinting and docstring, but I do want to point out something we didn’t seen in the previous examples, which is <code class="language-plaintext highlighter-rouge">loader: tc.utils.data.DataLoader</code>. This way of type hinting happens when the object being passed into the function is not some straighforward data type like an integer or float. In this case, you can simply write what one would write if they want to import that object. Here, I have already imported <code class="language-plaintext highlighter-rouge">torch</code> as <code class="language-plaintext highlighter-rouge">tc</code> so I simply need to complete the path to import <code class="language-plaintext highlighter-rouge">DataLoader</code> and use that as the type hint.</p>

<p>Let’s jump to the code portion since we have pretty much already discussed type hinting and docstrings. The function is written in such a way as to allow someone to pass in grayscale or color images. This can be seen from the following line: <code class="language-plaintext highlighter-rouge">if img.shape[-1] == 1:</code>. This if-statement checks if the image is grayscale, and, if so, it squeezes the single channel input portion so that a 2D image can be plotted. If the last element size is not $1$, there is no squeezing and the plots are shown in color with the $3$ input channels. The final line saves the plot to a folder and appropriately labels it based on the data that was passed in.</p>

<p>Summary:<br />
So we’ve gone over quite a bit in this blog post. We first discussed how to save parameters in a <code class="language-plaintext highlighter-rouge">yaml</code> file, how to import them into the workspace, and how to turn them into attributes of an object. We went over type hinting and writing docstrings for functions and classes, as well as implementing a machine learning pipeline for easier project deployment. To be sure we did not introduce any errors in the code, we ran the same example as in the previous week and got the same outputs, which means our refactoring did not introduce any bugs. Overall, we’ve managed to take a specific problem and create a pipeline that can then be applied to a wide variety of datasets.</p>

<p>The code presented and plots displayed in the Convolutional neural network with the MNIST dataset posts can be found on my Github under the <a href="https://github.com/AJG91/build-cnn" title="build-cnn-repo">build-cnn</a> repo <a class="citation" href="#Garcia2025BuildCNN">[1]</a>. Feel free to reach out if you have any questions about what we covered this week. I will take a break the next few weeks and come back with hyperparameter tuning and model debugging. Stay tuned!</p>

<ol class="bibliography"><li><span id="Garcia2025BuildCNN">[1] Alberto J. Garcia, <i>Build CNN</i>, 2025. Available at: https://github.com/AJG91/build-cnn.</span></li></ol>
<p><a href="/" class="btn">⬅ Back to Home</a></p>]]></content><author><name></name></author><category term="machine-learning" /><summary type="html"><![CDATA[September 14, 2025 Last week we went over how to construct a convolutional neural network (CNN) and applied the model to the MNIST dataset. This week I will show you how to modify the code to create a machine learning pipeline and well-documented. As before, here is the documentation for the function that we will be using to load the MNIST dataset. The process of refactoring and creating a pipeline is important because, if done correctly, it can save time and reduce errors. A code pipeline creates an automated and efficient system. By standardizing the workflow, the amount of errors will be reduced and, if there is an error, it will be easier to pinpoint and fix. It also makes starting future projects easier by allowing the use of the different components of the pipeline along different projects, thus speeding up the coding process and not reinventing the wheel. Another key feature of creating code pipelines is that it makes it simple to try different algorithms, hyperparameters, or preprocessing steps by keeping the workflow consistent. The importance of documenting your code is something that cannot be stressed enough. Oftentimes, one finds that they come back to different pieces of code that they wrote a few months and have to take time to figure out what they did, which can be time consuming. The lack of documentation can be even more stressful when working with other people’s code that isn’t documented. Today I will show you how I document my code. I will also show a cool way to store parameters. I will be using the same CNN, same dataset, and same random seed to make sure that our changes do not affect the output. First, let’s look at how to store parameters. I will present one way, but there are many different ways. In my case, I use the function SimpleNamespace and a yaml file. Here’s the yaml file I am using in this particular case: path: '../outputs/' seed: 42 split_size: 0.1 batch_size: 64 learning_rate: 0.001 epochs: 3 input_size: 1 output_size: 10 hidden_size: 128 The first thing we see is the path where we will be outputting the plots to. We also have the seed to seed all random number generators. The next batch are parameters used for splitting the dataset, the learning rate for the optimizer, and the number of times we will train the model. The last batch of parameters indicate the different sizes that we need to build our CNN. from types import SimpleNamespace dpi = 400 dataset = 'mnist' config = load_config(path=f'../config_{dataset}.yaml') cfg = SimpleNamespace(**config) print(json.dumps(vars(cfg), indent=2)) set_seed(cfg.seed) device = "cuda" if tc.cuda.is_available() else "cpu" We load the yaml file into the variable config as a dictionary. This variable is then passed into the function SimpleNamespace. The double asterisk unpacks the key-value pairs as keyword arguments into the variable cfg, allowing us to call the parameters as attributes of cfg. This is pretty neat! Now all the parameters are stored in one place. All you have to do is call them from cfg as is shown in the function call set_seed(cfg.seed). We also specify the device that will be used, writing the code in such a way so that CUDA is the desired choice if available. Next, we initialize the model, optimizer, loss function, and get our train, validation, and test dataloaders. cnn = SimpleCNN(cfg.input_size, cfg.output_size, cfg.hidden_size).to(device) optimizer = optim.AdamW(cnn.parameters(), lr=cfg.learning_rate) loss_fn = nn.CrossEntropyLoss() train, val, test = get_dataloaders(dataset, cfg.split_size, cfg.batch_size, cfg.seed) Overall, this should be self-explanatory since it’s the same process as shown in last week’s post, but now we have a function for getting the dataloaders. Let’s look at this function closely. I will use this function and the subsequent classes used in get_dataloaders to explain creating a machine learning pipeline and documenting the code. I won’t go over every detail, but will focus on what I feel is important to understand. I will start out by showing each individual component of get_dataloaders, then show the full function at the end. For subsequent classes, I will only show the relevant subset of the class. Let’s get started: def get_dataloaders( dataset: str, split_size: float = 0.1, batch_size: int = 64, seed: int = 42, **kwargs ) -&gt; Tuple[DataLoader, DataLoader, DataLoader]: First, let’s look at the header. If you contrast the inputs with my previous codes, you see that I now show the data types of the inputs. This is called type hinting. It allows one to quickly understand what kind of inputs the function expects. For example, I can have a function header such as def add(x, a, b): While we can probably guess that we are adding numbers, what if the function looked like the following: def add(x, a, b): return x[a] + x[b] if we would have passed floats into a and b, it would have resulted in an error. Imagine instead we wrote: def add(x: numpy.ndarray, a: int, b: int) -&gt; float: return x[a] + x[b] Now we know that x is a numpy array, a is an integer, b is an integer, and the resulting output is a floating point. While we may still need to look at the documentation to get more information on the function, at least we wouldn’t have made the initial error of passing in floats. Looking back at the get_dataloaders function, we see that dataset is a string. If there was no type hinting we might have thought it was the actual dataset that was being passed into the function. We can also see that split_size is a float and has a default value, and batch_size and seed are integers with default values, as well. The expected output of the function is also shown and is a tuple composed of three dataloaders. At a glance, we immediately have more information about the function compared to if it did not have type hinting. From here, we look at the docstring, which is the documentation: """ Create PyTorch DataLoaders for training, validation, and test sets. Parameters ---------- dataset : str Name of the dataset to load. split_size : float, optional (default=0.1) The fraction of data that will be split off for the validation/test set. batch_size : int, optional (default=64) Number of samples per batch. seed : int, optional (default=42) Random seed for reproducible splitting of the dataset. **kwargs Additional keyword arguments. Returns ------- train_loader : DataLoader DataLoader for the training set, with shuffling enabled. val_loader : DataLoader DataLoader for the validation set, without shuffling. test_loader : DataLoader DataLoader for the test set, without shuffling. """ Basic documentation is broken down into three parts: description, inputs, and outputs. One can get more sophisticated with the docstring if one wishes, but let’s keep it basic for now. The description of this function is short and to the point. The function creates PyTorch DataLoaders for training, validation, and test sets. The input section is given by “Parameters” and shows the data type and gives a short description of each input variable and whether or not they have a default value (find optional in docstring). Here, I don’t have arrays (or tensors), but if one does it is always good to include the expected shape of the array in the short description. The final section is given by “Returns” and displays the outputs of the function with the data type and short description of each output variable just like with the inputs. At this point we know exactly what the function does, the different data types involved, and the inputs and outputs of the function. Now, all that’s left is the code itself so that we can capture all the details of the function. data = PreprocessDataset(dataset, split_size, rand_seed=seed, **kwargs) return ( DataLoader(TensorDataset(data.X_train, data.y_train), batch_size=batch_size, shuffle=True), DataLoader(TensorDataset(data.X_val, data.y_val), batch_size=batch_size), DataLoader(TensorDataset(data.X_test, data.y_test), batch_size=batch_size) ) Here, we see that we call PreprocessDataset with the dataset, which returns an object data with the training, validation, and test data as attributes. These attributes are then passed into TensorDataset and DataLoader like in my previous post. The order of the dataloaders in the output tuple are (train, validation, test). Combining the header, docstring, and code gives us the full function: def get_dataloaders( dataset: str, split_size: float = 0.1, batch_size: int = 64, seed: int = 42, **kwargs ) -&gt; Tuple[DataLoader, DataLoader, DataLoader]: """ Create PyTorch DataLoaders for training, validation, and test sets. Parameters ---------- dataset : str Name of the dataset to load. split_size : float, optional (default=0.1) The fraction of data that will be split off for the validation/test set. batch_size : int, optional (default=64) Number of samples per batch. seed : int, optional (default=42) Random seed for reproducible splitting of the dataset. **kwargs Additional keyword arguments. Returns ------- train_loader : DataLoader DataLoader for the training set, with shuffling enabled. val_loader : DataLoader DataLoader for the validation set, without shuffling. test_loader : DataLoader DataLoader for the test set, without shuffling. """ data = PreprocessDataset(dataset, split_size, rand_seed=seed, **kwargs) return ( DataLoader(TensorDataset(data.X_train, data.y_train), batch_size=batch_size, shuffle=True), DataLoader(TensorDataset(data.X_val, data.y_val), batch_size=batch_size), DataLoader(TensorDataset(data.X_test, data.y_test), batch_size=batch_size) ) It’s definitely more verbose than if I just included the code portion, but there is now so much information embedded in it that will be useful when it comes time to use this function! Let’s now take a look at a subset of the class PreprocessDataset: class PreprocessDataset(Dataset): """ Preprocess a dataset and provide train, validation, and test splits as PyTorch tensors. Attributes ---------- X_train : tc.Tensor Training input features. y_train : tc.Tensor Training labels. X_val : tc.Tensor Validation input features. y_val : tc.Tensor Validation labels. X_test : tc.Tensor Test input features. y_test : tc.Tensor Test labels. Parameters ---------- dataset : str Name of the dataset to load. Options: 'MNIST', 'MEDMNIST' split_size : float, optional (default=0.1) The fraction of data that will be split off for the validation/test set. rand_seed : int, optional (default=42) Random seed for reproducible splitting. **kwargs Additional keyword arguments. """ def __init__( self, dataset: str, split_size: float = 0.1, rand_seed: int = 42, **kwargs ): loader = LoadDatasets(dataset, **kwargs) X, y = loader.X / 255.0, loader.y X_train, X_val, y_train, y_val = self._split_dataset(X, y, split_size, rand_seed) X_train, X_test, y_train, y_test = self._split_dataset(X_train, y_train, split_size, rand_seed) self.X_train, self.y_train = X_train, y_train self.X_val, self.y_val = X_val, y_val self.X_test, self.y_test = X_test, y_test def _split_dataset( self, X: tc.Tensor, y: tc.Tensor, split_size: float, rand_seed: int ) -&gt; Tuple[tc.Tensor, tc.Tensor, tc.Tensor, tc.Tensor]: """ Split the dataset into train/test or train/validation subsets. Parameters ---------- X : tc.Tensor Input features. y : tc.Tensor Target labels. split_size : float The fraction of data that will be split off for the validation/test set. rand_seed : int Random seed for reproducibility. Returns ------- X_train : tc.Tensor Features of the first split. X_test : tc.Tensor Features of the second split. y_train : tc.Tensor Labels of the first split. y_test : tc.Tensor Labels of the second split. """ return train_test_split(X, y, test_size=split_size, random_state=rand_seed) The main docstring for a class documents the constructor. Then, each individual function has it’s own docstring. Looking at the constructor docstring, you will see that we have now included “Attributes”. These are instance attributes that belong to the class objects. This essentially means you can do things like: data = PreprocessDataset('mnist') X_train, y_train = data.X_train, data.y_train where data is the object and X_train and y_train are the attributes. In the case of this class, we have six instance attributes, which are our data sets. The docstring for these attributes is written in the same way as “Parameters” and “Returns”*. Note that the docstring says that the variable datasets can either be ‘MNIST’ or ‘MEDMNIST’. More about this later. One can also choose to give a brief summary for each method in the class in the constructor docstring, but I will not be doing this. In the constructor, we instance an object loader from the LoadDatasets class (which we will see in a bit). The instance attributes tied to the loader object are the x and y datasets. We then use these and call the method _split_dataset, which will now give us our train, validation, and test sets. The underscore in the method name indicates that this is meant to be an internal method that should not be accessed from outside the class. Python does not enforce this so it can still be called from outside the class if you so wish, but if you find yourself purposely writing underscores to call methods outside of the class constructor or other methods you may want to rethink your coding pipeline. For the last part of the constructor, we turn the different data sets into instance attributes. This is easily done by writing self.X_train = X_train for all variables you want to save. At this point you may be thinking to yourself, “This is so complicated!! Why are we doing all this if our previous code, without functions and classes, worked just fine?” As Yoda would say, “patience you must have, my young Padawan.” Let’s look at the last class, LoadDatasets and hopefully everything becomes clear. class LoadDatasets(): """ Load and return datasets as PyTorch tensors. Attributes ---------- dataset : str Name of the dataset to load (e.g., 'MNIST', 'MEDMNIST'). X : tc.Tensor Input features of the dataset as a PyTorch tensor. shape: (num_samples, channels, height, width) y : tc.Tensor Target labels as a PyTorch tensor. Parameters ---------- dataset : str The name of the dataset to load. Options: 'MNIST' or 'MEDMNIST' **kwargs Additional keyword arguments passed to the specific dataset loader methods. Raises ------ ValueError If an unsupported dataset name is provided. """ def __init__(self, dataset: str, **kwargs): self.dataset = dataset.upper() self.loaders = { 'MNIST': lambda: self.load_mnist(**kwargs), 'MEDMNIST': lambda: self.load_medmnist(**kwargs), } if self.dataset not in self.loaders: raise ValueError(f'Dataset not supported -&gt; {dataset}. ' f'Available: {list(self.loaders.keys())}') self.X, self.y = self.loaders[self.dataset]() def load_mnist(self, version: int = 1) -&gt; Tuple[tc.Tensor, tc.Tensor]: """ Load the MNIST dataset and convert it to PyTorch tensors. Parameters ---------- version : int, optional (default=1) The version of the MNIST dataset from OpenML. Returns ------- X : tc.Tensor MNIST images as a float32 tensor normalized to [0, 1]. Shape: (num_samples, 1, 28, 28) y : tc.Tensor MNIST labels as int64 tensor. Shape: (num_samples,) """ from sklearn.datasets import fetch_openml X, y = fetch_openml('mnist_784', version=version, return_X_y=True, as_frame=False) X = tc.tensor(X, dtype=tc.float32) y = tc.from_numpy(y.astype('int64')) return X.view(-1, 1, 28, 28), y def load_medmnist(self, flag: str = 'bloodmnist') -&gt; Tuple[tc.Tensor, tc.Tensor]: """ Load a MedMNIST dataset and convert it to PyTorch tensors. Parameters ---------- flag : str, optional (default='bloodmnist') Specifies which MedMNIST dataset to load. Must be one of the keys in `medmnist.INFO`. Returns ------- X : tc.Tensor MedMNIST images as a float32 tensor normalized to [0, 1]. Shape: (num_samples, channels, height, width) y : tc.Tensor MedMNIST labels as long tensors. Shape: (num_samples,) Raises ------ ValueError If the specified `flag` does not correspond to a valid MedMNIST dataset. """ import medmnist from medmnist import INFO if flag not in INFO: raise ValueError(f'Invalid MedMNIST flag -&gt; {flag}. ' f'Available datasets: {list(INFO.keys())}"') print(INFO[flag]['label']) DataClass = getattr(medmnist, INFO[flag]['python_class']) train_ds = DataClass(split='train', download=True) val_ds = DataClass(split='val', download=True) test_ds = DataClass(split='test', download=True) X = np.concatenate([train_ds.imgs, val_ds.imgs, test_ds.imgs], axis=0) y = np.concatenate([train_ds.labels, val_ds.labels, test_ds.labels], axis=0) X = tc.tensor(X, dtype=tc.float32) y = tc.tensor(y).long().squeeze() return X.permute(0, 3, 1, 2), y In the docstring for this class I have included yet another section, “Raises”. By seeing this we know that somewhere in the constructor there will be a piece of code that will potentially raise an error if something is wrong. The docstring says that this will happen if an unsupported dataset name is provided. Aha! So If I pass in an incorrect string or a string that does not match a known dataset in the class an error will be raised. Looking at the constructor, we see that the dataset string is capitalized. This is to prevent case sensitivity raising an error. The dataset name is passed into a dictionary that calls the method that corresponds to the dataset being asked for. We also see that the methods are called via lambda function. The reason is so that if we have parameters that we need to pass into the dataset methods we can do it as a keyword argument (a.k.a. kwargs). For example, take MEDMNIST, which is a library of medical datasets. An input to the method is the specific MEDMNIST dataset that we want. In this case, the default option is “bloodmnist”. If instead we wanted ChestMNIST, then we pass in “chestmnist” as a keyword argument into get_dataloaders. If no keyword arguments are passed in, then the lambda function is treated as if it was a regular method call. So let’s review the function and classes used for obtaining the dataloaders. The get_dataloaders function calls the PreprocessDataset class. This class is solely used for grabbing the already loaded features and targets, normalizing, and splitting them into train, validation, and test sets. The PreprocessDataset class calls the LoadDatasets class, which is solely used for loading the features and targets from their respective Python package. The classes do not care about the inner workings of each other. PreprocessDataset only cares that there is a class with the feature and target variables as attributes. On the other hand, LoadDatasets can be called independently from everything else if one only wants the feature and target variables. This way of modularizing code helps once projects start getting big and pipelines get complicated. It keeps everything in such a way so that you can swap out functions or classes with ease. You don’t have everything tangled up together, which can result in headaches when making changes. Another cool thing is that adding datasets to LoadDatasets is a breeze. All you have to do is write a function in the class that outputs the feature and target variables with the shape $\text{X.shape} = (\text{num_samples}, \text{channels}, \text{height}, \text{width})$ and $\text{y.shape} = (\text{num_samples},)$ and add the dataset to the dictionary in the constructor. From there, getting the dataloaders for this new dataset is as easy as calling get_dataloaders and passing in the appropriate name. You don’t have to copy over you preprocessing pipeline or the train_test_split function. I won’t go over the functions train_model and evaluate_model since they are pretty self-explanatory and the bulk of it was covered last week. What I do want to show are the train, validation, and test results: Epoch 1, Train Accuracy: 0.88814815, Validation Accuracy: 0.94385714 Epoch 2, Train Accuracy: 0.95839506, Validation Accuracy: 0.96385714 Epoch 3, Train Accuracy: 0.97007055, Validation Accuracy: 0.96928571 Test Accuracy: 0.97190476 We note that they are the same as last week! This means that our code refactoring did not fundamentally change anything in the model, which is great validation that we did not introduce a bug into the code. The misclassified images are also the same: which is to be expected since we are seeding all the random number generators. All is well. The cool thing is that all the training and evaluation code is agnostic to the dataset, which means that you can use it on other datasets with very minimal tweaks! The functions used to produce the plots are also written in such a way as to allow flexibility when using different datasets. I will only go over one of the functions: def plot_images( loader: tc.utils.data.DataLoader, path: str, dpi: int, data: str, size: list = [2, 5], figsize: Tuple = (14, 6), cmap: str = None ) -&gt; None: """ Plots a subset of the dataset images. Parameters ---------- loader : tc.utils.data.DataLoader DataLoader for the training set. path : str String to the directory where the plot is saved. dpi : int Dots per inch. A higher dpi results in a sharper image. data : str The name of the dataset being plotted. This is used to label the plot when saving to directory. size : list, optional (default=[2, 5]) Specifies the number of rows and columns. figsize : Tuple, optional (default=[14, 6]) Specifies the figure size. cmap : str, optional (default=None) Colormap for 2D images. """ images, labels = next(iter(loader)) fig, axes = plt.subplots(size[0], size[1], figsize=figsize) for i, ax in enumerate(axes.flat): img = images[i].permute(1, 2, 0) if img.shape[-1] == 1: img = img.squeeze() cmap = 'gray' ax.imshow(img, cmap=cmap) ax.set_title(f"Label: {labels[i].item()}", fontsize=16) ax.axis('off') plt.show(); fig.set_constrained_layout_pads(w_pad=0.01, h_pad=0.01) fig.savefig(path + f'example_image_{data}.png', bbox_inches='tight', dpi=dpi) I won’t belabor the type hinting and docstring, but I do want to point out something we didn’t seen in the previous examples, which is loader: tc.utils.data.DataLoader. This way of type hinting happens when the object being passed into the function is not some straighforward data type like an integer or float. In this case, you can simply write what one would write if they want to import that object. Here, I have already imported torch as tc so I simply need to complete the path to import DataLoader and use that as the type hint. Let’s jump to the code portion since we have pretty much already discussed type hinting and docstrings. The function is written in such a way as to allow someone to pass in grayscale or color images. This can be seen from the following line: if img.shape[-1] == 1:. This if-statement checks if the image is grayscale, and, if so, it squeezes the single channel input portion so that a 2D image can be plotted. If the last element size is not $1$, there is no squeezing and the plots are shown in color with the $3$ input channels. The final line saves the plot to a folder and appropriately labels it based on the data that was passed in. Summary: So we’ve gone over quite a bit in this blog post. We first discussed how to save parameters in a yaml file, how to import them into the workspace, and how to turn them into attributes of an object. We went over type hinting and writing docstrings for functions and classes, as well as implementing a machine learning pipeline for easier project deployment. To be sure we did not introduce any errors in the code, we ran the same example as in the previous week and got the same outputs, which means our refactoring did not introduce any bugs. Overall, we’ve managed to take a specific problem and create a pipeline that can then be applied to a wide variety of datasets. The code presented and plots displayed in the Convolutional neural network with the MNIST dataset posts can be found on my Github under the build-cnn repo [1]. Feel free to reach out if you have any questions about what we covered this week. I will take a break the next few weeks and come back with hyperparameter tuning and model debugging. Stay tuned! [1] Alberto J. Garcia, Build CNN, 2025. Available at: https://github.com/AJG91/build-cnn. ⬅ Back to Home]]></summary></entry><entry><title type="html">Convolutional neural networks: MNIST dataset</title><link href="https://ajg91.github.io//posts/machine-learning/convolutional-neural-network-with-mnist-dataset/" rel="alternate" type="text/html" title="Convolutional neural networks: MNIST dataset" /><published>2025-09-07T00:00:00-07:00</published><updated>2025-09-07T00:00:00-07:00</updated><id>https://ajg91.github.io//posts/machine-learning/convolutional-neural-network-with-mnist-dataset</id><content type="html" xml:base="https://ajg91.github.io//posts/machine-learning/convolutional-neural-network-with-mnist-dataset/"><![CDATA[<p class="post-date">September 07, 2025</p>

<p>Today we will learn about convolutional neural networks (CNNs). A CNN is a neural network designed to process grid-like data, such as images. It works by applying a convolution, filters (usually small $n \times n$ matrices) that slide over the data to extract useful features, across the 2D grid to detect features such as edges and shapes. We can apply these neural networks to images so that the network can learn how to classify images. In this post, we will construct a CNN and apply it to preprocessed images known as the MNIST dataset.</p>

<p>What is MNIST?<br />
MNIST stands for Modified National Institute of Standards and Technology, which is the institute that first created this dataset. The dataset is composed of thousands of grayscale images of handwritten numbers from $0$ to $9$ (ten categories). It is a very popular dataset used when one is first learning about CNNs since it is simple and easy to understand. The documentation for the function that we will be using to load the MNIST dataset can be found <a href="https://www.openml.org/search?type=data&amp;sort=runs&amp;id=554&amp;status=active" title="MNIST-docs">here</a>. Here is a plot of a small subset of the dataset:</p>

<p><img src="/assets/images/build_cnn/example_image_mnist.png" alt="mnist_example" /></p>

<p>Let’s jump into creating the model.</p>

<p>First, we must import the data. I used the MNIST dataset provided by Scikit-learn:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.datasets</span> <span class="kn">import</span> <span class="n">fetch_openml</span>

<span class="n">mnist</span> <span class="o">=</span> <span class="nf">fetch_openml</span><span class="p">(</span><span class="sh">'</span><span class="s">mnist_784</span><span class="sh">'</span><span class="p">,</span> <span class="n">version</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
<span class="n">X</span> <span class="o">=</span> <span class="n">mnist</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="n">values</span><span class="p">.</span><span class="nf">astype</span><span class="p">(</span><span class="sh">'</span><span class="s">float32</span><span class="sh">'</span><span class="p">)</span> <span class="o">/</span> <span class="mf">255.0</span>
<span class="n">y</span> <span class="o">=</span> <span class="n">mnist</span><span class="p">.</span><span class="n">target</span><span class="p">.</span><span class="nf">astype</span><span class="p">(</span><span class="sh">'</span><span class="s">int64</span><span class="sh">'</span><span class="p">)</span>
</code></pre></div></div>

<p>The $255$ is used to normalize the dataset between $0$ and $1$. This comes from the fact that a single byte ($8$ bits) can store $2^8=256$ different values. For images, this means there are $256$ different shades per each color channel. Note that we divide by $255$ instead of $256$ because Python starts at $0$ instead of $1$.</p>

<p>We must now split this dataset into a training set, validation set, and testing set. The training set will be used to train the model, the validation set is used to see how well the model has generalized at each epoch, and the testing set is used at the end to see how well the fully trained model can predict on a new dataset. While not everyone uses a validation set, it is good to have since it tells us how well our model is generalizing after each epoch. This will tell us at a glance whether or not our model is overfitting as it learns more.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Train/validation split
</span><span class="n">X_train</span><span class="p">,</span> <span class="n">X_val</span><span class="p">,</span> <span class="n">y_train</span><span class="p">,</span> <span class="n">y_val</span> <span class="o">=</span> <span class="nf">train_test_split</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">test_size</span><span class="o">=</span><span class="mf">0.1</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>
<span class="n">X_train</span><span class="p">,</span> <span class="n">X_test</span><span class="p">,</span> <span class="n">y_train</span><span class="p">,</span> <span class="n">y_test</span> <span class="o">=</span> <span class="nf">train_test_split</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">,</span> <span class="n">test_size</span><span class="o">=</span><span class="mf">0.1</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>
</code></pre></div></div>

<p>The function <code class="language-plaintext highlighter-rouge">train_test_split()</code> only splits data into two sets so we pass in the data twice to get all three datasets. The overall split is going to be $80\%$ training set, $10\%$ validation set, and $10\%$ testing set. We initialize the random_state so that the results are reproducible.</p>

<p>We also have to convert to PyTorch tensors and reshape the $X$ datasets so that the CNN can accept the data. The input expected for CNNs are $(N, C, H, W)$ where $N$ is batch size (number of images in a batch), $C$ is number of channels (for grayscale this is $1$), $H$ is the height of the image and $W$ the width. Our reshape will put the data into this format. This looks like so:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># # Convert to PyTorch tensors
</span><span class="n">X_train</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">X_train</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">28</span><span class="p">,</span> <span class="mi">28</span><span class="p">)</span>
<span class="n">y_train</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">y_train</span><span class="p">.</span><span class="n">values</span><span class="p">)</span>
<span class="n">X_val</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">X_val</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">28</span><span class="p">,</span> <span class="mi">28</span><span class="p">)</span>
<span class="n">y_val</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">y_val</span><span class="p">.</span><span class="n">values</span><span class="p">)</span>
<span class="n">X_test</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">X_test</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">28</span><span class="p">,</span> <span class="mi">28</span><span class="p">)</span>
<span class="n">y_test</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">y_test</span><span class="p">.</span><span class="n">values</span><span class="p">)</span>

<span class="c1"># # DataLoaders
</span><span class="n">train_loader</span> <span class="o">=</span> <span class="nc">DataLoader</span><span class="p">(</span><span class="nc">TensorDataset</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">),</span> <span class="n">batch_size</span><span class="o">=</span><span class="mi">64</span><span class="p">,</span> <span class="n">shuffle</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="n">val_loader</span> <span class="o">=</span> <span class="nc">DataLoader</span><span class="p">(</span><span class="nc">TensorDataset</span><span class="p">(</span><span class="n">X_val</span><span class="p">,</span> <span class="n">y_val</span><span class="p">),</span> <span class="n">batch_size</span><span class="o">=</span><span class="mi">64</span><span class="p">)</span>
<span class="n">test_loader</span> <span class="o">=</span> <span class="nc">DataLoader</span><span class="p">(</span><span class="nc">TensorDataset</span><span class="p">(</span><span class="n">X_test</span><span class="p">,</span> <span class="n">y_test</span><span class="p">),</span> <span class="n">batch_size</span><span class="o">=</span><span class="mi">64</span><span class="p">)</span>
</code></pre></div></div>

<p>The last step above is to set up the data loaders. First, <code class="language-plaintext highlighter-rouge">TensorDataset()</code> is a function that pairs the features $X$ with the labels $y$ in a tuple. The function <code class="language-plaintext highlighter-rouge">DataLoader()</code> then provides an iterator that iterates over minibatches of the dataset with the batch size, in this case, being $64$. We also shuffle the training data so that the model doesn’t learn the ordering and can generalize better.</p>

<p>Now that we have taken care of the dataset, let’s move on to the actual CNN:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">SimpleCNN</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">conv</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Conv2d</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">32</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="n">padding</span><span class="o">=</span><span class="mi">1</span><span class="p">),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">MaxPool2d</span><span class="p">(</span><span class="mi">2</span><span class="p">),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Conv2d</span><span class="p">(</span><span class="mi">32</span><span class="p">,</span> <span class="mi">64</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="n">padding</span><span class="o">=</span><span class="mi">1</span><span class="p">),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">MaxPool2d</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">fc</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Flatten</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">64</span><span class="o">*</span><span class="mi">7</span><span class="o">*</span><span class="mi">7</span><span class="p">,</span> <span class="mi">128</span><span class="p">),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">128</span><span class="p">,</span> <span class="mi">10</span><span class="p">)</span>
        <span class="p">)</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">conv</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">fc</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
</code></pre></div></div>

<p>Let’s unpack the class. First, we pass in <code class="language-plaintext highlighter-rouge">nn.Module</code> as an argument into the class and write <code class="language-plaintext highlighter-rouge">super().__init__()</code> in the constructor. This is called inheritance and ensures that all the functions normally available in the parent class <code class="language-plaintext highlighter-rouge">nn.Module</code> are also available in the child class, which is our <code class="language-plaintext highlighter-rouge">define_CNN()</code> class. In the constructor you will see <code class="language-plaintext highlighter-rouge">nn.Sequential()</code>, which is a container module that lets you build neural networks by stacking layers and activation functions sequentially. This container module is used in two different places, to define the CNN <code class="language-plaintext highlighter-rouge">self.conv</code> and the multi-layer perception (MLP) <code class="language-plaintext highlighter-rouge">self.fc</code>. The reason we define both a CNN and a MLP is because the CNN’s sole focus is to learn the features, while the MLP is supposed to take those features and map them to a final output (category). In this case, the CNN is known as the backbone and the MLP as the neck.</p>

<p>You may ask, “Why separate them?” Here are some reasons why we may want to build a model with a separate backbone and head:<br /></p>
<ol>
  <li>Modularity:<br />
    <ul>
      <li>You can change the convolutional part without touching the fully connected layers, or vice versa.<br /></li>
      <li>Example: Add more convolutional layers, or replace <code class="language-plaintext highlighter-rouge">self.fc</code> with a different classifier.</li>
    </ul>
  </li>
  <li>Conceptual clarity:<br />
    <ul>
      <li><code class="language-plaintext highlighter-rouge">self.conv</code> –&gt; extract patterns<br /></li>
      <li><code class="language-plaintext highlighter-rouge">self.fc</code> –&gt; decide which class based on patterns<br /></li>
    </ul>
  </li>
  <li>Industry standard:<br />
    <ul>
      <li>Most CNNs follow this “feature extractor + classifier” design, which makes it easier to swap backbones or use pretrained models.</li>
    </ul>
  </li>
</ol>

<p>Let’s look at the model and break down each layer:</p>

<p>The convolutional neural network: <br /></p>
<ol>
  <li><code class="language-plaintext highlighter-rouge">nn.Conv2d(1, 32, 3, padding=1)</code><br />
    <ul>
      <li>This is a two-dimensional convolutional layer with $1$ input channel, $32$ output channels (learning $32$ features), and a convolutional kernel size of $3$. Finally, we zero pad with size $1$ to keep the spatial size of the image the same as the input since the function defaults to stride=$1$.<br /></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.ReLU()</code><br />
    <ul>
      <li>This is a Rectified Linear Unit activation function. It is defined as $\text{ReLU}(x) = \text{max}(0, x)$. Essentially, it zeros out the negative values while introducing non-linearity. This type of activation function is simple and fast to use compared to others such as $\text{tanh}(x)$ or sigmoid.<br /></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.MaxPool2d(2)</code><br />
    <ul>
      <li>This is a two-dimensional max pooling layer meant to reduce spatial dimensions while keeping important information. In this case, $2$ denotes that a $2 \times 2$ kernel is used. The max pooling layer will then take a $2 \times 2$ window and slide it over the feature map, taking the maximum value within the window and assigning it to the corresponding location in the output.</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.Conv2d(32, 64, 3, padding=1)</code><br />
    <ul>
      <li>This convolutional layer now has $32$ input channels, $64$ output channels, meaning it is meant to learn more finer details than the previous one since the outputs channels have now increased.<br /></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.ReLU()</code><br />
    <ul>
      <li>Same as before.<br /></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.MaxPool2d(2)</code><br />
    <ul>
      <li>Same as before.<br /></li>
    </ul>
  </li>
</ol>

<p>The multi-layer perceptron:<br /></p>
<ol>
  <li><code class="language-plaintext highlighter-rouge">nn.Flatten()</code><br />
    <ul>
      <li>This is used to flatten the input tensor. The output of the CNN is a four-dimensional tensor. Since the input to the MLP is the output of the CNN, the tensor has to be flatten.<br /></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.Linear(64*7*7, 128)</code><br />
    <ul>
      <li>This is a fully connected linear layer with input size $64*7*7$ (output of CNN) and output size of $128$ neurons.<br /></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.ReLU()</code><br />
    <ul>
      <li>Same as before.<br /></li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">nn.Linear(128, 10)</code><br />
    <ul>
      <li>This is another fully connected linear layer, but now the input size is $128$ and the output size is $10$, which matches how many categories we have ($0$-$9$).<br /></li>
    </ul>
  </li>
</ol>

<p>The last part of the class is the function <code class="language-plaintext highlighter-rouge">forward()</code> that calls the CNN, then passes the output of the CNN to the MLP so that the features can be mapped to a category.</p>

<p>Now, let’s see how the model is trained. First, we need to initialize the CNN, the optimizer, and the loss function:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cnn</span> <span class="o">=</span> <span class="nc">SimpleCNN</span><span class="p">()</span>
<span class="n">optimizer</span> <span class="o">=</span> <span class="n">optim</span><span class="p">.</span><span class="nc">AdamW</span><span class="p">(</span><span class="n">cnn</span><span class="p">.</span><span class="nf">parameters</span><span class="p">(),</span> <span class="n">lr</span><span class="o">=</span><span class="mf">0.001</span><span class="p">)</span>
<span class="n">loss_fn</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">CrossEntropyLoss</span><span class="p">()</span>
</code></pre></div></div>

<p>In this case, we will use the optimizer <code class="language-plaintext highlighter-rouge">optim.AdamW()</code> with an initial learning rate of $0.001$. This optimizer is a popular choice since it takes the fast convergence of Adam (another optimizer) and the generalization properties of SGD (optimizer we used last week) with weight decay. For more information on this optimizer see <a href="https://docs.pytorch.org/docs/stable/generated/torch.optim.AdamW.html" title="AdamW-docs">here</a>. The loss function we will be using is cross-entropy loss. This loss function measures the difference between two probability distributions and is widely used for classification problems. For more information on this loss function see <a href="https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html" title="Cross-entropy-docs">here</a>.</p>

<p>The training will look like the following:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">epoch</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">3</span><span class="p">):</span>
    <span class="n">cnn</span><span class="p">.</span><span class="nf">train</span><span class="p">()</span>
    <span class="n">correct</span><span class="p">,</span> <span class="n">total</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span>
    <span class="c1"># Trains over the training set
</span>    <span class="k">for</span> <span class="n">X</span><span class="p">,</span> <span class="n">y</span> <span class="ow">in</span> <span class="n">train_loader</span><span class="p">:</span>
        <span class="n">optimizer</span><span class="p">.</span><span class="nf">zero_grad</span><span class="p">()</span>
        <span class="n">output</span> <span class="o">=</span> <span class="nf">cnn</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>
        <span class="n">loss</span> <span class="o">=</span> <span class="nf">loss_fn</span><span class="p">(</span><span class="n">output</span><span class="p">,</span> <span class="n">y</span><span class="p">)</span>
        <span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">()</span>
        <span class="n">optimizer</span><span class="p">.</span><span class="nf">step</span><span class="p">()</span>
        
        <span class="n">preds</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">output</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">).</span><span class="nf">argmax</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">correct</span> <span class="o">+=</span> <span class="p">(</span><span class="n">preds</span> <span class="o">==</span> <span class="n">y</span><span class="p">).</span><span class="nf">sum</span><span class="p">().</span><span class="nf">item</span><span class="p">()</span>
        <span class="n">total</span> <span class="o">+=</span> <span class="n">y</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
    <span class="n">train_acc</span> <span class="o">=</span> <span class="n">correct</span> <span class="o">/</span> <span class="n">total</span>
    
    <span class="c1"># Evaluates over the validation set
</span>    <span class="n">cnn</span><span class="p">.</span><span class="nf">eval</span><span class="p">()</span> <span class="c1"># put model in evaluation mode
</span>    <span class="n">correct</span><span class="p">,</span> <span class="n">total</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span>
    <span class="k">with</span> <span class="n">tc</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span> <span class="c1"># no gradient tracking during evaluation
</span>        <span class="k">for</span> <span class="n">X</span><span class="p">,</span> <span class="n">y</span> <span class="ow">in</span> <span class="n">val_loader</span><span class="p">:</span>
            <span class="n">output</span> <span class="o">=</span> <span class="nf">cnn</span><span class="p">(</span><span class="n">X</span><span class="p">)</span> <span class="c1"># forward pass
</span>            <span class="n">preds</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">output</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">).</span><span class="nf">argmax</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
            <span class="n">correct</span> <span class="o">+=</span> <span class="p">(</span><span class="n">preds</span> <span class="o">==</span> <span class="n">y</span><span class="p">).</span><span class="nf">sum</span><span class="p">().</span><span class="nf">item</span><span class="p">()</span>
            <span class="n">total</span> <span class="o">+=</span> <span class="n">y</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
    <span class="n">val_acc</span> <span class="o">=</span> <span class="n">correct</span> <span class="o">/</span> <span class="n">total</span>

    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Epoch </span><span class="si">{</span><span class="n">epoch</span> <span class="o">+</span> <span class="mi">1</span><span class="si">}</span><span class="s">, 
            Train Accuracy: </span><span class="si">{</span><span class="n">train_acc</span><span class="si">:</span><span class="p">.</span><span class="mi">8</span><span class="n">f</span><span class="si">}</span><span class="s">, 
            Validation Accuracy: </span><span class="si">{</span><span class="n">val_acc</span><span class="si">:</span><span class="p">.</span><span class="mi">8</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>I won’t go over all the details since some of this code is present in last week’s post, so I will only go over the new additions. In the training portion, we now implement <code class="language-plaintext highlighter-rouge">tc.softmax()</code> on the outputs, which converts the output prediction to probabilities that overall sum to $1$. The method <code class="language-plaintext highlighter-rouge">.argmax(1)</code> finds the index of the maximum value of each row. In essence, we are picking out the prediction that has the highest probability of matching up to the true target. The next line calculates how many of our predictions are actually correct, while the line after find the total number of targets that the model is supposed to predict. With these values, we can calculate a percentage of correct predictions (given by train_acc).</p>

<p>Like I previously mentioned, we use a validation set to determine how well our model generalizes between epochs. The next part of the loop does exactly this. We first put the model in evaluation mode using <code class="language-plaintext highlighter-rouge">.eval()</code>. While in evalulation mode, the weights of the model are not changed with the new data and processes like dropout are deactivated. The rest of the loop is identical to the training portion, which is just calculating the percentage of correct predictions (given by val_acc) with the validation set. After both calculation, we print out the current epoch and the accuracies. The model is trained and the validation set accuracy computed for a total of three epochs. Once done training, our model is now full trained and ready to go.</p>

<p>The next step is to see how the fully trained model generalizes with unseen data. We will use the same method of calculating the validation accuracy with the test data since the point is to see how well the model generalizes to unknown data. An example of what the print statement output looks like is the following:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Epoch</span> <span class="mi">1</span><span class="p">,</span> <span class="n">Train</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.88814815</span><span class="p">,</span> <span class="n">Validation</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.94385714</span>
<span class="n">Epoch</span> <span class="mi">2</span><span class="p">,</span> <span class="n">Train</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.95839506</span><span class="p">,</span> <span class="n">Validation</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.96385714</span>
<span class="n">Epoch</span> <span class="mi">3</span><span class="p">,</span> <span class="n">Train</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.97007055</span><span class="p">,</span> <span class="n">Validation</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.96928571</span>
<span class="n">Test</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="mf">0.97190476</span>
</code></pre></div></div>

<p>These numbers will change when the training is reran since we are not seeding the random numbers in PyTorch.</p>

<p>We have now fully trained a model composed of a CNN for feature learning and a MLP for learning the mapping between the data and the targets. We also included an evaluation over a validation set to check how well the model is generalizing to unknown data as it is being trained, and an evaluation over a test set to check how well the full trained model performs on unseen data. Overall, the model seems to be very accurate with a test accuracy of $\approx 97 \%$. That being said, there is still $\approx 3 \%$ of the test data that is not being categorized correctly. Let’s see a subset of these predictions to try to understand what is going on.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">misclassified_images</span> <span class="o">=</span> <span class="p">[]</span>
<span class="n">misclassified_preds</span> <span class="o">=</span> <span class="p">[]</span>
<span class="n">true_labels</span> <span class="o">=</span> <span class="p">[]</span>

<span class="n">cnn</span><span class="p">.</span><span class="nf">eval</span><span class="p">()</span>
<span class="k">with</span> <span class="n">tc</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
    <span class="k">for</span> <span class="n">X</span><span class="p">,</span> <span class="n">y</span> <span class="ow">in</span> <span class="n">test_loader</span><span class="p">:</span>
        <span class="n">output</span> <span class="o">=</span> <span class="nf">cnn</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>
        <span class="n">preds</span> <span class="o">=</span> <span class="n">tc</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">output</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">).</span><span class="nf">argmax</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">mismatches</span> <span class="o">=</span> <span class="n">preds</span> <span class="o">!=</span> <span class="n">y</span>
        
        <span class="k">if</span> <span class="n">mismatches</span><span class="p">.</span><span class="nf">any</span><span class="p">():</span>
            <span class="n">misclassified_images</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="n">X</span><span class="p">[</span><span class="n">mismatches</span><span class="p">].</span><span class="nf">cpu</span><span class="p">())</span>
            <span class="n">misclassified_preds</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="n">preds</span><span class="p">[</span><span class="n">mismatches</span><span class="p">].</span><span class="nf">cpu</span><span class="p">())</span>
            <span class="n">true_labels</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="n">y</span><span class="p">[</span><span class="n">mismatches</span><span class="p">].</span><span class="nf">cpu</span><span class="p">())</span>
</code></pre></div></div>

<p>Evaluating the misclassified images is similar to the evaluation block explained before. One of the differences is that we add <code class="language-plaintext highlighter-rouge">mismatches = preds != y</code>, which saves all the predictions that do not match up to the ground truth. We also include an if-statement that checks for any mismatches and, if found, saves the image, prediction, and ground truth. The method <code class="language-plaintext highlighter-rouge">.cpu()</code> moves the tensor from the GPU to the CPU. In this case, we don’t need to put it since we are training exclusively on the CPU, but it’s something to keep in mind. Plotting the misclassified images, we get the following:</p>

<p><img src="/assets/images/build_cnn/misclassified_images_mnist.png" alt="mnist_misclassified" /></p>

<p>From the image above, we can begin to understand why the model is misclassifying some numbers. The number in the upper left is supposed to be a $7$, but it was written in such a way that it can potentially be confused with a $9$. The number in the upper right is supposed to be an $8$, but it really looks like a $9$. The number $2$ in the bottom row does look like a $2$, but it’s understandable that the model may confuse it with an $8$ since the upper and lower loops look like what someone would do to write an $8$. With the subset of misclassified images we have plotted, it makes sense that the model may struggle with these cases, especially since we only trained it using a small CNN and three total epochs.</p>

<p>Overall, the model does a great job at correctly classifying the MNIST dataset, minus some small subset where the confusion can be attributed to messy numbers. As we saw, training a basic image recognition model is not difficult! Of course, these models are not perfect or fool-proof and with more complex images, we may need more layers, larger layers, or maybe more advanced machine learning techniques.</p>

<p>Feel free to reach out if you have any questions about what we covered this week. Next time, I will show you how to modularize and make improvements to the code. Stay tuned!</p>

<p><a href="/" class="btn">⬅ Back to Home</a></p>]]></content><author><name></name></author><category term="machine-learning" /><summary type="html"><![CDATA[September 07, 2025 Today we will learn about convolutional neural networks (CNNs). A CNN is a neural network designed to process grid-like data, such as images. It works by applying a convolution, filters (usually small $n \times n$ matrices) that slide over the data to extract useful features, across the 2D grid to detect features such as edges and shapes. We can apply these neural networks to images so that the network can learn how to classify images. In this post, we will construct a CNN and apply it to preprocessed images known as the MNIST dataset. What is MNIST? MNIST stands for Modified National Institute of Standards and Technology, which is the institute that first created this dataset. The dataset is composed of thousands of grayscale images of handwritten numbers from $0$ to $9$ (ten categories). It is a very popular dataset used when one is first learning about CNNs since it is simple and easy to understand. The documentation for the function that we will be using to load the MNIST dataset can be found here. Here is a plot of a small subset of the dataset: Let’s jump into creating the model. First, we must import the data. I used the MNIST dataset provided by Scikit-learn: from sklearn.datasets import fetch_openml mnist = fetch_openml('mnist_784', version=1) X = mnist.data.values.astype('float32') / 255.0 y = mnist.target.astype('int64') The $255$ is used to normalize the dataset between $0$ and $1$. This comes from the fact that a single byte ($8$ bits) can store $2^8=256$ different values. For images, this means there are $256$ different shades per each color channel. Note that we divide by $255$ instead of $256$ because Python starts at $0$ instead of $1$. We must now split this dataset into a training set, validation set, and testing set. The training set will be used to train the model, the validation set is used to see how well the model has generalized at each epoch, and the testing set is used at the end to see how well the fully trained model can predict on a new dataset. While not everyone uses a validation set, it is good to have since it tells us how well our model is generalizing after each epoch. This will tell us at a glance whether or not our model is overfitting as it learns more. # Train/validation split X_train, X_val, y_train, y_val = train_test_split(X, y, test_size=0.1, random_state=42) X_train, X_test, y_train, y_test = train_test_split(X_train, y_train, test_size=0.1, random_state=42) The function train_test_split() only splits data into two sets so we pass in the data twice to get all three datasets. The overall split is going to be $80\%$ training set, $10\%$ validation set, and $10\%$ testing set. We initialize the random_state so that the results are reproducible. We also have to convert to PyTorch tensors and reshape the $X$ datasets so that the CNN can accept the data. The input expected for CNNs are $(N, C, H, W)$ where $N$ is batch size (number of images in a batch), $C$ is number of channels (for grayscale this is $1$), $H$ is the height of the image and $W$ the width. Our reshape will put the data into this format. This looks like so: # # Convert to PyTorch tensors X_train = tc.tensor(X_train).view(-1, 1, 28, 28) y_train = tc.tensor(y_train.values) X_val = tc.tensor(X_val).view(-1, 1, 28, 28) y_val = tc.tensor(y_val.values) X_test = tc.tensor(X_test).view(-1, 1, 28, 28) y_test = tc.tensor(y_test.values) # # DataLoaders train_loader = DataLoader(TensorDataset(X_train, y_train), batch_size=64, shuffle=True) val_loader = DataLoader(TensorDataset(X_val, y_val), batch_size=64) test_loader = DataLoader(TensorDataset(X_test, y_test), batch_size=64) The last step above is to set up the data loaders. First, TensorDataset() is a function that pairs the features $X$ with the labels $y$ in a tuple. The function DataLoader() then provides an iterator that iterates over minibatches of the dataset with the batch size, in this case, being $64$. We also shuffle the training data so that the model doesn’t learn the ordering and can generalize better. Now that we have taken care of the dataset, let’s move on to the actual CNN: class SimpleCNN(nn.Module): def __init__(self): super().__init__() self.conv = nn.Sequential( nn.Conv2d(1, 32, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2), nn.Conv2d(32, 64, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2) ) self.fc = nn.Sequential( nn.Flatten(), nn.Linear(64*7*7, 128), nn.ReLU(), nn.Linear(128, 10) ) def forward(self, x): x = self.conv(x) return self.fc(x) Let’s unpack the class. First, we pass in nn.Module as an argument into the class and write super().__init__() in the constructor. This is called inheritance and ensures that all the functions normally available in the parent class nn.Module are also available in the child class, which is our define_CNN() class. In the constructor you will see nn.Sequential(), which is a container module that lets you build neural networks by stacking layers and activation functions sequentially. This container module is used in two different places, to define the CNN self.conv and the multi-layer perception (MLP) self.fc. The reason we define both a CNN and a MLP is because the CNN’s sole focus is to learn the features, while the MLP is supposed to take those features and map them to a final output (category). In this case, the CNN is known as the backbone and the MLP as the neck. You may ask, “Why separate them?” Here are some reasons why we may want to build a model with a separate backbone and head: Modularity: You can change the convolutional part without touching the fully connected layers, or vice versa. Example: Add more convolutional layers, or replace self.fc with a different classifier. Conceptual clarity: self.conv –&gt; extract patterns self.fc –&gt; decide which class based on patterns Industry standard: Most CNNs follow this “feature extractor + classifier” design, which makes it easier to swap backbones or use pretrained models. Let’s look at the model and break down each layer: The convolutional neural network: nn.Conv2d(1, 32, 3, padding=1) This is a two-dimensional convolutional layer with $1$ input channel, $32$ output channels (learning $32$ features), and a convolutional kernel size of $3$. Finally, we zero pad with size $1$ to keep the spatial size of the image the same as the input since the function defaults to stride=$1$. nn.ReLU() This is a Rectified Linear Unit activation function. It is defined as $\text{ReLU}(x) = \text{max}(0, x)$. Essentially, it zeros out the negative values while introducing non-linearity. This type of activation function is simple and fast to use compared to others such as $\text{tanh}(x)$ or sigmoid. nn.MaxPool2d(2) This is a two-dimensional max pooling layer meant to reduce spatial dimensions while keeping important information. In this case, $2$ denotes that a $2 \times 2$ kernel is used. The max pooling layer will then take a $2 \times 2$ window and slide it over the feature map, taking the maximum value within the window and assigning it to the corresponding location in the output. nn.Conv2d(32, 64, 3, padding=1) This convolutional layer now has $32$ input channels, $64$ output channels, meaning it is meant to learn more finer details than the previous one since the outputs channels have now increased. nn.ReLU() Same as before. nn.MaxPool2d(2) Same as before. The multi-layer perceptron: nn.Flatten() This is used to flatten the input tensor. The output of the CNN is a four-dimensional tensor. Since the input to the MLP is the output of the CNN, the tensor has to be flatten. nn.Linear(64*7*7, 128) This is a fully connected linear layer with input size $64*7*7$ (output of CNN) and output size of $128$ neurons. nn.ReLU() Same as before. nn.Linear(128, 10) This is another fully connected linear layer, but now the input size is $128$ and the output size is $10$, which matches how many categories we have ($0$-$9$). The last part of the class is the function forward() that calls the CNN, then passes the output of the CNN to the MLP so that the features can be mapped to a category. Now, let’s see how the model is trained. First, we need to initialize the CNN, the optimizer, and the loss function: cnn = SimpleCNN() optimizer = optim.AdamW(cnn.parameters(), lr=0.001) loss_fn = nn.CrossEntropyLoss() In this case, we will use the optimizer optim.AdamW() with an initial learning rate of $0.001$. This optimizer is a popular choice since it takes the fast convergence of Adam (another optimizer) and the generalization properties of SGD (optimizer we used last week) with weight decay. For more information on this optimizer see here. The loss function we will be using is cross-entropy loss. This loss function measures the difference between two probability distributions and is widely used for classification problems. For more information on this loss function see here. The training will look like the following: for epoch in range(3): cnn.train() correct, total = 0, 0 # Trains over the training set for X, y in train_loader: optimizer.zero_grad() output = cnn(X) loss = loss_fn(output, y) loss.backward() optimizer.step() preds = tc.softmax(output, dim=0).argmax(1) correct += (preds == y).sum().item() total += y.size(0) train_acc = correct / total # Evaluates over the validation set cnn.eval() # put model in evaluation mode correct, total = 0, 0 with tc.no_grad(): # no gradient tracking during evaluation for X, y in val_loader: output = cnn(X) # forward pass preds = tc.softmax(output, dim=0).argmax(1) correct += (preds == y).sum().item() total += y.size(0) val_acc = correct / total print(f"Epoch {epoch + 1}, Train Accuracy: {train_acc:.8f}, Validation Accuracy: {val_acc:.8f}") I won’t go over all the details since some of this code is present in last week’s post, so I will only go over the new additions. In the training portion, we now implement tc.softmax() on the outputs, which converts the output prediction to probabilities that overall sum to $1$. The method .argmax(1) finds the index of the maximum value of each row. In essence, we are picking out the prediction that has the highest probability of matching up to the true target. The next line calculates how many of our predictions are actually correct, while the line after find the total number of targets that the model is supposed to predict. With these values, we can calculate a percentage of correct predictions (given by train_acc). Like I previously mentioned, we use a validation set to determine how well our model generalizes between epochs. The next part of the loop does exactly this. We first put the model in evaluation mode using .eval(). While in evalulation mode, the weights of the model are not changed with the new data and processes like dropout are deactivated. The rest of the loop is identical to the training portion, which is just calculating the percentage of correct predictions (given by val_acc) with the validation set. After both calculation, we print out the current epoch and the accuracies. The model is trained and the validation set accuracy computed for a total of three epochs. Once done training, our model is now full trained and ready to go. The next step is to see how the fully trained model generalizes with unseen data. We will use the same method of calculating the validation accuracy with the test data since the point is to see how well the model generalizes to unknown data. An example of what the print statement output looks like is the following: Epoch 1, Train Accuracy: 0.88814815, Validation Accuracy: 0.94385714 Epoch 2, Train Accuracy: 0.95839506, Validation Accuracy: 0.96385714 Epoch 3, Train Accuracy: 0.97007055, Validation Accuracy: 0.96928571 Test Accuracy: 0.97190476 These numbers will change when the training is reran since we are not seeding the random numbers in PyTorch. We have now fully trained a model composed of a CNN for feature learning and a MLP for learning the mapping between the data and the targets. We also included an evaluation over a validation set to check how well the model is generalizing to unknown data as it is being trained, and an evaluation over a test set to check how well the full trained model performs on unseen data. Overall, the model seems to be very accurate with a test accuracy of $\approx 97 \%$. That being said, there is still $\approx 3 \%$ of the test data that is not being categorized correctly. Let’s see a subset of these predictions to try to understand what is going on. misclassified_images = [] misclassified_preds = [] true_labels = [] cnn.eval() with tc.no_grad(): for X, y in test_loader: output = cnn(X) preds = tc.softmax(output, dim=0).argmax(1) mismatches = preds != y if mismatches.any(): misclassified_images.extend(X[mismatches].cpu()) misclassified_preds.extend(preds[mismatches].cpu()) true_labels.extend(y[mismatches].cpu()) Evaluating the misclassified images is similar to the evaluation block explained before. One of the differences is that we add mismatches = preds != y, which saves all the predictions that do not match up to the ground truth. We also include an if-statement that checks for any mismatches and, if found, saves the image, prediction, and ground truth. The method .cpu() moves the tensor from the GPU to the CPU. In this case, we don’t need to put it since we are training exclusively on the CPU, but it’s something to keep in mind. Plotting the misclassified images, we get the following: From the image above, we can begin to understand why the model is misclassifying some numbers. The number in the upper left is supposed to be a $7$, but it was written in such a way that it can potentially be confused with a $9$. The number in the upper right is supposed to be an $8$, but it really looks like a $9$. The number $2$ in the bottom row does look like a $2$, but it’s understandable that the model may confuse it with an $8$ since the upper and lower loops look like what someone would do to write an $8$. With the subset of misclassified images we have plotted, it makes sense that the model may struggle with these cases, especially since we only trained it using a small CNN and three total epochs. Overall, the model does a great job at correctly classifying the MNIST dataset, minus some small subset where the confusion can be attributed to messy numbers. As we saw, training a basic image recognition model is not difficult! Of course, these models are not perfect or fool-proof and with more complex images, we may need more layers, larger layers, or maybe more advanced machine learning techniques. Feel free to reach out if you have any questions about what we covered this week. Next time, I will show you how to modularize and make improvements to the code. Stay tuned! ⬅ Back to Home]]></summary></entry></feed>